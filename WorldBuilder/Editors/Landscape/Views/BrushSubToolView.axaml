<UserControl
    x:Class="WorldBuilder.Editors.Landscape.Views.BrushSubToolView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:WorldBuilder.Editors.Landscape.ViewModels"
    d:DesignHeight="450"
    d:DesignWidth="800"
    x:CompileBindings="True"
    x:DataType="vm:BrushSubToolViewModel"
    mc:Ignorable="d">
    <StackPanel Spacing="10">
        <TextBlock
            FontSize="14"
            FontWeight="SemiBold"
            Text="Brush Settings" />

        <!--  Brush Radius  -->
        <StackPanel Spacing="5">
            <TextBlock Text="Brush Radius" />
            <Grid ColumnDefinitions="*,80">
                <Slider
                    Grid.Column="0"
                    IsSnapToTickEnabled="True"
                    Maximum="200"
                    Minimum="0.5"
                    TickFrequency="0.5"
                    Value="{Binding BrushRadius}" />
                <NumericUpDown
                    Grid.Column="1"
                    Margin="6,0,0,0"
                    Minimum="0.5"
                    Maximum="200"
                    Increment="0.5"
                    FormatString="F1"
                    ShowButtonSpinner="False"
                    Value="{Binding BrushRadius}"
                    FontSize="11"
                    Padding="4,2" />
            </Grid>
        </StackPanel>

        <TextBlock
            FontSize="11"
            Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
            Text="Select a texture from the palette on the left."
            TextWrapping="Wrap"
            Opacity="0.7" />
    </StackPanel>
</UserControl>
