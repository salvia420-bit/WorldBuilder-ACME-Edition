# PIPELINE RE-ENGINEERING — IMPLEMENTATION TRACKING (BINDING PROTOCOL)

<!-- ============================================================ -->
<!-- HEADER — NORMATIVE. EVERY IMPLEMENTATION AGENT IS BOUND BY IT. -->
<!-- ============================================================ -->

## PROTOCOL HEADER — READ FIRST, ADHERE COMPLETELY

This document governs the implementation of `SPEC.md` (this folder — the authoritative
architecture + implementation spec produced by the 12-pass effort; `TRACKING.md` and the
pass files are its design history). One agent implements one task at a time; at most two
agents run concurrently, with disjoint file scopes. An orchestrator session enforces
this header; agents do not modify it.

**RULES — violations invalidate the task:**

I1. **Read order is mandatory.** (1) This file top to bottom; (2) `SPEC.md` §0–§2 plus
    your task's entry in §3 and its row's cited findings; (3) the pass files your task's
    SPEC entry cites (for full-fidelity design detail); (4) the current code you will
    touch. SPEC.md is authoritative; pass files are rationale; where they disagree,
    SPEC.md wins.

I2. **Scope is your task only.** Your assignment lists your file scope. Files outside it
    may be READ freely but edited only when unavoidable for your task, minimally, and
    recorded in your report. Never touch another in-flight task's scope (listed in the
    table below as ACTIVE).

I3. **Deviations need evidence.** If implementation reveals SPEC.md is wrong or
    unimplementable as written, do the minimal sound thing and record a
    `DEVIATION: <spec §> because <read-verified evidence>` block in your report. Never
    silently drift. Never edit SPEC.md (the orchestrator propagates accepted deviations).

I4. **Read-verify before you assert or alter.** file:line claims come from files opened
    this session. Traps: the wasm crate is `apps/holtburger-web/src/lib.rs`
    (`crates/holtburger-web/` does not exist); `pkg/` is gitignored build output; URL
    flags share `flagIsOff` — trust `*Enabled()` functions; `dist` is a symlink to
    `/mnt/wbterminal2/holtburger-dist-hires-bc7m-xu7t2`; never pass `-rln` to rg.

I5. **Build rules — this is an 8 GB laptop that OOMs.** Before any Rust build:
    `kill $(pgrep -f rust-analyzer)`. Rust builds ONLY via
    `env PATH="/home/wbterminal/.cargo/bin:/usr/local/bin:/usr/bin:/bin" capped-build cargo build -p <one-package> --release`
    — single package, NEVER `--workspace`, never bare cargo. wasm ONLY via capped-build
    wasm-pack with `--out-dir pkg-<task>/` then `rsync -a --delete pkg-<task>/ pkg/`
    (parallel builds clobber pkg/; back up pkg/*.wasm first; release before any
    measurement — ~4.5 MB = release, ~18 MB = dev). JS/node tests run direct. At most
    ONE headless chromium per agent; prefer node-based tests. Big artifacts (bakes,
    corpora, captures) go to `/mnt/wbterminal2/reeng/<task>/` — the system disk is
    85–96% full; never bake into the source tree. Bakes read `~/ac_base_dats/` only
    and emit `bake-source.sha256`.

I6. **Commit discipline.** Work directly in the holtburger repo. Stage ONLY your scope's
    files (`git add <paths>`, never `-A`). Commit style matches the repo: one summary
    line `holtburger: <what>` + a body that records what was measured/decided (read
    `git log` for the house voice). Multiple commits are encouraged where SPEC asks for
    bisectable landings. If `index.lock` contention occurs, wait 5 s and retry (another
    agent may be committing). NEVER push, never rebase/rewrite, never commit files you
    don't own — if unrelated dirty files exist, leave them staged-out and say so in
    your report.

I7. **Flag lifecycle.** New behavior lands behind its SPEC-named flag, DEFAULT-OFF.
    Implementation agents never flip defaults — default flips are migration events
    (SPEC §3 serialization policy) executed by the orchestrator after gates pass.
    The OFF arm must remain byte-identical legacy behavior (that is the kill path).

I8. **Output contract.** Exactly one report: `impl/task-TNN-report.md` in this folder,
    sections in order: `## Shipped` (files + commit hashes) · `## Spec conformance`
    (every acceptance bullet from your SPEC §3 entry: MET / DEFERRED-TO-BATCH /
    FAILED, each with evidence) · `## Deviations` (or "none") · `## Tests run`
    (command + result, @scale tags where the measurement protocol applies) ·
    `## Handoffs & risks`. Then update YOUR ROW ONLY in the status table below
    (status → DONE or BLOCKED, date, report file, ≤2-line summary). Touch nothing
    else in this file.

I9. **Honesty over completeness.** Failing tests are reported failing. Gates needing
    the 1070 or an owner eye are marked DEFERRED-TO-BATCH, never simulated. A task
    that cannot meet its acceptance gate reports BLOCKED with the evidence — that is
    a compliant outcome; a papered-over gate is not.

**ORCHESTRATOR DUTIES:** at most 2 agents in flight (hard cap); disjoint scopes
enforced at launch; verify each report against I8 and the SPEC gate before marking a
task done; sequence default flips and doc propagation; keep the user advised of which
tasks are active.

<!-- ============================================================ -->
<!-- END NORMATIVE HEADER                                          -->
<!-- ============================================================ -->

## Task status

Charges are abbreviations — SPEC.md §3 is the authoritative statement of each task.

| Task | Stage | Charge (abbrev.) | Size | Status | Date | Report | Summary |
|------|-------|------------------|------|--------|------|--------|---------|
| T00 | spike | class-cardinality census over today's materials | S | DONE | 2026-08-09 | impl/task-T00-report.md | Harness+35-check test (0ad7e151); LIVE census ran run-3 (4.3 GB avail, no earlyoom, wasm 1f6f57c9 release, commit 4ecc8ec6). VERDICT: RE-EXAMINE — nanto 122 classes/352 pools, townnetwork 80/274 vs bounds 48/300; texDims is the fragmenter (+92/+54; 30/26 classes without it), state/patch core sound. R-03 MEASURED: T22 must NOT size until pass-7's tex axis is re-keyed (candidate keys re-reducible offline from /mnt/wbterminal2/reeng/T00 snapshots). Both scenes settled:false; TN vfx-config floor — recorded in report. |
| T01 | found. | diag-schema registry, RESULTS-v2 writer, console allowlist | S | DONE | 2026-08-08 | impl/task-T01-report.md | 4 commits (e1349f13…85c9d497): registry (20 surfaces) + lint, @scale-enforcing writer, QuickEmote-seeded allowlist, moving-bench→v2. Tests 60/39/20 green + cam-bench 38/38; run-all --js RED is pre-existing (17 unregistered app-root suites, none T01's). |
| T02 | found. | manifest/fetch caller sweeps → T12 inputs | S | DONE | 2026-08-08 | impl/task-T02-report.md | Findings in impl/task-T02-findings.md: 0 strict dist-manifest parsers (additive v2+ fields safe repo-wide); F-11.6 class = terrain + 6 more world-data tracks (scenery/spawns/events JSONL, suite bins, vfx catalog, wcid_to_setup) incl. wasm-side direct fetch_bytes callers (F-C) + suite lane gap (F-A) + external stars.bin (F-B). |
| T10 | ST1 | dual-emit bake (HBP1/HBSI1, walk-widening, previews, t128, manifest fields), serve rules, BAKE-CI | L | DONE | 2026-08-09 | impl/task-T10-report.md | 3 commits (1a02e73e, ebc47f56, b298d976): widened walk + additive manifest fields; HBP1/HBSI1 emitter (`--emit-packs --legacy-layers`) + xu7 preview deriver; serve.py pack tier + BAKE-CI. Bounded-region GATE-BAKE green (closure/missingPvw=0/determinism/differ 2022 models+3365 cells); legacy A/B byte-identical ex generated_at. Full-world bake = buildbox (report Handoffs); 5 recorded deviations D1–D5. |
| T11 | ST-SHELL | esbuild bundle + content-hashed shell | M | DONE | 2026-08-09 | impl/task-T11-report.md | 3 commits (85a50065, 14cb0671, +deploy/tests): build-shell.mjs (esbuild 0.28.2 standalone, main + 4 verified workers, deterministic sha256-8 shell/, generated index-bundled.html loader; index.html byte-untouched = kill path) + serve.py immutable-identity shell tier + deploy CAS staging + 56-check suite. Cold shell 7–9 req (≈8 ✓) vs 267 unbundled; warm 0 immutable refetches. T2-BOT/1070 floor + comparative browser run DEFERRED (RAM/shared-chromium death); D4 plugin-lane finding (~95 arms-invariant requests + double-instancing) needs an orchestrator call. ORCHESTRATOR-VERIFIED per I8 2026-08-09: report complete, tests re-run green (build-shell 56/56, diag-schema 65/65; url-flags lint clean of T11 rows — the in-flight slotGrid row is now documented). |
| T12 | ST2 | pack client (PackFetchController, PackSource, SW v3) `?packSource` | L | DONE | 2026-08-09 | impl/task-T12-report.md | 5 commits (5e284731…156ecd40): controller (lanes/promotion/latch/quarantine/sha-on-receipt) + PackSource/CompositeSource seam + SW v3 dormant + wiring, all DEFAULT-OFF. Local battery green over T10's region: 0 mismatch / 0 terminal quarantines, 7,228 records byte-identical to base DATs. Comparative arms + T2-BOT DEFERRED (need full-world dual bake / RAM); 5 deviations D1–D5 (incl. T02's F-2/3/5/6 exceptions + Blob-worker sha fallback). |
| T13 | ST3 | HBG1 geometry bundles + consumer swap `?geomBundles` | L | DONE | 2026-08-09 | impl/task-T13-report.md | 9 commits (9d6f5205…5865018c): HBG1 codec (holtburger-dat) + bake GEOM emission with verify_geom/BAKE-CI legs + PackSource registration + assemble_model/envcell_geometry exports + bundleToGeometryGroups + the four consumer swaps in card order (one commit each), all DEFAULT-OFF behind `?geomBundles` (requires `?packSource`; forces gfxRelief off — report D3). Differ green at both layers: BAKE-CI 1,927 rows byte-identical re-encode (bounded region, 214 s) + native runtime-vs-bundle differ 300 GfxObjs EXACT / 150 setups position-exact vs base DATs. Release wasm shipped (6.40 MB); new 54-check JS battery + all touched-family suites green (test_envcell_guard fails identically on clean HEAD — pre-existing). E1 + P-ASSEMBLE + parked-mid p50 DEFERRED-TO-BATCH (Batch A queue prereqs updated with verified spellings); 6 deviations D1–D6 incl. the pass-4 S1 size-sum fixes and the relief interplay. |
| T14 | ST4 | texture worker `?texWorkers` | M | DONE | 2026-08-08 | impl/task-T14-report.md | 2 commits (d8cfd82e, 23383e2a): worker (transcode+terrain-assemble+NRA, results-enqueue-only) + `?texWorkers` DEFAULT-OFF routing with the FIFO arm verbatim and counted-never-silent fallbacks. Node suites 118/118 new + all texture-family regressions green; byte-identity proven vs Rust goldens AND real transcoder/corpus. GATE-TEXWORKER 1070 arms DEFERRED-TO-BATCH. |
| T15 | ST5 | compressed-only texture path `?texCompressedOnly` | L | DONE | 2026-08-10 | impl/task-T15-report.md + impl/task-T15R-report.md + impl/task-T15R-TERRAIN-report.md | 5 commits (3c49c17d…): PVW/TEXREF pack registration + scalars-only meta + lane-T CAS info (Rust); preview-born materials, lane-T upgrade + worker-NRA, preview-commit + atlasRefeed re-home (throwaway, seam producer-agnostic), full-chain+aniso arrays, ×1.5 growth, 128 MB budget, demoteToPreview — all DEFAULT-OFF, OFF arm suite-proven identical. New 84-check battery + all texture-family neighbors green; release wasm shipped. GATE-TEX (E2/E3, M4-M6, B1, C4, frame-1-white) DEFERRED-TO-BATCH (T31 riders now fillable); 3 deviations D1–D3 (hasPalette/category scalar gaps w/ TEXREF-bit successor; missing-PVW legacy-route pre-ST7; shard-CAS lane-T substrate). **REMAINDER 2026-08-10 (T15R, impl/task-T15R-report.md; commits 04806ad6, 10e685f4):** rehydrate-v3 row 2 CLOSED — full-tier CPU mirrors now arm a source-keyed rehydrator (re-fetch xu7 CAS -> worker re-transcode, shared with the upgrade) and are RELEASED when the record cache evicts them, so the 128 MB budget frees real heap; `demoteFullTierUnderPressure` lands the H-05.1 R1 texture rung. Battery 84 -> 112, all texture-family neighbours green, no wasm change. Terrain ladder + terrain-mirror freeing STILL QUEUED (T15R D1: `terrain_bc7.js`, outside that task's scope). DEFAULT-ARM VERDICT for E1: `fullSwaps=0` with the flag OFF is a counter-naming artifact (ST5-only counter) — the same capture shows `singletonUpgrades=306` and 112 MB of full-tier bytes, so the packs arm does NOT lose the full-res tier; the softness candidate is the untracked atlas hold-out class (T15R report C1) and E1's re-run should add a `texCompressedOnly=on` arm plus a `deferredNodes` read on both. **REMAINDER CLOSED 2026-08-10 (T15R-TERRAIN, impl/task-T15R-TERRAIN-report.md; commit f8ee7508):** the terrain tier ladder ships behind `?terrainT1024` (DEFAULT OFF = absent = today's t1024-first boot, the kill path; `defer`/`eager` arm it, `off` PINS t128) — boot builds the 33-layer pair at 128² from the D-12.6 lane-B slice packs (terrain converges at t128), the full tier is promoted WHOLESALE IN PLACE (same texture objects ⇒ zero consumer edits; `terrain.js` untouched) and staged as two `renderer.initTexture` calls, one array per frame, per P-88MIB. D-05.7 row 1 lands with it: terrain mirrors are FREED post-upload with the way back registered first (source-keyed re-fetch→re-assemble), and `demoteTerrainUnderPressure` is the H-05.1 R1 terrain rung. LIVE-PROVEN on the deployed pack dist (SwiftShader/T2, `terrainBc7=512` promote target): sliceSource pack, 2 lane-B requests / 1,270,680 B, t128 in 179 ms, promotion 1,376 ms, **22.0 MiB of mirrors freed**, 0 console errors; OFF arm reads mode `absent` with every counter 0. New 105-check battery + all texture-family neighbours green. 7 deviations — the load-bearing ones: D1 the ABSENT 4th flag state (SPEC's 3-value grammar becomes whole at the flip), D4 terrain mirrors ride `texture_rehydrate.js` NOT `registerFullTierMirror` (that seam re-adopts into the 128 MB record budget SPEC keeps terrain out of; `bc7_textures.js` untouched), D5 `initTexture` staging is a CORRECTNESS requirement (three live boots: arrays still un-uploaded 150 s post-promotion, so mirrors freed 0), D6 pass-05's t128 GPU figure is 1.38 MiB not ~0.9. GATE-TEX gains a terrain leg (all 1070/owner).|
| T16 | ST6 | q75 corpus + owner decisions (bake-side) | M | DONE (encode; E4+decisions owner-gated) | 2026-08-09 | impl/task-T16-report.md | q75 no-RDO corpus encoded on the buildbox (3,985/3,985, 0 FAIL, sha-verified) → /mnt/wbterminal2/xubc7-corpus-q75; 36 E4 sheets regenerated; GATE-Q75 + both in-writing decisions DEFERRED to owner redmi (Batch B). NEW [M]: corpus q75/lossless = 0.690 (vs 0.644 projected) → B4a ≈ 69.6 MB [D] at q75 — over the ≤65 gate; election evidence in the report. |
| T20 | ST7 | slot grid residency authority `?slotGrid` | L | DONE | 2026-08-09 | impl/task-T20-report.md | 5 commits (432e8791/4a07e021/b98d315c inherited+verified; 5575c55f S7.3 docs; 107baf22 live-arm fixes: init3D export bag, STAGED refire, EMPTY readmit, controller keep-set). Release wasm shipped (6.34 MB). LIVE arm on the full-world pack dist: 14 crossings + zigzag + teleport ended gridLruDivergence=pinLeaks=shiftMismatches=slotDesyncs=0, verify 126/0, 0 quarantines, 0 console errors; suites 394+25 + all neighbors green. E5/M1-M2/BENCH-*/TAIL-ULTRA DEFERRED-TO-BATCH (T32 unblocked); D4 flags R4-vs-migration-era-M3 for the orchestrator. |
| T21 | ST8 | FrameWorkScheduler stage A `?frameWork` | M | DONE | 2026-08-09 | impl/task-T21-report.md | 4 commits (7731250a…80761d45): scheduler core (W1..W6, modes, shrink, CROSSING lever) + all six 6 ms families and inline tickEviction as W6 clients (code unchanged, OFF byte-identical) + `__framePhase`/`__frameWork` landed in registry + census reducer. 144+25 new checks, all touched-family suites green. GATE-PHASE census run + BENCH-CROSS-SETTLE DEFERRED (memory / 1070 batch). |
| T22 | ST9 | draw pools + scheduler B/C + closed-class prewarm `?drawPools` | L | DONE (substrate + producer swap; remainder: envcells + the worker-side ladder) | 2026-08-10 | impl/task-T22-report.md + impl/task-T22P-report.md | 6 commits (a62e1a22…56e63f33): pool_class_key (array-page tex tier — the T00 re-key APPLIED to code; census re-pointed at the same key, offline re-reduce of the real snapshots reproduces 63 cls/271 pools/24 prog Nanto + 51/238/23 TN, WITHIN-BOUNDS) · pool_registry (sector×class BatchedMesh pools, D-07.4 early-out pair, S2 transitions, exact-key dedup, F-11.3 loud chain, F-11.18 fix verified at pool scale) · tile_plan (+ bake worker `tileBake`) · pool_stream + upload_stage TOGETHER (stage B P4 relocation + F-11.19 bake dispatch + stage C initTexture staging + F-11.10 nullRender rule, sharing the S2.4 LIVE-flip ordering) · pool_prewarm (closed-class colour compile + the CSM depth-variant warm RENDER over REAL BatchedMesh proxies). New 333-check battery + 17 neighbour suites green; OFF arm additive-only (one pre-existing file, 23 added lines, 0 removed). **REMAINDER: the PRODUCER SWAP** — page-dim resample at bake/transcode (D2, hard prereq), `MaterialCache.getClassMaterial`, the worker-side record→axes ladder (D1), the grid's four event hooks (unwired at index.js:6306), the statics/buildings/cells consumer swaps, the `atlasRefeed` pool handler, and the `__diag.pools`/`__prewarmStats` registry flips (D6). GATE-POOLS E6/MOVE-FIX/TAIL-ULTRA/ktris DEFERRED-TO-BATCH — E6 is not yet RUNNABLE (no pooled world). 6 deviations D1–D6. **PRODUCER SWAP LANDED 2026-08-10 (T22P, impl/task-T22P-report.md; commits 473c056b, 52f8d82d, cfd4bc1d, 3340c79f, d7a035ba, 0b8d98e8):** the class-material tier (`pool_material.js` — classKey -> ONE array PAGE at page dims + ONE `makeArrayMaterial` material + refcounted layers, reusing the atlas's own allocation/growth/layer-write primitives; `static_atlas.js` untouched except one `export`), `pool_producer.js#addSingletonsToPools` at the ATLAS'S OWN SEAM, the statics and buildings swaps (one commit each), the four SlotGrid hooks wired at last, `poolTickP4` in the P4 slot, the boot prewarm + `sealClassSet()` on ring-settle, the pool `atlasRefeed` handler, and the D6 diag flips (`__diag.pools`/`__prewarmStats` reserved -> current). **LIVE-PROVEN** on the deployed pack dist (ONE headless SwiftShader arm, agentp09, six-flag chain, Nanto): **51 pools / 17 classes / 33 resident tiles, `classesCreatedPostBoot=0` (sealed), parked `mutationsThisFrame=0` on two reads, 36/36 grid slots LIVE, shiftMismatches=slotDesyncs=planErrors=unresolvedGeometry=0, `fix.applied=true`, prewarm 17 colour programs in 14.2 ms, 0 console errors.** Battery 333 -> 413; all neighbour suites green. **E6 IS NOW RUNNABLE** — but the arm is a MIXED producer: 666 of 815 offered nodes route LEGACY and are counted (bc7Pending 363, deformed 218, **needsResample 85** — the D2 gate consuming the exported predicate exactly as required), and the ENVCELL domain is untouched. 7 new deviations: D1 the class is derived MAIN-THREAD from the resolved material (divergence impossible by construction; the worker ladder is now a relocation, not a rewrite), D2 class-page layers are session-resident in v1, **D3 the envcell swap is the recorded remainder** (layer-1 isolation + per-cell PVS + the portal ticks — three read-verified blockers, designed shape recorded), D4 the live census counts the POOLED SUBSET so 17/51 is a floor and NOT the T00 comparison, D5 the harness settle never plateaued though the GRID settled (taint recorded), D6 one out-of-scope `export`, **D7 the TEXREF page stitch** (leg 6, commit 4d9ddbd8 — PAGE-RESAMPLE's Handoff #2 closed: pooled members now key on the DECLARED page dims with the `FULL_PAGE_DIMS` bit as authority, never the dims byte; battery 396 -> 413) taken with two recorded refinements — the f7/f8 bit comes from the LIVE texture so the class key can never disagree with what the page allocates, and DECLARED != RESIDENT (not the bit alone) is what routes a member legacy, because refusing on the bit alone would have zeroed the 51-pool arm on today's pre-resample dist. **TWO [M] NUMBERS FOR THE ORCHESTRATOR:** pool geometry allocated:used reads ~55x (22.3 vs 0.4 MiB) — `POOL_INIT_VERTS`/`POOL_INIT_INSTANCES` are `[A] re-classed by the first soak` and THIS WAS THE FIRST SOAK; and the 17 class pages allocate 127.8 MiB, an M4 rider once envcells + the resample residue join. **RSID-MARKER CLOSED 2026-08-10 (T22P Handoff 3; impl/task-RSID-MARKER-report.md; commits 2e4c4018, 47205788):** the `bc7Pending=363` hold-outs are re-offerable at last. Cause was a MARKER GAP — `__pvwRsId` is written only for preview-BORN materials and `__bc7RsId` only after a full tier LANDS, so a material in `__bc7Pending` (precisely the refused state) carried NEITHER, which is why the live arm read `holdoutRsIds=0` beside 363 refusals. Leg 1 (texture lane): `stampRsId`/`materialRsId` — a universal `__texRsId` IDENTITY stamped in the SAME synchronous breath as `__bc7Pending`, before the `_bc7Asked` ask-once gate (so a rebuilt material is stamped too), on the ST5 preview-born path, and inherited by every variant clone; plus **pass-05 S8 point 3 landed on the X6 path at last** — `upgradeMaterialToBc7` calls `atlasRefeed(rsId)` when the verdict SETTLES, on ALL THREE outcomes (landed / absent / failed), because a negative verdict settles dims/format just as finally. OFF-arm identity is STRUCTURAL, not asserted: `_rsMembers` is populated only under `texCompressedOnlyActive() && __texFullPending` (both sites read-verified), so on the DEFAULT arm — where `?texBc7` is ON — the new call is one Map miss per verdict. Leg 2 (pool feed, at T22P's own already-built but UNREACHABLE seams): `_rsIdOf` -> `materialRsId` is the one line that turns the empty ledger into a real one, plus the counted ledger (`heldOut`/`heldOutNoRsId`/`heldOutDupes`/`reOffered`/`reOfferAdmitted`/`reOfferStale`/`reOfferRefused` per reason, all registered) and two guards found by reasoning about a loop that was reachable for the first time: `refeedRsId` refuses a FORMAT-mismatched rewrite (an RGBA8 page written from a CompressedTexture would ZERO the layer — the member goes BLACK), and the hold-out mark is cleared BEFORE the re-offer so a still-pending member is RE-FILED, never dropped (F-11.17 'nothing can stick' applied to the ledger itself). New 87-check battery + 24 neighbour suites green; `test_draw_pools` 494/0; no new flag (rides `?drawPools`), no browser spent. 4 deviations — D1 the two pool-file edits (the entry point existed but was unreachable), D2 `offPage` deliberately NOT filed (a bc7 verdict ALWAYS settles so that ledger drains by construction; an off-page refusal has no event, so filing it would retain nodes for the session = a leak I would be creating), D3 a narrow ST5 churn case that self-heals via re-tracking, D4 the two guards. **DEFERRED-TO-BATCH:** the live bc7Pending-shrink read — `producer.heldOutNoRsId` MUST read 0 or the stamp missed a material class. |
| T30 | batch A | 1070 queue batch A (E1 + probes) — queue-file prep | S | PREPARED (execution owner-gated) | 2026-08-09 | impl/task-T30-report.md | Queue at queue-1070/batch-A-2026-08-09.json (hb-1070-queue-v1, 10 items): E1 + 5 probes (SPEC card) + T11 shell arms (D4 recorded: plugin lane accepted, ~95 reqs → B2 ledger) + T14 texWorker arms + cadence-optional MOVE-FIX baseline. E1/P-ASSEMBLE carry T13-TODO prerequisites; all flags/surfaces read-verified (kickDance stale, geomBundles absent on HEAD). |
| T31 | batch B | 1070 queue batch B (E2–E4) — queue-file prep | S | PREPARED (execution owner-gated) | 2026-08-09 | impl/task-T31-report.md | queue-1070/batch-B-2026-08-09.json: E2+E3+E4 (owner redmi; T16's two in-writing decisions carried) + T11-D5 bundled-shell eye item (deviation: pass-10 assigns it no batch) + 2 GATE-TEX bench riders (cmd null until T15). E2/E3 prereq T15 landed; E5 stays Batch C with a D-09.5 pull-in note. |
| T32 | batch C | 1070 queue batch C (E5–E6) — queue-file prep | S | PREPARED (execution owner-gated) | 2026-08-09 | impl/task-T32-report.md | queue-1070/batch-C-2026-08-09.json: E5 against the landed `?slotGrid` arm (prereq: T20 stage-4 report green; integrity-counter checklist) + E6 (prereq T22; full F-11.3 flag chain, ClipMap/depth-bias/shadowed-town/baked-light vantages) + MOVE-FIX-ST9 rider + GATE-GRID bench-set placeholder. |
| T40 | ST10 | legacy retirement (fires on SPEC §3 conditions only) | M | TODO | | | |
| RELIEF-BAKE | post-ST3 | bake gfxRelief's material-identity relief into HBG1 GEOM variants `?reliefBundles` | M | DONE | 2026-08-10 | impl/task-RELIEF-BAKE-report.md | 4 commits (60395dbc, 53c6e266, f7e8b48c, a4f94b7f): `ReliefBake`/`encode_gfx_part_relief` codec + `GEOMR` (0x0C) variant section under `dat-shard --geom-relief` + PackSource registration + `assemble_model_geometry_relief` + the DEFAULT-OFF `?reliefBundles` consumer. Closes T13 D3: the bundles arm consumes baked relief instead of force-disabling it. Bounded region [M]: GEOM unchanged (1,927 rows / 2,238,672 B — identical to T13), GEOMR 125 rows / 83 of 796 models railed / +7,760 tris / 1.32 MB; all 796 defaults + all 125 variants byte-identical re-encode; fixture + real-DAT differs prove the baked variant equals the RUNTIME relief triangulation exactly (120 GfxObjs EXACT), and `differ_real_dats_models` re-ran unchanged (300/150). Battery 54→78, release wasm shipped (6,423,996 B). D1: relief ships as ADDITIVE `gfx_remodel` rails, not the retired displacement path — acceptance restated as "identical subset table + appended triangles + base untouched" and enforced at bake. D2 REMAINDER: ENV/interior variants (OP3 reads the per-CELL palette). E-RELIEF eye pair DEFERRED-TO-BATCH (needs a `--geom-relief` dist; assert `variantRowsResident > 0`). |
| PAGE-RESAMPLE | post-ST9 | bake/transcode resample of texture members to array-page dims (T22 D2, the producer swap's hard prereq) | M | DONE (bake half; corpus-wide encode = buildbox) | 2026-08-10 | impl/task-PAGE-RESAMPLE-report.md | 4 commits (6625e821, cb3674f4, 6eff9597, 8ab2eb3d): `page_resample.rs` (page-tier arithmetic + an exact-rational INTEGER area kernel — integer upscale degenerates to texel replication, so a box mip chain reproduces the original at level log2(k), byte-exact at 512→2048: the page costs VRAM, never sharpness) PINNED to `pool_class_key.js` by a test that runs the real JS module under node over 2,888 (hasTex,w,h) records; the `page-resample` derived-tier producer (refuses `--src == --out`, identity members symlinked ⇒ byte-identical, plan + `page-resample.sha256` + `bake-source.sha256` + PROVENANCE.md, `--verify-deterministic`); the bake wiring (KTX2-header-declared TEXREF dims + `FULL_PAGE_DIMS` tier bit 5 + on/off-page census + `verify_texref_pages` + `--require-page-dims`); and `texRefPageInfo()`, the ONE client-side read, counted and inert. **GATE GREEN** on T10's bounded region [M]: full-tier off-page **185 → 0** (on-page 228 → 413 of 462 rows), measured through real KTX2 headers — 185 members resampled + re-encoded locally, 0 FAIL. T10's `bake_ci_bounded_region` re-ran unchanged (differ 2,022 models / 3,365 envcells, missingPvw 0). **TWO FINDINGS.** (1) TEXREF declared the DAT record's dims while the shipped full tier is the 4× upscale corpus — 400/400 exactly 4×, and the page tier SHIFTS for **253 of 400** when keyed on the DAT record; the bake now reads the KTX2 header (D1). (2) The dims byte is 4 bits per axis of ceil(log2), so the corpus's 1096² member is indistinguishable from a real 2048² page — the BIT is the authority, the byte cannot be (D2), which is why the client reader exists. 6 deviations D1–D6 (also: D3 the gate is scoped to full-tier rows, 49/462 legacy-only stay off-page and are counted separately; D4 the pack/dist story = replacement payloads at bake via a sibling CAS `--tex-xu7` tier + one tier bit, SPEC silent; D5 the PVW preview tier is untouched — re-key §4 leaves the preview feed to T22, so `refused.needsResample` will not reach 0 on live dims until that call is made; D6 corpus-wide encode is a buildbox job). Full derived PNG tier produced + sha-verified (3,985 members, 2,676 identity). **NEXT FULL-WORLD BAKE:** encode `/mnt/wbterminal2/reeng/page-resample/src-corpus-pages/` with the UNCHANGED per-member basisu command (only 1,309 need encoding; the rest symlink), then run `run-world-bake.sh` with `--tex-xu7 <page-dim ingest>` **and** `--require-page-dims`. RISKS: encoded full tier +13.2% over the region [M] — the B4a election should see a world-scale figure first; and `pool_producer.js:195` must pass `texRef` from `texRefPageInfo()` or a page-dim dist buys the pools nothing. |

## Log (orchestrator only)

- 2026-08-08: Implementation phase opened. Slot policy: 1 critical-path + 1 independent, max 2. T10 + T01 launched.
- 2026-08-09: OOM incident (hard reboot ~03:10) killed T20 mid-task + the full-world bake; T11 finished just prior. Root cause + now-binding scheduling rules (R-MEM1..4), in-flight bake state, and full orchestrator state → `ORCHESTRATOR-HANDOFF.md` (keep that file current; it is the successor session's first read).
