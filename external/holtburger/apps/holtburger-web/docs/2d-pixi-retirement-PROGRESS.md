# 2D-PIXI retirement — execution ledger

Branch: `2d-pixi-retirement` (off `master` @ 1e6ee27d). Driven by the `/loop` self-paced loop.
Spec: `/home/wbterminal/from-vm/2d-pixi-export/docs/GROUND-TRUTH.md` (authoritative) + `PLAN.md` + `DELETE-LIST.md`.
Bar per item: local build green + no NEW test failures + (where runnable) bare-default loads/spawns/0-errors. **No 1070 eye-test.**

## Baseline (pre-existing, NOT caused by this work)
Tier-1 `run-all.mjs --js` is RED at branch point with **5 pre-existing failures** (verified by stashing all edits and re-running — identical set):
`test_a14_i1_input_controller.mjs`, `test_a14_i3_run_keys.mjs`, `tests/jump_charge_parity.test.cjs`, `test_a11_s0_blocking_particle.mjs`, `test_a11_s5_default_script_spawn.mjs`.
→ The per-item bar is therefore **"zero NEW failures vs this baseline"**, not absolute green.

Environment note: **playwright is ABSENT**, BUT the live stack is up (serve.py:8765 + wsbridge:8080 + ACE on UDP 9000/9001) and the **chrome-devtools MCP driver works** for a real browser smoke. App URL: `http://127.0.0.1:8765/apps/holtburger-web/`; bare-default smoke = that URL + `?nosw=1&autoLogin=1&account=tailnet1&password=tailnet1&autoSpawn=first` (NO `renderer` flag, to prove the 3D default). **SW-CACHE FOOTGUN (important):** the page registers a service worker that caches the app shell — a plain load serves a STALE `index.html` and your edits won't appear. ALWAYS append `?nosw=1` (unregisters SW + clears caches + cold-reloads once) for the smoke. Reliable in-page 3D signals: `canvas.getContext("webgl2")` truthy + `window.liveScene` falsy (2D skipped) + `getLocalPlayerGuid()` non-null (spawned); NOTE `__USE_RENDERER_3D`/`liveScene3d` are module-scoped, NOT window globals — don't probe them. Viewport renders dark under swiftshader (expected; visual fidelity is the 1070's job, not the smoke). **PRESERVED-MESSAGE FOOTGUN:** `list_console_messages includePreservedMessages:true` returns errors from the last ~3 navigations — a stale error from a PRIOR (reverted) load can look like a current break. ALWAYS confirm with `includePreservedMessages:false` (current navigation only); cross-check by whether the page actually reached in-world (it can't if it errored at boot). **GHOST-SESSION FOOTGUN:** `tailnet1` is single-login — if a prior smoke's WS is still alive when the next connects, ACE thrashes both ("Account In Use / booting currently connected account") → boot `error`, no spawn (NOT a code bug). Before each smoke: navigate the tab to `about:blank`, `sleep 18 & wait` for ACE to drop the session, THEN connect once. Confirm via ACE log `~/...ace-0612-restart.log` / wsbridge `~/...wsbridge-0611.log`. (Foreground `sleep` is blocked; use `sleep N & wait`.)

## Queue
### PHASE 1 — flip unified pipeline DEFAULT-ON (W6)
- [x] **1. unifiedDispatch default-on** — `index.html:4669` `v === "on"…` → `v?.toLowerCase() !== "off"` (mirrors `scene3d/loop.js:291`); comment + `docs/url-flags.md` row reconciled to default-ON. Verify: dedicated `test_a15_q4_renderer_neutral_core.mjs` **46/0 PASS**; Tier-1 JS baseline-identical (zero new fails); flag logic unit-checked (missing→ON, `=off`→OFF). Browser smoke: PENDING (batched). Commit: `c90d4ac4`.
- [x] **2. renderer=3d unconditional default** — inverted all **6** `get("renderer") === "3d"` → `!== "2d"` (sites 2343/4614/6038/7304/7336/7547) + comment + url-flags.md row. `?renderer=2d` = escape. Verify: logic (missing→3D, =2d→2D, =3d→3D); Tier-1 JS baseline-identical (zero new); **REAL browser smoke GREEN** via chrome-devtools + `?nosw=1` — bare default → webgl2 canvas + `liveScene` null + spawned in-world (localGuid 1342177287) + channel-join + **0 console errors**. Commit: `241ebf4e`.
- 3. flip rest default-on (SPLIT per-flag for bisectability):
  - [x] **3a. unifiedClone default-on** + doc-reconcile unifiedClientEvent/singleDriver (already on in code). `index.html:4638` → `v?.toLowerCase() !== "off"`. Verify: logic; dedicated `test_a15_q2_entity_update_clone.mjs` **21/0**; Tier-1 baseline-identical; browser smoke GREEN (clean connect → in-world 19.6s, 0 console errors). Commit: `2e19d8ac`.
  - [x] **3b. unifiedMotion default-on, CLASS-BY-CLASS** (user §9 ruling: defer locomotion for B-1). New `"default"` mode in scene3d/entities.js (UNIFIED_MODE `null→"default"`, +`UNIFIED_DEFAULT`; attack/death/cast/door/missile gates OR it; LOCO stays `=locomotion`/`=on` only) + animation.js `UNIFIED_MOTION_ON` (`(v??"")!=="off"`) + url-flags.md. `=off` all-off, `=on` all incl loco. Verify: gate-logic table; Tier-1 baseline-identical incl. both unifiedMotion tests PASS; browser smoke GREEN (in-world 19.8s, webgl2, 0 console errors, unifiedMotion null=default). Commit: `7b53b30a`.
  - [~] **3c. worldLifecycle=on — DEFERRED (not retirement-critical, unsafe standalone).** `worldLifecycle=on`'s canonical ObjectDelete defers eviction to the `world.tick()` sweep, which needs `unifiedTick=on` (default-OFF) + `maintPrune=on` (default-OFF) to actually evict/forward despawns; and `unifiedTick=on` has a known manual-movement collision hole (lib.rs:118) unless `unifiedTransition=on` too. So a safe flip requires the worldLifecycle→unifiedTick→maintPrune(+unifiedTransition) chain — a physics-risk unification sub-project, NOT a validated gate, and ORTHOGONAL to the 2D-render retirement (it's wasm entity-lifecycle dispatch, same for both renderers). Left default-off; flag to user as a separate follow-on. Phase 1 flag-flips otherwise COMPLETE (1, 2, 3a, 3b shipped).
### PHASE 2 — strip prerequisites (correctness refactors)
- [x] **4. `__doorStates` module-scope hoist** (GROUND-TRUTH §3.1) — added `window.__doorStates ||= new Map()` at module scope (index.html:4570, by entityMap); removed the 2D lazy-init (kept the liveScene link) AND the 3D-path fallback init (kept the write). Behaviorally neutral; unblocks the door split (item 6). Verify: Tier-1 baseline-identical; browser smoke GREEN (in-world 34.9s, `__doorStates instanceof Map`, 0 errors). Commit: `a99d9907`.
- [x] **5. client-prediction refactor — RE-SCOPED (no index.html change).** Direct inspection corrected GROUND-TRUTH §3.2: `setLastClientPrediction` is called from ONE site (index.html:11744), gated on `localEntry?.sprite` = **2D-only**, so it's **dead in 3D-default** and deletes cleanly with the 2D block (item 7) — no "re-home" needed. The real work (a 3D oracle-shadow tap from camera.js `predictedPlayerPos`) is additive but its correctness needs the `physics_replay` test, NOT the smoke → **folded into item 9**. Rust setter still exported (lib.rs:26875). GROUND-TRUTH §3.2 updated. Commit: `8c491973`.
- [x] **6. DoorStateChanged surgical split** (§6.1) — extracted the 2D PIXI door branch + `findClosestBuildingPart` + `__doorBuildingParts` to **legacy/door_2d.js**; removed from index.html (kept the shared `__doorStates.set`, the 3D branch `inst.root`/`playDoorMotion`, the door-state log de-referenced from `matchedPart`, and the `getBuildingPartForDoor` Rust export). Also dropped the now-2D-only `rotation` const. **CORRECTS GROUND-TRUTH §6.1**: `getBuildingPartForDoor` was 2D-only in JS (0 scene3d callers), not "used by both". Verify: removed symbols gone (matchedPart=0, others comment-only), legacy parses, Tier-1 baseline-identical, browser smoke GREEN (in-world 34.7s, `__doorStates` Map, 0 errors). Caveat: door *interaction* not smoke-exercised; 3D branch untouched + 0 3D readers. Commit: `76a31b70`.
### PHASE 3 — physical strip to legacy/
- [x] **7c. ensure* PIXI-tail SEAM** (GROUND-TRUTH §5b, highest risk) — DONE. Stripped ONLY the liveScene-gated PIXI render tails: ensureCellContainers (bakeCellTextures→buildCellsContainer→addChild), ensureTerrain (addLandblockToScene), and ensureLandblockObjects (whole pure-2D body, 92 lines → legacy). ensureBuildingAabbs is fully wasm (no tail). **KEPT all 4 wasm-populate halves** (populateTerrain/populateBuildingAabbs/populateStaticsAabbs/fetchEnvCells — 21 refs intact) + the 3 shared Sets + worldStreamer. Verify: zero live baker refs remain; **A15-Q4 seam-parity test 46/0**; Tier-1 baseline-identical; smoke GREEN — in-world, **currentCell 0xA9B4 (Holtburg) = wasm streaming fired in 3D**, 0 errors. index.html 9222→9108. (Minor follow-up: the 2 now-mostly-orphaned 2D Sets terrainMeshAddedLbs/objectsRenderAddedLbs left in place — 2 lines, not worth the risk now.) Commit: `324cb9d2`.
- [x] **8a. REMOVE pixi.js — MILESTONE.** Stubbed the last PIXI user (ensurePortalChip's `new PIXI.Text` chip — body retired, guards kept; it early-returns on !liveScene so dead in 3D), then removed `import * as PIXI` (1258) + the importmap `pixi.js` pin (931) + updated the doc comment. Verify: zero live PIXI.* refs (6 remaining are all comments); three/@takram/postprocessing pins KEPT; Tier-1 baseline-identical; **browser smoke GREEN — app loads + spawns in-world (19.6s) with `typeof PIXI === "undefined"`, 0 console errors.** holtburger now runs with NO pixi.js. index.html 9108→9097. Commit: `ca5ba439`.
  - [x] **8b. dead-2D cleanup — SCOPE CORRECTED (2 of 4 candidates are load-bearing → KEPT).** Removed the genuinely-dead: `let __deferredSpawnsOverflowWarned` (declared, never read) + `const deferredSpawns = []` (its only consumer, the 2D Spawn-replay, was retired in 7b — 0 code refs remained) + the dead renderHoltburg **2D else-block** (37 lines: the dangling `await renderNeighbourhood(...)` — extracted to legacy/render_2d.js in 7a-1, NOT imported, so a `ReferenceError` reachable only via the now-inert `?renderer=2d` escape; removed → `?renderer=2d` degrades to blank, KEEPING the `if (useRenderer3d)` guard). **KEPT (NOT dead, corrects the 8b plan):** (1) the backlog clone machinery `__scene3dEntityBacklog`/`__scene3dCloneEntityUpdate`/`__pushBacklog` — **scene3d/loop.js:2818 still drains it** at installSharedDrainHook (the Workstream-E pre-init3D buffer); smoke confirmed it buffered 119 events then drained 119→0. (2) the `useSharedDrain` early-return (loop.js:2366) — **load-bearing**: the net-frame pump (drainEvents→pumpNetFrame→`__scene3dEntityHook`, index.html:8105) is the ACTIVE drain in 3D; the early-return stops `drainEntityEvents3D` double-consuming. Removing it would split events across two consumers, not clean up. Left `__backlogOverflowWarned` (still used 3096) + minor dead colourMap computation (out of scope; touches the shared fetch path). Verify: `node --check` on the 7954-line module clean; Tier-1 JS **baseline-identical (5 pre-existing fails, 0 new)**; **browser smoke GREEN** — bare default → webgl2 + `PIXI===undefined` + `liveScene` falsy + spawned (localGuid 1342177287) + init3D completed (bootState "ready", liveScene3d set, real entityHook installed) + full 3D world-stream (2334 building tris / 123 cell AABBs / 1953 cell physics tris / scenery 40197 LBs) + **backlog drained 119→0** + **0 console errors**. (The `scene-ready did not fire within 90000ms` boot-state line is a premature cold-load watchdog on the weak iGPU — scene reached "ready" right after; pre-existing, outside the dead-code path.) index.html 9097→9065. Commit: PENDING.
- [~] 7. move 57 DELETE_2D regions + PIXI-render tails of ensure* (KEEP wasm halves + 3 shared Sets) — IN PROGRESS, chunked:
  - [x] **7a-1. renderNeighbourhood (341 lines) → legacy/render_2d.js** via a line-range extraction script (reliable for big blocks). Verified removable: handlePositionUpdate/handleEntitySpawn are SEPARATE top-level fns (not inside it); shared window state (entityMap/getLocalPlayerGuid/setLocalPlayerGuid) is re-exposed at module scope 4605-4607; the other window.* taps it dropped were redundant (live dispatch uses fns directly, scene3d guards typeof). Verify: Tier-1 baseline-identical; only live ref left = the dead 2D else-block call (7126, → 7b); browser smoke GREEN (in-world 35.2s, entityMap Map + getLocalPlayerGuid fn INTACT, 0 errors). Commit: `2e8dab0d`.
  - [x] **7a-2. the 2D bakers + interspersed shaders/consts** (buildTerrainAtlas → addLandblockToScene, the contiguous block 2802-4196 = **1395 lines** incl. TERRAIN_*_GLSL/ATLAS_PNG-JS_URL/SUN_*/TILE_TARGET_PX/LIVE_MODEL_*_GLSL/FLAT_FALLBACK) → legacy/render_2d.js via the line-range script. Verified: 0 window.* exposures in-block, 0 scene3d callers, in-block consts have no external refs; remaining baker refs are dead-2D only (fetchEntityModelOnDemand=7b, ensure* tails=7c). Tier-1 baseline-identical; browser smoke GREEN (in-world 37s, entityMap Map + getLocalPlayerGuid fn intact, 0 errors). Commit: `c4254aca`.
  - [x] **7a-3. straggler consts** TERRAIN_TYPES (2601-2634) + ATLAS_* (2797-2800) → legacy/render_2d.js (orphaned after the bakers moved). KEPT METERS_PER_LANDBLOCK (shared, used at 4746) + NEIGHBOURHOOD/HOLTBURG_* (3D uses them) + the stance helpers. Verify: zero live refs, Tier-1 baseline-identical, smoke GREEN (in-world 36.6s, 0 errors). **7a COMPLETE** (render pipeline fully → legacy/render_2d.js; index.html 12483→10604). Commit: `ac8f7465`.
  - [x] **7b. 2D entity render/dispatch/tickers — DONE (caller-first)** [1222a1f1 + accel batch]. Neutralized call sites first (handlePositionUpdate 2D tail → liveScene early-return; pumpNetFrame ticker block; dispatch2dSpawn → 3D no-op; deferred-spawn replay), THEN extracted the orphaned leaf cluster (cloneEntitySpawn + glyph stack + the ensureEntitySprite fetch/bake/cache family + 2D nameplate + handleEntitySpawn + the 3 standalone tickers + ensurePortalSwirl/drawPortalSwirl) → legacy/entity_2d.js (1372 lines total). **§9 RULING (user said accelerate; plan calls it minor/non-load-bearing): 2D per-portal swirl RETIRED, 3D portal VFX = global portal_space.js donut** (reversible — a per-entity 3D swirl is a separable feature-add if wanted). Verify: dangling-ref grep clean, Tier-1 baseline-identical, browser smoke GREEN (in-world 34.4s, entityMap Map + getLocalPlayerGuid fn intact, 0 current-nav errors). index.html 10603→9222. **REMAINS as quarantined 2D backend** (no-op stubs in 3D, registered in __dispatch2d, RULINGS quarantine): handleEntityRemove/handleEntityMetaRefresh/ensurePortalChip/handleEntityVelocity/handleEntityMotion — removing them needs a __dispatch2d backend-table rework (lower value; left for a follow-up). Commit: `3f4e0775`.
  -    (superseded plan text:) MUST BE CALLER-FIRST (lesson from iter 11, reverted): unlike the 7a render leaves (called only by removed/dead-2D code), the 2D entity fns are called from 3D-REACHABLE shared code — so removing the leaves first throws `ReferenceError` in 3D. Required order: (1) neutralize handlePositionUpdate's 2D tail (replace from the "2D-only branch below" comment with `if (!window.liveScene) return;`, dropping the ensureEntitySprite/sprite/lerp calls — KEEP the shared streaming body above it); (2) remove the pumpNetFrame 2D-ticker call block (tickEntityInterpolation/updateNameplatePositions/tickEntityAnimations/tickCellVisibility — they're 3D no-ops, ~8758); (3) neutralize dispatch2dSpawn (its `if(liveScene)`→handleEntitySpawn / `else`→cloneEntitySpawn branches; the elseif already 3D-skips); THEN (4) extract the now-orphaned leaf fns (cloneEntitySpawn, glyph stack, ensureEntitySprite family, tickers) → legacy/entity_2d.js. §9 GATE at ensurePortalSwirl unchanged. 7c. ensure* PIXI tails.
  NOTE: large extractions use the node line-range script pattern. 7a leaves were safe to pull first; 7b leaves are NOT — neutralize call sites first.
- [ ] 8. remove pixi.js import (1258) + importmap (931); delete backlog clone machinery + useSharedDrain early-return + deferredSpawns
- [~] 9. move/delete 2D captures; port 4 coverage-critical captures to 3D; fix run-all.mjs skip-as-green; +item-5's 3D oracle tap. IN PROGRESS, split 9a-9d. **Env constraint (user ruling 2026-06-18): Playwright is ABSENT here → 9b (capture ports) + 9d (oracle tap) cannot be RUN-validated; the loads/spawns smoke does NOT cover them. User chose: write them UNVALIDATED with explicit caveats, for a later Playwright run.** 9a (delete provably-dead 2D captures) + 9c (harness fix) ARE verifiable here.
  - [x] **9d. item-5's 3D oracle tap — written, UNVALIDATED (no Playwright).** Added a `setLastClientPrediction` write at the tail of `scene3d/camera.js::_advancePrediction` (after the WASD integration step, gated on `advanced`). Captures the per-second velocity in each branch (`predVx/predVy = sin/cos(effHeading)*speed` — the 3D integrator convention, NOT the 2D path's screen-space `-cos/sin`); passes `predictedPlayerPos.x/y/z` (WORLD coords — same space the retired 2D tap fed via `localEntry.sprite.x` = wx, legacy/entity_2d.js:87; the Rust doc's "landblock-local" is stale vs the 2D caller, physics_replay is the arbiter), vz=0, on_ground=true, tick=`__predTickCount` (incremented here, matching the dead 2D tap at index.html:8219), t=now. **ADDITIVE + WRITE-ONLY** (only populates the `getLastClientPrediction` shadow; nothing reads it back into the camera → can't snapback). Verify: node --check clean; Tier-1 baseline-identical (5 fails, 0 new); **browser smoke GREEN** — init3D loaded camera.js cleanly, reached "ready", backlog drained 152→0, real entityHook installed, `getLastClientPrediction` getter wired, 0 console errors. **NOT validated:** the tap's actual firing/values (needs WASD movement → `capture_physics_replay`). Commit: PENDING.
  - [ ] 9a. move/delete provably-dead 2D captures (drive renderNeighbourhood/liveScene/PIXI — dead post-8a). Verifiable by static grep + smoke.
  - [ ] 9b. port the 4 coverage-critical captures to 3D (capture_phase4_step3, phase6_step_a_geometry, phase6_step_e_doors, capture_physics_replay). UNVALIDATED (no Playwright).
  - [ ] 9c. fix run-all.mjs skip-as-green clause. Verifiable.
### PHASE 4 — WS-B motion teardown (AFTER unifiedMotion=on shipped + smoke)
- [ ] 10. delete setSwingPose/setCastPose/tweens/FULL_BODY_ONE_SHOT/CROSSFADE_S/RESUME_WINDOW

## Open / tracked (do NOT block the loop)
- **B-1** locomotion movement-integrator overshoot (Walk→Stop→Walk) — movement-layer bug, fix at wasm `MovementSystem`/`get_state_velocity`; never re-add the band-aids.
- **Batched browser smoke** — run once (loads/spawns/0-errors across the flipped defaults) before Phase 3; needs playwright install or the chrome-devtools driver + ACE up.
- **§9 human decisions** — per-entity portal swirl; unifiedMotion flip granularity.

## Log
- iter 1 — branch created; ledger created; item 1 (unifiedDispatch default-on) shipped.
- iter 2 — item 2 (renderer=3d default) shipped; real browser smoke GREEN via chrome-devtools+nosw=1; discovered the SW app-shell cache footgun (see Environment note).
- iter 3 — item 3a (unifiedClone default-on + doc reconcile) shipped; first smoke hit the ghost-session account-in-use race (diagnosed via ACE log, NOT the flag); clean re-connect GREEN. Split item 3 → 3a/3b/3c.
- iter 4 — reached item 3b (unifiedMotion). §9 decision surfaced; user chose CLASS-BY-CLASS (defer loco). Implemented the `"default"` all-but-loco mode; smoke GREEN. Shipped 3b.
- iter 5 — DEFERRED 3c (worldLifecycle): unsafe standalone (needs unifiedTick+maintPrune chain w/ collision risk) + orthogonal to retirement. Shipped item 4 (__doorStates hoist). Phase 1 flag-flips complete (1/2/3a/3b); Phase 2 started.
- iter 6 — RE-SCOPED item 5: inspection corrected §3.2 (setLastClientPrediction is 2D-only/dead-in-3D, not KEEP_SHARED). No index.html change; 3D oracle tap folded into item 9 (validated by physics_replay, not the smoke). Docs-only.
- iter 7 — item 6 (door split) shipped: 2D door branch + findClosestBuildingPart + __doorBuildingParts → legacy/door_2d.js; first legacy/ extraction. Corrected §6.1 (getBuildingPartForDoor 2D-only). Smoke GREEN.
- iter 8 — started item 7 (bulk strip). Heavy entanglement analysis resolved: renderNeighbourhood is removable (handlers are separate top-level fns; shared state has module-scope dups). 7a-1: extracted renderNeighbourhood → legacy/render_2d.js via line-range script. Smoke GREEN, shared state intact. Established the script-based extraction pattern.
- iter 9 — 7a-2: extracted the 1395-line baker block (2802-4196, bakers + interspersed shaders/consts) → legacy/render_2d.js. Smoke GREEN. index.html 12483→10640 lines so far. 7a-3 reduced to TERRAIN_TYPES+ATLAS_* stragglers.
- iter 10 — 7a-3: TERRAIN_TYPES + ATLAS_* stragglers → legacy/. 7a COMPLETE (entire 2D render pipeline in legacy/render_2d.js; index.html 12483→10604). Smoke GREEN. Next: 7b (entity/dispatch/tickers) — will hit the §9 portal-swirl gate.
- iter 13 (ACCELERATE) — 7b DONE in one batch: ticker block + dispatch2dSpawn neutralized, 1230-line leaf cluster extracted → legacy/entity_2d.js, deferred-replay removed, §9 portal-swirl decided global-only. Smoke GREEN. index.html 10487→9222. Quarantined 2D backend handlers (handleEntityRemove/MetaRefresh/Velocity/Motion/ensurePortalChip) left as no-op stubs (need __dispatch2d table rework).
- iter 12 — 7b-i: neutralized handlePositionUpdate's 2D tail (121 lines → legacy/entity_2d.js, replaced with `if(!window.liveScene)return`; kept the shared streaming body). Caller-first step 1. Smoke GREEN (in-world 19.6s, 0 current-nav errors — the updateNameplatePositions error was a STALE preserved message from iter 11; see preserved-message footgun). index.html 10603→10487.
- iter 11 — attempted 7b leaf-first; REVERTED. Two findings: (a) quaternionToYaw/landblockToWorldXY are SHARED and interspersed in the entity block (excluded on retry); (b) the 2D entity fns are called from 3D-reachable shared code → leaf-first removal throws ReferenceError (updateNameplatePositions, then cascade). 7b MUST be CALLER-FIRST (see the 7b plan above). No code committed; index.html clean at 10603.
- iter 14 — item 8b shipped (dead-2D cleanup). CALLER-FIRST grep first corrected the scope: 2 of the 4 listed candidates are LOAD-BEARING, not dead → KEPT (backlog machinery still drained by scene3d/loop.js:2818; `useSharedDrain` early-return guards against double-draining since the net-frame pump is the active 3D drain). Removed only the genuinely-dead: 2 unread decls (`__deferredSpawnsOverflowWarned`, `deferredSpawns`) + the dangling-`renderNeighbourhood` 2D else-block in renderHoltburg (37 lines). node --check clean; Tier-1 baseline-identical; smoke GREEN (spawned, init3D ready, backlog buffered 119→drained 0, 0 errors). index.html 9097→9065. The 90s scene-ready watchdog warning is a pre-existing cold-iGPU timing artifact (scene reached "ready"), outside the dead-code path.
