# VFX (Visual-Behavior Suite) — build progress log

Implementing the §17 build order from `docs/visual-suite-spec/VISUAL-SUITE-BUILD-SPEC.md`
(source copy: `~/from-vm/visual-suite-brainstorm-2026-06-23/`). Design doc:
`docs/visual-behavior-suite-design-2026-06-23.md`. Branch: `feat/tree-wind-sway-2026-06-23`.
Driven by a self-paced `/loop`. Commits are LOCAL only (no push until asked).

Legend: `[JS]` locally verifiable · `[C#]` WB.Terminal (BUILD-UNVERIFIED locally) · `[WASM]` Rust rebuild (buildbox) · `[bake]` offline.

## Phase 0 — minimal vertical slice
Spec-commit 1 is split into 1a/1b/1c for small, safe, individually-verifiable steps.

- [x] **1a — VFX component registry + contract + `deformation.windBend`** `[JS]` ✅ verified
  - New: `scene3d/vfx/registry.js` (the VisualComponent contract + `registerComponent`/`validateComponent`/`getComponent`, `FAMILY_ORDER`, legacy-safety read/write vocab enforced at register time), `scene3d/vfx/components/windBend.js` (MECH-A; wraps `buildBboxRig`+`buildTreeWindClip`).
  - Test `test_vfx_windbend.mjs`: buildClip **byte-identical** to the inline tree-wind math; registry **rejects** lightCountDelta!=0, cacheKeyScope=instance, and non-render writes (the firewall corollaries).
  - Touches NO existing runtime → zero risk to the working tree-wind. `?treeWind` unchanged.
  - **Adjudication:** the design doc's `procMotion.windBend` family isn't in the build-spec §2.2 enum (deformation|weathering|emissive|texture|particle). Standardized to **family `deformation`, mech `A`, id `deformation.windBend`**. The C# classifier (1b/commit-2) must emit this same id.
- [x] **1b — dormant material substrate** `[JS]` ✅ verified — in `materials.js`: `VFX_GLOBALS` (5 shared uniforms, exported), the single `_patchSetCacheKey` `|v + __vfxSetKey` line (constant `"|v"` suffix on every key when unset → no behavior change), `vfxVariants` Map (ctor) + `getCachedVariant` (mirrors `getCachedFloorBias`) + dispose walk. Inert until a frag/MECH-B component uses it. Test `test_vfx_material_substrate.mjs` 6/6; `test_materials_paletted_lru` no regression.
  - **Follow-up (deferred to first frag commit):** per-DID invalidation of `vfxVariants` (the LRU `floorBiasMaterials.delete(did)` twins at materials.js ~2602/~3238 don't yet clear `vfxVariants` entries whose key starts with `${did}|`). Harmless while dormant.
- [x] **1c — runtime rewire** `[JS]` ✅ verified — `animated_scenery.js getOrCreateWindGroup` now calls `windBend.buildClip({numParts, rig}, windParams)` instead of `buildTreeWindClip` directly (removed the now-unused import; added the `windBend` import). `windBend.buildClip` accepts a precomputed rig OR derives one from partBoxes. Test asserts `buildClip({numParts,rig}) == partBoxes-path == old inline call` byte-for-byte → the live tree-wind is unchanged. `test_vfx_windbend` 11/11; `test_animated_scenery` + all wind tests no regression.
  - **✅ Spec-commit 1 (1a+1b+1c) COMPLETE: the shipped tree-wind now flows through the VFX component system, byte-identical.**
- [x] **Commit 2 — C# classifier + descriptor schema** `[C#]` ✅ BUILD-VERIFIED + RUN-VERIFIED locally (dotnet 10.0.203, net8.0 target).
  - New: `WorldBuilder.Shared/Lib/VisualDescriptor.cs` (records `VisualDescriptor`/`VisualComponentRef`/`VisualSignal`/`DatSelfAnim`/`VisualArchetypeRule`/`VisualArchetypeSelect`, the `VisualArchetypeIds` const-string class, `VisualDescriptorIndex` JSONL save/load, + `"0x%08X"` hex-DID `JsonConverter`s). Landed in **WorldBuilder.Shared** (the spec §3.4 location; Terminal references it).
  - New: `WorldBuilder.Terminal/VfxData/visual_archetype_rules.jsonl` (4 lines: `trunk-canopy` + `rigid-glint` + `tip-flex` + `rigid` fallback). Shipped as Content (CopyToOutputDirectory). The `trunk-canopy` rule carries the explicit 6-DID Phase-0 seed.
  - New: `WorldBuilder.Terminal/CommandEngine.Vfx.cs` (`vfx classify <DID>` §3.2 cascade + `vfx emit-allowlist <archetype>`; rule loader resolves the jsonl from project dir → binary dir → CWD). Registered in BOTH dispatchers: REPL `["vfx"]=HandleVfx` (TerminalRepl.cs) + JSON `["vfx-classify"]`/`["vfx-emit-allowlist"]` (JsonCommandProcessor.cs).
  - **`dotnet build` clean (0 errors)**; `vfx emit-allowlist trunk-canopy` (REPL + JSON) reproduces the 6 `TREE_WIND_DIDS` EXACTLY (order-insensitive set match). `vfx classify 0x02001063` → trunk-canopy / conf 1.0 / source allowlist / mech A / component `deformation.windBend` (channel transform).
  - **Adjudication (tip-flex component id):** design doc §2.3 wrote `procMotion.tipFlex`, but build-spec §2.2's family enum is {deformation|weathering|emissive|texture|particle} (no `procMotion`). Followed the SAME adjudication already applied to windBend (commit 1a) → **`deformation.tipFlex`**. rigid-glint = `emissive.glint`+`weathering.tarnish`. All component ids match the family-enum convention.
  - **DATA GAP (build-spec §18 #1):** WeaponType/MaterialType/ValidLocations are NOT on `WeenieIndexEntry`, so the rigid-glint/tip-flex WeaponType selectors can't FIRE from data this commit. The rule registry transcribes them faithfully (taxonomy stable; seed-bearing archetypes' emit-allowlist works); `classify` resolves only the wind allowlist + geometry hints + rigid fallback; selector-only archetypes report `selectorOnly:true` honestly. Maturing weapon-prop selectors = Phase-1.
  - Still TODO for commit 3: hand-author a small `visual_descriptors.jsonl` fixture so the JS client loader can be tested without re-running the C# classifier.
- [x] **Commit 3 — client catalog fetch + descriptor-by-mech router** `[JS]` ✅ verified — new `scene3d/vfx_catalog.js`: `visualEnabled()` (`?visual` default-OFF), `parseDescriptorsJsonl` (tolerant of the C# emit shape — hex `did`, components as id-strings OR `{id}` objects), `ensureVfxCatalog()` (cached fail-soft fetch of `../../dist/vfx/visual_descriptors.jsonl`), `vfxDescriptorFor`/`hasWindBend`/`descriptorMechs` + `COMPONENT_MECH` router map. Generalized the `statics.js` wind divert (BOTH sites via replace_all): a placement peels to the wind player if `(treeWind && isTreeDid)` OR `(visual && hasWindBend(descriptor))`; `if (visual) await ensureVfxCatalog()` first. Test `test_vfx_catalog.mjs` 14/14; off=frozen + all wind/scenery tests no regression.
  - **Byte-identical guarantees:** both flags off → outer `if` false → `statics` untouched; `?treeWind` on / `?visual` off → identical to before (catalog never fetched); absent/unreadable catalog → empty Map → VFX path selects nothing.
  - **Runtime-wiring follow-ups (NOT blocking Phase-0 exit):** (a) the live `?visual` path is inert until `dist/vfx/visual_descriptors.jsonl` exists — needs a C# `vfx export` verb (not yet built) or a hand-staged dist file; (b) confirm the dist URL once the served dir is settled (build-spec §18 OQ-1). The descriptor *config* (per-archetype dir/strength) is not yet threaded into `attachWindTrees` (still uses `?treeWindStrength/Dir`) — a Phase-1 refinement.
- [x] **Commit 4 — legacy-safety lint** `[JS]` ✅ verified — `scene3d/vfx/lint_caps.js`: frozen `ALLOWED_READS`/`ALLOWED_WRITES`/`FORBIDDEN_*` Sets + `FORBIDDEN_SOURCE` (code-targeted denylist, scanned over COMMENT-STRIPPED source so descriptive comments don't false-positive, `// vfx-lint-allow:` exempt) + `lintManifest`/`lintSource`. Test `test_vfx_legacy_safety.mjs` 17/17 — Layer A (every registered manifest conforms), Layer B (no forbidden patterns in component source), Layer C (desync-proof: `setPose copy()` stomps render writes, omega survives only via post-copy re-apply), + 8 negatives (forbidden read/write, cacheKeyScope=instance, lightCountDelta!=0, Math.random, wasm enqueue, the comment-aware + allow-line cases). Registered the 7 VFX/tree-wind tests in `harness/run-js-headless.mjs` TIER1 — all PASS via the harness.
- [x] **Commit 5 — `vfx gauge` Half-A + cost model** `[C#]` ✅ BUILD+RUN-VERIFIED + `[JS]` instrumentation — `WorldBuilder.Terminal/VfxData/cost_model.jsonl` (5 component cost rows: windBend cheap MECH-A 0/0, glint/tarnish cheap frag, tipFlex medium MECH-B, rigid no-op); `CommandEngine.Vfx.cs` `vfx gauge [--ref holtburg]` — Half-A static estimator: enumerates the real Holtburg radius-1 ring scenery (27 unique SetupDIDs / 299 placements, all 6 trunk-canopy present), classifies, sums cost **per unique driver NOT per placement** (the scaling invariant), gates G1 (Δprograms ≤ Kp link-explosion), G2 (ΔCalls=0), G3 (ΔVRAM ≤ budget) → **VERDICT: STRUCTURAL-PASS** (never "PASS" on SwiftShader). Registered in both dispatchers. `scene3d/index.js` `?vfxGauge=on` tick instrumentation (T_cpu pair + T_gpu via timer-query/finish-fence/N-A → `window.__diag.vfxGauge`), ZERO-cost when unarmed (early guard, render path byte-identical). dotnet build clean; `vfx gauge --ref holtburg` STRUCTURAL-PASS (independently re-run); 7 VFX harness tests no regression; index.js node --check OK.
  - **Queued-for-1070 (Timing Meter / Half-B, gates G5-G7):** real T_gpu via EXT_disjoint_timer_query_webgl2 + the 12.5 ms ceiling calibration + the frame-ms regression gates — N/A on SwiftShader.

**✅ PHASE 0 COMPLETE — exit bar GREEN:** classifier round-trips `TREE_WIND_DIDS` byte-identically (commit 2) · `vfx gauge` STRUCTURAL-PASS (commit 5) · legacy-safety lint green (commit 4) · `?treeWind` byte-identical + `?visual` default-OFF (commits 1/3) · all 7 VFX harness tests + C# build green. **Next = Phase 1 (emissive/material bundle) — AWAITING USER GO (don't auto-cross phase boundaries).**

## Phase 1 — emissive/material/frag bundle (kit §11 P1.1→P1.16)
Driven by the self-paced `/loop`, one commit per step. Authentic leaf source recovered
from the buildbox working tree (the kit's "✅ on disk" files were never committed) →
`/mnt/wbterminal1/tmp/claude-scratch/phase1-reference-2026-06-23/` (checksum-verified). Each
step copies leaf files verbatim from the reference + applies that step's portion of the
existing-file edits; P1.14 statics activation / `vfx_catalog.js` §7-A / P1.16 barrel are
authored from the kit's §-blocks (not applied on the box).

- [x] **P1.1 — oscillator registry + per-frame tick** `[JS]` ✅ verified — NEW `scene3d/vfx/oscillators.js`
  (THREE-free leaf: `WAVES` sine/triangle/smoothNoise/decay, `sampleWave`, `setMasterClock`,
  `registerOscillator`/`update`/`unregister`/`get`/`list`/`_clear`, `tickOscillators`, `OSCILLATOR_INFRA_MANIFEST`)
  copied verbatim from the reference. **Applied handoff R-D:** `tickOscillators` now wraps the clock
  `t = tSec % 3600` (float32 GLSL precision guard, kit R6) — master clock + every channel read the SAME
  wrapped value (phase-locked; no-op for tSec<3600). `loop.js` got the **oscillator-only** edits (the P1.12
  weather + P1.13 flameFlicker ticks bundled in the reference loop.js were excluded): import + `setMasterClock(VFX_GLOBALS.uTime)`,
  `tickVfxOscillators(scene3d)` fn (mirrors `tickTerrainUTime` clock resolution), and the try/catch tick call
  right after `tickTerrainUTime` (never budget-gated — it IS the clock).
  - **Gate GREEN:** `test_vfx_oscillators` 32/32 (+1 new wrap assertion `tickOscillators(3601.5)→1.5`) · legacy-safety 17/17 · P0 regression windbend 11 / catalog 14 / substrate 6 · `loop.js` node --check OK · harness TIER1 7/7.
  - **Byte-identical when off:** registry empty until an effect registers a channel (P1.6+), so the tick only advances `uTime` (`{value}` no material binds until a frag variant exists) → zero render change with `?visual` off. Gauge unaffected (infra row, not yet added — P1.15).
- [x] **P1.2 — frag-install path + componentSetKey + installVfxComponentPatch** `[JS]` ✅ verified — NEW `scene3d/vfx/frag_install.js`
  (copied verbatim from the audited reference): `fragComponentsForDescriptor`, `componentSetKey` (the FIREWALL key = ordered ids + each `linkVariant()` token ONLY — never config/instance), `fragConfigKey` (heap-dedup only), `configForComponent`, `resolveFragMaterial` / `resolveFragMaterialForDid`, `PATCH_MECHS`, `_resetFragInstall`. THREE-free (imports only `registry.js` + `vfx_catalog.js`); the host injects `globals`+`installComponentPatch` so the material surgery stays in materials.js. EDIT `materials.js`: added one export `installVfxComponentPatch` (kit §2b EDIT 1) right after the `VFX_GLOBALS` block — chains a component's `declareUniforms`+`inject` via the module-private `_chainBeforeCompile`, per-hook try/catch fail-soft.
  - **Gate GREEN:** `test_vfx_frag_install` 27/27 (proves program-count ≈ SETs not DIDs: 15 resolves / 5 surfaceDids × 3 configs → 1 setKey/1 program, 15 clone keys) · legacy-safety 17/17 · P0+P1.1 regression (windbend 11 / catalog 14 / substrate 6 / oscillators 32) · `test_static_batch` 10/10 (R-B untouched) · materials.js + frag_install.js node --check OK · harness TIER1 7/7.
  - **Byte-identical when off:** NO call site — frag_install is not wired into statics until P1.14, and `resolveFragMaterial` returns `null` when `!visualEnabled()` ⇒ caller keeps the base material. Zero render change.
  - **⚠ DEVIATION from handoff §4/R3 (`buildFragVariant` bridge) — DEFERRED to P1.14.** The handoff says P1.2 adds a `buildFragVariant` "§2b EDIT 2", but **the kit has no such edit** (part 02 §2b contains only `installVfxComponentPatch`), and the audited reference `frag_install.js` does NOT contain `buildFragVariant`. The symbol is referenced only in `frag_attach.js` (P1.14) COMMENTS as `buildFragVariant(mc, surfaceDid, plan.entries)` — it couples to P1.14's `fragPlanForDid` `entries` shape (merged config + the `comp.enabled()` per-effect gate) and the THREE-free firewall constraint. Authoring it now = an untested firewall-critical fn with no integration test until P1.14, and it has NO caller before P1.14 (frag_attach isn't imported until then). So it is authored + tested AT P1.14 alongside frag_attach + the statics swap that calls it — still before the activation renders. P1.2 stays byte-identical and fully tested; the decision only binds at P1.14.
- [x] **P1.3 — per-instance `vVfxHash` varying** `[JS]` ✅ verified — NEW `scene3d/vfx/per_instance.js`
  (verbatim from reference): `ensureVfxHashVarying(shader)` (idempotent; injects the varying + Dave-Hoskins `vfxHash01` hash-without-sine + the `USE_BATCHING`/`USE_INSTANCING`/else `#ifdef` ladder reading `batchingMatrix`/`instanceMatrix`/`modelMatrix[3].xy`), `hasVfxHashVarying`, GLSL const arrays (built via `[...].join("\n")` — backtick-safe; the 5 backticks in the file are markdown code-spans in JS comments, not GLSL/template literals), `VFX_HASH_VARYING="vVfxHash"`, `vfxHash01Ref` JS port. THREE-free string-surgery; PROCEDURAL (rides the per-instance matrix three already uploads) so program count stays O(SETs) — BatchedMesh has no custom float attr, so this is the only mechanism covering Mesh+Instanced+Batched uniformly.
  - **Gate GREEN:** `test_vfx_per_instance_hash` 24/24 (injection shape/order, 3-branch ladder, idempotency, firewall: no `customProgramCacheKey`/`aVfxHash`/`InstancedBufferAttribute`, lintSource clean, hash determinism/range) · legacy-safety 17/17 · regression (windbend 11 / catalog 14 / substrate 6 / oscillators 32 / frag_install 27) · per_instance.js node --check OK · harness TIER1 7/7.
  - **Byte-identical when off:** inert until a frag effect's `inject` calls `ensureVfxHashVarying` (P1.6+); no call site, no flag of its own. Harness TIER1 row deferred to P1.16 (per part 03, to avoid clobbering parallel appends).
- [x] **P1.4 — shadow/depth-pass exclusion guard** `[JS]` ✅ verified — NEW `scene3d/vfx/shadow_guard.js`
  (verbatim from reference): `DEPTH_PASS_COPY_KEYS` (mirrors three r184 `getDepthMaterial` copy allowlist), `VFX_COLOR_PASS_TAG`, `isVfxColorVariant`, `assertNoVfxDepthLeak(object)`, `projectDepthMaterial`, `assertDepthMaterialUnpatched`. THREE-free. Core finding (verified at source in part 04): r184's shadow pass is ALREADY isolated — casters render with three's internal `_depthMaterial`/`_distanceMaterial`, which copies only a fixed allowlist (never `onBeforeCompile`/`customProgramCacheKey`/`userData`/`emissive`). The one real risk = an integrator assigning a VFX color variant as `customDepthMaterial`; this guard makes that the one checked invariant. EDIT `materials.js` getCachedVariant clone (now ~:1873): added `__vfxColorPassOnly: true` to the clone's `userData` (the tag the guard reads) — diffed reference-vs-live, applied ONLY the stamp (P1.2's installVfxComponentPatch untouched).
  - **Gate GREEN:** `test_vfx_shadow_pass` 20/20 (classification, depth material unpatched, leak guard flags a VFX variant set as customDepth/DistanceMaterial, + source-assertion that materials.js stamps `__vfxColorPassOnly` and never assigns a custom depth/distance material) · legacy-safety 17/17 · regression (windbend/catalog/substrate/oscillators/frag_install/per_instance all green) · materials.js + shadow_guard.js node --check OK · `test_static_batch` 10/10 · harness TIER1 7/7.
  - **Byte-identical when off:** the stamp only lands on a getCachedVariant CLONE, which is only created when `?visual` triggers a frag attach (P1.14); inert to rendering + `_patchSetCacheKey`. No call site, no GLSL. Harness TIER1 row (`vfxShadowPass`) deferred to P1.16 with the others.
- [x] **P1.5 — per-effect flag readers + `?visualBudget` stub** `[JS]` ✅ verified — NEW `scene3d/vfx_flags.js` (source = kit part 14; lands at P1.5 per the audited reorder because P1.14 gates on it — only the url-flags.md DOCS stay at P1.16). Verbatim from reference: `visualAllEffects()` (`?visual=all`/`?visualAll=on`), 7 memoized per-effect readers (`glintEnabled`…`flameFlickerEnabled`, default = `visualAllEffects()`), `visualBudget()` (governor STUB, default ∞, clamp 0..4096), `VFX_EFFECT_FLAGS` router (7 ids), `vfxActiveEffectIds`, `_resetVfxFlags`, and the one gate **`vfxEffectEnabled(componentId)` = `visualEnabled() && <per-effect flag>`** (master off ⇒ always false = the firewall; unknown id ⇒ `visualAllEffects()` fallback). Imports ONLY `visualEnabled` from `vfx_catalog.js` → no cycle.
  - **Gate GREEN:** `test_vfx_flags` 39/39 (default-OFF all readers false; `?visual=on` alone ⇒ master on but every effect off; `?visual=on&glint=on` surgical (only glint); **firewall** `?glint=on` w/o `?visual` ⇒ `vfxEffectEnabled` false; `?visual=all` lights 7 + unknown-id ON; `=all&glint=off` opt-out wins; `?visualBudget` parse/clamp; memo reset) · legacy-safety 17/17 · regression (windbend/catalog/substrate/oscillators/frag_install/per_instance/shadow_pass all green) · vfx_flags.js node --check OK · harness TIER1 7/7.
  - **Byte-identical when off:** all flags default-OFF; no installer consumes `vfxEffectEnabled` yet (effects land P1.6+). url-flags.md rows (A/B from part 14) + harness row deferred to P1.16.
- [x] **P1.6 — emissive.glint** `[JS]` ✅ verified — NEW `scene3d/vfx/components/glint.js` (kit part 05) + `test_vfx_glint.mjs`, verbatim. First REGISTERED effect component: `id:"emissive.glint"`, family `emissive`, mech `frag`, `reads:["clock","instanceHash","surface"]`, `writes:["materialUniform"]`, `linkVariant()→""`, self-registers via `registerComponent`. GLSL (view+time specular sparkle, lands after `#include <emissivemap_fragment>`) built via `.join("\n")`; the 7 backticks are markdown code-spans in JS comments only (node --check clean).
  - **Gate GREEN:** glint 27/27 · **legacy-safety 17/17 now dir-scans glint.js → it passes the component lint** (THE RULE: only materialUniform writes, deterministic, no per-instance program key) · regression (windbend/catalog/substrate/oscillators/frag_install/per_instance/shadow_pass/flags all green) · glint.js node --check OK · harness TIER1 7/7.
  - **Byte-identical when off:** registered but UNATTACHED — no statics call site until P1.14; gated by `vfxEffectEnabled("emissive.glint")` (default-OFF). Zero render change.
- [x] **P1.7 — emissive.magicGlow** `[JS]` ✅ verified — NEW `scene3d/vfx/components/magicGlow.js` (kit part 06) + `test_vfx_magicglow.mjs`, verbatim. `id:"emissive.magicGlow"`, family `emissive`, mech `frag`, `reads:["surface"]` (constant ambient glow — no per-instance phase, so no `instanceHash`), `writes:["materialUniform"]`, self-registers. GLSL via `.join`. Gate GREEN: magicglow 23/23 · legacy-safety 17/17 (scans magicGlow.js) · regression (incl. glint 27) green · node --check OK · harness TIER1 7/7. Byte-identical: registered but unattached (P1.14), gated by `vfxEffectEnabled` default-OFF.
- [x] **P1.8 — emissive.enchantShimmer** `[JS]` ✅ verified — NEW `scene3d/vfx/components/enchantShimmer.js` (kit part 07) + `test_vfx_enchantshimmer.mjs`, verbatim. `id:"emissive.enchantShimmer"`, family `emissive`, mech `frag`, `reads:["clock","instanceHash"]` (pulsing emissive shimmer, phase per-instance via vVfxHash, driven by uTime), `writes:["materialUniform"]`, self-registers. GLSL via `.join`. Gate GREEN: enchantshimmer 21/21 · legacy-safety 17/17 (scans it) · regression (glint 27 / magicGlow 23 / all P0-P1.7) green · node --check OK · harness TIER1 7/7. Byte-identical: registered but unattached (P1.14), gated by `vfxEffectEnabled` default-OFF. **✅ Emissive cluster (glint/magicGlow/enchantShimmer) complete.**
- [x] **P1.9 — weathering.tarnish** `[JS]` ✅ verified — NEW `scene3d/vfx/components/tarnish.js` (kit part 08) + `test_vfx_tarnish.mjs`, verbatim. `id:"weathering.tarnish"`, family `weathering`, mech `frag`, **`channel:"tarnish"`** (§14 conflict unit = the diffuse/roughness weathering slot), `reads:["setup","instanceHash"]` (deterministic per-instance age from `hash01(setupDid^instanceHash)` + global age — NEVER the server wear value), `writes:["materialUniform"]`, self-registers. GLSL lands after `#include <map_fragment>` (post-palette decode). Gate GREEN: tarnish 31/31 · legacy-safety 17/17 (scans it) · regression (all P0-P1.8 green) · node --check OK · harness TIER1 7/7. Byte-identical: registered but unattached (P1.14), default-OFF.
  - **Note for P1.10/11:** components carry a `channel` property (the §14 conflict unit). Per handoff §5, wetness + frost must SHARE a `channel:"precip"` (mutually exclusive — avoids latent double-darken). At P1.10/P1.11, check the reference component's `channel` and rename to `"precip"` if it isn't already.
- [x] **P1.10 — weathering.wetness** `[JS]` ✅ verified — NEW `scene3d/vfx/components/wetness.js` (kit part 09) + `test_vfx_wetness.mjs`, verbatim, **+ handoff §5 precip rename**: reference shipped `channel:"wetness"`; renamed to **`channel:"precip"`** in both source (line 76) and the test's channel assertion so wetness + frost share the conflict unit (mutually exclusive — a surface is rain-wet OR frosted, never both, avoiding double-darken when uWetness & uFrost both > 0). `id:"weathering.wetness"`, family `weathering`, mech `frag`, `reads:["weather","geometry"]` (uWetness + world-normal up), `writes:["materialUniform"]`, self-registers; GLSL after `#include <map_fragment>`/`<roughnessmap_fragment>`. Gate GREEN: wetness 25/25 (updated channel assertion) · legacy-safety 17/17 · regression (all P0-P1.9 green) · node --check OK · harness TIER1 7/7. Byte-identical: reads uWetness=0 default; registered but unattached (P1.14), default-OFF.

## Queued for buildbox / 1070 (not done locally)
- (none yet — Phase 0 1a is JS-only and verified)

## Notes
- 8GB laptop: never `cargo build/test --workspace` / `wasm-pack` locally → any WASM step is written then queued for the buildbox.
- WB.Terminal C# has no local `.sln`/csharp-lsp → C# steps are written faithfully but marked BUILD-UNVERIFIED; JS steps that depend on a C# artifact get a hand-authored fixture.
