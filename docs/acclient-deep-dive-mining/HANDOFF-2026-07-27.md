# HANDOFF — mining WORK-PLAN execution session, 2026-07-27

Resume point for working through `WORK-PLAN.md`. Everything below is committed
and pushed through `b37a762c` (`origin/master`). Working tree was clean at
handoff; no agents or test rigs left running.

## What is DONE (do not redo)

| Item | Commit | Status |
|---|---|---|
| P0.5 jump-block ordinals (ERA-01/02) | `ab51df7f` | Landed. BOTH predicate copies fixed (`motion_interp.rs` AND `holtburger-world/src/player/types.rs:93` — the live gate calls the latter; the plan only named the former). |
| P5.1 `__diag.collision` + `collisionResidencyDiag()` | `60ed2dea` | Landed, confirmed live in probe output. |
| P0.3 camera degenerate-basis guard | `31dfb5a2` | Landed. Root cause: `clipFinalTo` permits t=0. |
| P0.4 icon cache LRU 400 + negative TTL | `e4c86546` | Landed. `?iconCacheMax`, `?iconNegTtlMs`. |
| P5.2/P5.5 heap instrument + movement gates | `0a8c2ff2` | Landed. perf-walk pose fields fixed (landblocksVisited was ALWAYS empty before). Out-of-repo: `/mnt/wbterminal2/collision-probe-2026-07-26/collide.cjs` also gated. |
| P0.1 dungeon streaming gate (PHY-25) | `85433dd4` | Landed + live A/B'd: ring evals 3776→2 per 4-min run. `?dungeonStreamGate` default ON, `?loadPointShiftOnly` follows it, `?eagerDungeons=on` wins. |
| P0.2 door bisect (COL-03) | `c7570823` | **Premise refuted** — no regression; doors only ever blocked head-on (circle collider). |
| COL-03 replacement: entity physics-BSP sweep arm | `3f380c50` | Landed + live-validated (`b37a762c`): ±0.45 m laterals no longer reach the shop. |
| OQ-3 walk/run speed | `37e0f276` | **Settled**: base RUN speed correct (7.785 vs 7.787 m/s at skill 105) — COL-09 refuted, Tier 3 unblocked. WALK is broken (see task 8 below). |
| RQ-08 surfaceParityV2 check | `37e0f276` | **Not a promotion — do NOT flip the flag.** Real item is ClipMap blend parity (task 9). |

Ledger updates for all of the above are in `VERIFICATION-LOG.md` (§COL-03,
§OQ-3, §RQ-08 + COL-03 live-validation addendum) and inline in `WORK-PLAN.md`
(P0.2 entry, Tier 2 RQ-08 paragraph, Tier 3 sequencing note).

## OPEN TASKS (priority order)

1. **COL-10 walk fork — USER DECISION REQUIRED before Tier 3 movement work.**
   Body walks at the DAT-derived 2.6017 m/s (`common.rs:844`) while
   `stateGroundSpeed` returns retail's `WalkAnimSpeed` 3.1199999
   (`apps/holtburger-web/src/lib.rs:6691`), so the walk clip plays 1.199× travel
   (foot-slide; backstep timescale 0.78 vs correct 0.65). Options: body adopts
   3.12 (matches ACE server model) OR stateGroundSpeed adopts 2.6017 (DAT
   truth). Opposite client/server-skew implications. Evidence: VERIFICATION-LOG
   §OQ-3; derivation validated by RunForward reproducing exactly 4.0.

2. **Doorframe gap (new, from COL-03 live validation).** +0.45 m lateral at the
   grocer door slides NE along the leaf, exits past the leaf's NE end
   (~82.36, 34.30), and continues over OUTDOOR terrain to y 67 without entering
   interior cells — building/doorframe geometry never catches the mover.
   Distinct from the entity-BSP arm. Repro: rig below, `door_sweep.cjs` offset
   +0.45. Also check whether the head-on stop migrates 0.88→~0.69 m once BSP
   geometry is resident before first contact (the validated run's head-on used
   the circle arm; residency landed between runs).

3. **1070 batch (queue, run as ONE off-screen session per the batching rule):**
   (a) sealed-purge/park-storm — under `?nullRender` the purge published
   `_sealedEvictLbKey=0x00070000` but `evicted=0` in BOTH gate arms
   (pre-existing); measure the PHY-25 gate's park-storm effect with a real
   renderer. (b) any foliage/ClipMap visual confirms from task 4.

4. **RND-08/33 ClipMap alpha parity (replaces the RQ-08 flag flip).** Split
   retail's ClipMap `SetAlphaBlendEnable(1)` (src/dst 2/6) + per-format ref
   (paletted 100/255≈0.392, DDS 200/255≈0.784; `acclient.c:454497-454511`)
   onto the LEGACY ladder `materials.js:3523-3527` AND `static_atlas.js`
   (which hard-resets ref to 0.5 and buckets on a boolean). Run a hasPalette
   census of ClipMap surfaces first. Fix stale `url-flags.md:235` ("inert until
   M3a" — wrong, `SurfacePixels.hasPalette` is live).

5. **camera.js flat prediction speed.** `camera.js:1772`
   `RUN_SPEED = consts.FALLBACK_RUN_RATE_SCALAR ?? 4.5` misuses a run-rate
   scalar as m/s (defined `index.html:5882`). Rust retired exactly this 4.5→1.0
   (`lib.rs:36697-36707`); port the retirement to JS. S-sized.

6. **Then continue WORK-PLAN order**: Tier 1 (DAT-01 scenery collision — the
   big one; `holtburger-scenery-bake/src/aabb.rs` bake-time AABBs are the
   starting point), Tier 2 lighting campaign (ground in DAT dumps, NOT
   screenshots — user has burned many sessions on eyeballing), Tier 3 movement
   (now on a sound speed baseline), Tier 4, Tier 6.

## Environment facts discovered this session (save re-derivation)

- **`?renderOnDemand=1` prevents `window.liveScene3d` from EVER being
  assigned** (probed twice). Omit it from headless runs that need wasm getters.
- Movement-trace getters (`playerRunRateInputs` etc.) are NOT in the `init3D`
  bag — P5.1-class instrumentation gap, still open.
- **Door bisect rig** (reusable): `/mnt/wbterminal2/door-bisect-2026-07-27/` —
  isolated serving root on :8766, `run_arm.sh <pkg-dir> <label>`,
  `door_sweep.cjs <label> "-0.45,0.45"`, results in `results/*.json`, README
  has venue facts + recipe. Its CDP driver does NOT capture page console (the
  `[col03] … entity BSP arm live` residency line is invisible to it — judge by
  behavior, not logs).
- **Probe accounts**: `doorbisect1` (created for the rig; `tailnet1` collides
  with concurrent rigs — ACE boots whichever logged in first). Character
  `+Oq3low1` (run skill 105) on `doorbisect1` was created for the OQ-3
  low-skill arm and remains in the shard DB.
- `pkg/` holds the release build WITH the door fix (4,947,300 B, 05:13).
  Backup of the pre-door-fix release wasm was in the session scratchpad
  (ephemeral — gone after this session; rebuild from source if needed).
- Skill≥800 runs +21% vs retail **by deliberate ACE-matching**
  (`RETAIL_RUNRATE_EDGE=false`, `context.rs:95-104`) — do not "fix".
- Old pkg builds from `/mnt/wbterminal2/holtburger-pkg-archive/` all boot fine
  against current JS (no ABI break in the 07-20→07-27 window).

## Method notes that earned their keep (from README standing rules, re-proven)

- ~1/3 of agent-reported file:line leads were wrong or under-specified —
  re-open every citation. Twice this session the plan named the wrong/dead
  code site (P0.5's duplicate; RQ-08's flag).
- Live measurement beats source reading: three plan premises (COL-03
  regression, COL-09 speed, RQ-08 promotion) were refuted by measurement.
- Movement-gate every behavioural probe; never test collision head-on only.
