# HANDOFF — 2026-08-21 (r9 SHIPPED+GATED; r10 lineage built; one sample eye-test pending)

Session arc: finished the r9 gate (crash root-caused twice → reachability rule →
GREEN on the 1070), built AND passed the 4.P4 creature-subdiv eye-test on the
buildbox T4 wine rig, then opened the r10 lineage and landed three lanes into it.
Everything is committed on `integ/all-20260813` (…→ 11098861 → 68178c61 →
0ba823a2 → this commit).

## r9 — DONE, do not touch

- Gate GREEN on the 1070 (char-select survived, world entry, full 420 s
  lifetime) AND independently re-proven end-to-end under wine on the T4.
- The rule that fixed it: a 0x06 ships re-encoded ONLY if 0x05-SurfaceTexture-
  reachable or in the retail EoR highres set. `tools/dat-patch/surftex_reach.py`
  is the mandatory gate; `DatCompact --exclude` cuts by reconstruction.
  Full story: reports/phase4-fill-RESULTS.md (supersedes the 3d153372 version).
- Kit: `/mnt/wbterminal2/dat-patch-r9/kit-r9b/` — zip sha `98be2e60…`,
  highres `c68fb079…` (1,332,324,352 B, 9,081 recs), portal `03569ce4…`.
- The corrected zip is ON the 1070 (sha-verified); the old crashing zip deleted.

## r10 work lineage — where new work lands (shipped r9 untouched)

```
/mnt/wbterminal2/fill-2026-08-20/r9/client_highres.r10work.dat  9,085 entries; = gated world2 + 4.H2 (3 detail textures); walk OK; surftex_reach NOT_SAFE=0
/mnt/wbterminal2/fill-2026-08-20/r9/client_portal.r10work.dat   81,206 entries; 560,692,224 B; = r9 portal + 1,209 subdivided creature parts; walk OK
```

- **4.H1 CLOSED** (read-only): 749/811 recolor-live creature RS already shipped
  at 2× (89.7 % by exposure); the 62 gaps are all ≤32 px — below the deliberate
  fill floor. Zero overlap with the UI-blit exclusion. Nothing to do at policy.
- **4.H2 LANDED** in r10work highres (readback 3/3, reach-gate clean).
- **4.P4 LANDED at scale**: pilot 20 + scale-out 1,189 = **1,209 parts**
  (route=candidate, ≥31 tris, pn level 1 — exact POC parameters), every part
  invariant-verified vs the base record (physics polys byte-identical, drawn
  tris 4×, id/sort/degrade/surfaces unchanged), 13 parts correctly skipped by
  the r9-modified-record guard. Drivers: `tools/dat-patch/creature_scaleout.py`
  (+ `creature_pilot.py`); run artifacts
  `/mnt/wbterminal2/dat-patch-creature-subdiv/{pilot20,scaleout}/`.
  Details: reports/r10-lineage-2026-08-21.md.

## ⚠ THE ONE PENDING GATE — 4.P4 sample eye-test (do this before r10 ships)

The Banderling PASSED the close-range eye-test (reports/phase4-P4-eyetest-
2026-08-21.md). A sample of the SCALE-OUT parts still needs eyes. Everything is
staged:

- **Cast (verified to carry subdivided parts in the r10 portal):**
  `@create 7` Drudge Skulker (7/17 parts), `@create 8` Creeper Mosswart (9/17),
  `@create 49051` Grievver (15/18). PASS bar = same as the Banderling: parts
  track the skeleton through walk+attack, no poke-through on concave regions.
- **ACE is ALREADY serving the r10work pair** (portal=r10, cell=r8,
  highres=world2 in /mnt/wbterminal2/dat-patch-r9/ace-r9-dats/). r10 is
  r9-lineage → ACE-safe (custom 0x0D present; creature physics bytes unchanged).
- **buildbox route** (recipe proven, phase4-P4-eyetest-2026-08-21.md §rig):
  `~/ac_client` already holds the r10 portal (in place as client_portal.dat),
  the kit exe (acclient.kit.exe) and the world2 highres. Boot → Xorg :1 →
  launch → xdotool ENTER at (1221,901) → `@telepoi Holtburg` → the three
  @creates → ffmpeg frames. Box is POWERED OFF; it may have been SPOT-preempted
  mid-attempt at session end — the client's "Failed to establish connection /
  timed out" was most likely the box network dying, not a dat problem (the
  same stack connected fine at 07:27 and 07:36). Also: launch the client only
  AFTER ACE's "World is now open" line — the first attempt raced ACE's boot.
- **1070 route** (owner turning it on after this session): dats there are the
  SHIPPED r9 (fine for a plain re-gate but NOT for the subdiv sample — it
  needs the r10 portal copied to `D:\ac-dat-test\client_portal.dat` first,
  local source `/mnt/wbterminal2/fill-2026-08-20/r9/client_portal.r10work.dat`,
  sha `sha256sum` it after scp). Chat rig is deployed: write the command into
  `C:\Temp\acdt\chat.txt` then `schtasks /run /tn acdtchat` (types it into the
  test client; acdt-watch.ps1 writes pid.txt). Gate task `acdtgater9` runs the
  client off-screen+muted; `acdtclick1` double-clicks char select.
  **1070 cleanup while there:** `del D:\ac-dat-test\client_portal.dat.scratch`
  (927 MB stray — the defective WBT-export portal; NEVER serve it from ACE).

## Box / server state at handoff

- **ACE (laptop)**: RUNNING, serving the r10work pair (see above). To return it
  to shipped-r9: stop via `echo stop-now > ~/ace_stdin.fifo`, copy
  `kit-r9b/acme-r9/client_portal.dat` over ace-r9-dats/client_portal.dat,
  relaunch (`setsid nohup dotnet ACE.Server.dll < ~/ace_stdin.fifo …` from
  $ACERT). The eyetest portal (r9+Banderling only) is parked as
  ace-r9-dats/client_portal.dat.eyetest-hold.
- **buildbox**: OFF. Disk keeps: r10 portal in place, kit exe, world2 highres,
  r8 cell, wine prefix, Xorg config. Boot recipe + traps in
  phase4-P4-eyetest-2026-08-21.md (Xorg relaunch needed every boot; expect a
  SPOT preempt per session; `pkill wine`, never `pkill -f acclient`).
- **1070**: was asleep/off since ~21:15 on 08-20; owner will power it on.
  State: shipped-r9 dats + new kit zip (verified), gate rig intact, plus the
  one stray file to delete (above).

## Loose ends / options, in priority order

1. The sample eye-test above → then r10 is ready for kit assembly
   (assemble_kit.sh --tag r10, portal=r10work portal, highres=r10work highres,
   cell unchanged) whenever enough lanes accumulate.
2. 4.P3 env re-cut — staged portal-lineage session
   (PREP-envgeo-recut-lineage-2026-08-20.md).
3. Dungeon-geometry backlog — 6,236 wall clusters (env_geo --top cap),
   24.7 % of indoor cells never displaced.
4. 4.H4 selective 4096² — blocked on a true view-distance input.
5. Optional icon probe — icons can never ship DXT (CPU blit), but a ~10-icon
   UNCOMPRESSED-at-2× probe would answer the dims-tolerance question; ~205 MB
   for the full 2× set if it passes. Designed in conversation 2026-08-21,
   unrun.
6. `replaceDrawing` WBT change (POC §2) — cuts subdiv byte cost ~5×→4×;
   worth doing before any FURTHER creature tranches, though the shipped 1,209
   are fine as-is (additive model, eye-tested).

## Session traps worth re-reading before resuming

- rg `-rn` redacts (use `-n`); pkill self-match; single-login 30 s gap;
  serve.py/ACE dats must match the client's (DDD); the export tree is an
  encoding intermediary, never a servable portal; ACE crashes on portals
  lacking the shard's custom 0x0D PaletteSets (retail-derived portals
  included); launch clients only after ACE logs "World is now open".
