# HANDOFF — 2026-08-18 (executing PLAN-2026-08-18-hedonic-allocation.md)

Continues HANDOFF-2026-08-17-EOD3.md. This session executed Phase 0 + Phase 1 of
the hedonic-allocation plan and pre-built the Phase 2/3 mount mechanism.
All work is uncommitted working tree + /mnt/wbterminal2 artifacts unless noted.

## HEADLINE — r7.1 IS BUILT (portal), mount mechanism PROVEN, patches ready

## OWNER DECISIONS — signed off this session (docs/dat-patch/DECISIONS-2026-08-18.md)
1. **Mount = client-side force-mount patch** (keeps ACE vanilla). ACE product-bit-4
   is the documented server-operator alternative, not our ship path.
2. **F3 4K-res patch = ENABLE** (corrected one-byte variant, not Pea's verbatim).
3. **11 unnamed retail highres ids + 22 passthroughs = DROP with the rest** at the split.
4. **DDD mitigation = OPTION B** (advertise-cap byte patch) — the force-mount alone
   boots every player on vanilla ACE; B ships as a matched pair with force-mount.

## FINISHED

### r7.1 portal — BUILT + internally gated (tools/dat-patch/r7_take5.sh, exit 0 @20:21Z)
- `/mnt/wbterminal2/dat-patch-r7/export/client_portal.dat` = **1.29 GiB** (compacted
  1.81→1.35 GB, byte-verified `verified=83618 mismatch=0`).
- Deblock rebake (adopted A-arm corpus) + **colour anchor rgb+sat** + **colour ledger
  gate** (PASS: 6/2169 out of band 0.28%, 0 darker than retail) + degrade-chain fold
  (fix_degrade_chains --fix + packaged --check) all green; strict walk OK.
- **STILL NEEDS: the mandatory in-client VeryHigh eye-test before announcing** (below).

### Colour anchor (agent A) — I8 root-caused + fixed
- The anchor was NOT missing; the −8..−30% was raw-Remacri-vs-anchored comparison.
  BUT found a real shipped defect: the anchor's alpha-mask population mismatch put
  **175 of r7's 2192 shipped bakes (8.1%) out of colour band** (worst 16.45× → white
  silhouette). Fixed (legibility.py `_ref_mask`/`_mlum`), added rgb+sat mode +
  color_ledger.py gate + r7_take5.sh driver. Report: reports/color-anchor-2026-08-18.md.
- Files: legibility.py, matlib.py, texture_lane.py, README.md (M); color_ledger.py,
  r7_take5.sh (new).

### Highres bake — DONE on the T4 (1,320/1,320, 0 fail, 938 DXT deblocked, 27 min)
- Outputs pulled + extracted: `/mnt/wbterminal2/highres-bake/out/` (1,320 PNGs).
  Bundle + driver + README in /mnt/wbterminal2/highres-bake/.

### highres_lane sentinel fix (tools/dat-patch/highres_lane.py) — palette route
- Restricted `encode_paletted` to each source record's used indices; proven to stop
  3,075 spurious transparent pixels on a real opaque surface, ≤1.1/255 colour cost,
  lane still verifies OK. Report: reports/highres-lane-sentinel-fix-2026-08-18.md.
- Confirmed our lane output carries the mandatory 0xFFFF0001 iteration record (497)
  + HiFi header (dataSet=1 subSet=0x69466948) byte-identical to retail.

### Mount mechanism (patches in /mnt/wbterminal2/ac-eor-patch/, PATCHES.md) — BUILT
- **highres-force-mount** (enabled=False): NOP the product-bit-4 guard
  (acclient.c:293792; box offset 0xFA409 7405→9090). Absent-file safety byte-proven.
- **highres-advertise-cap** (enabled=False, OPTION B): cap the advertised dat list at
  3 (box offset 0xFA4B1 8b86e8010000→b8030000 90); m_num proven ∈{0,4} so no underflow.
- **F3 res-4k-unlock/-2** flipped enabled=True (corrected byte; shipping exe
  regenerated fb56d147…). Report: /mnt/wbterminal2/ac-eor-patch/REPORT-2026-08-18-*.

### Mount gate — PASSED on the box (reports/mount-gate-2026-08-18.md)
- 4 arms vs live ACE + real eor2013 highres: A `2073 no update` fd0 · B force-mount →
  `497 update required` → **session dropped** fd1 (proves the hazard) · C absent → no-op
  fd0 · **D force-mount+cap → `2073 no update` session survives, fd1** (mounts highres
  yet silent to ACE). Option B PROVEN. Resolves the "cap might suppress the mount" unknown.

### Terrain-2x — DEAD in current form (reports/terrain-d4b-high-2026-08-18.md)
- D4b High-detail arm OOM'd EARLIER than VeryHigh (same ~2.4 GB 32-bit wall). Plan
  fork resolved: terrain scope → D5 detail-textures + 1024-no-Region only. r7.1 ships
  NO terrain fold.

### Buildbox — MIGRATED us-central1-b → **us-east1-c** (3 SPOT preempts in 2 days)
- Same shape (n1-standard-2 + T4 SPOT, STOP-on-preempt, same disk via snapshot).
  Old resources deleted; rollback snapshot `buildbox-migrate-20260818` kept. Memory
  (fleet-runbooks.md + MEMORY.md) updated to the new zone. Box currently POWERED OFF;
  r7 dats + VeryHigh INI restored; real highres staged at ~/highres-verify/ (503e0828…).

### highres lane 1.3 — DONE + VERIFIED (r7.1 highres prototype)
- `/mnt/wbterminal2/dat-patch-r7.1/client_highres.dat` = **337 MB** (2,295 records).
  Run: upscaled_png 1320, failed 0, passthrough 22, carried 952. Verify **VERDICT OK**:
  NO-REGRESSION-VS-r7 1342/1342, palette-route 385/385 still INDEX16/P8, passthrough
  byte-identity 22/22. (DXT via the scaffolding encoder; a true r8 ship should route
  DXT through WBT render-surface-import.)
- So BOTH r7.1 files are now built + internally gated: portal 1.29 GiB + highres 337 MB.

## AUTONOMOUS RUN LAUNCHED AT HANDOFF (survives session close)
- **r7 vs r7.1 A/B eye-test CAPTURE** running unattended on buildbox (us-east1-c).
  Chain: laptop setsid uploads r7.1 portal (size-verified 1,348,675,584) + the
  driver, launches ~/eyetest-ab.sh setsid on the box. Driver tours BOTH dats at
  VeryHigh (box-decompress exe): Holtburg, Yaraq, Cave of Alabree, Muggy Guruk
  Caverns (the documented median deblock case), Braid Mansion Ruin — 2 shots/stop
  + soak, per arm, 150s single-login gap between arms. Then tars to
  **~/eyetest-ab.tgz** (+ .sha256), restores r7 as the box default, and POWERS THE
  BOX OFF. Shots in ~/eyetest-ab/ (persistent disk, survive a SPOT STOP).
  RETRIEVE NEXT SESSION: boot box → `gcloud compute scp buildbox:~/eyetest-ab.tgz*`
  → review the A/B gallery (deblock + colour before/after). This is a CAPTURE for
  owner review, not an auto-verdict. Risk to check in the shots: r7.1 is compressed
  — if a stop shows blank/char-select or the per-arm timeline logs page-faults,
  that arm didn't load/render and needs a re-run. Logs: box ~/eyetest-driver.log,
  ~/eyetest-ab/{r7,r71}-timeline.txt, laptop ~/eyetest-chain.log.

## REMAINING (next session)
0. **Review the A/B eye-test gallery** (above) — the r7.1 announce gate.
1. **MANDATORY in-client VeryHigh eye-test** (if the A/B capture is inconclusive) (PLAN §1.5) before announcing r7.1: stage
   r7.1 portal to the box, box-decompress exe, teleport to the deblock stops (0600378C
   severe-quilting wall, Muggy Guruk median, a creature closeup) + colour check vs
   retail + vs old bakes. 1070 offline → use the wine box (us-east1-c). Batch with:
3. **F3 box gate** (>2048 display, border-drag regression = the defect test) and the
   **force-mount arm-D WORLD tour** (mount gate stopped at char-select). Only after
   these pass: flip highres-force-mount + highres-advertise-cap enabled=True TOGETHER,
   and confirm F3 in-world.
4. **r8 HIFI split execution** (PLAN Phase 3): move baked 0x06 → client_highres.dat,
   delete superseded retail copies from portal, DatCompact both. Then Phase-4 fill.

## GATE HARNESS NOTE (learned this session)
- ACE holds the single-login session ~110 s after the client dies → multi-arm login
  gates on one account need ≥130 s between arms (or gate on the ACE "session dropped"
  line). 30 s collided ("Account In Use").
- take-5 on the 8 GB laptop: base+final DatCompact ~2 h EACH (the first was ×extract
  contention; the final was clean but still ~2 h — the driver is built for the old
  18-vCPU box). Bakes were cached so re-bake was skipped. For future big dat builds,
  a resized high-mem buildbox is the right host.

## UNCOMMITTED TREE (not lost — record before committing)
- tools/dat-patch: M README.md, highres_lane.py, legibility.py, matlib.py,
  texture_lane.py; new color_ledger.py, r7_take5.sh.
- docs/dat-patch: PLAN-2026-08-18-hedonic-allocation.md, DECISIONS-2026-08-18.md,
  HANDOFF-2026-08-18.md (this), reports/{color-anchor,mount-gate,terrain-d4b-high,
  highres-lane-sentinel-fix}-2026-08-18.md.
- External (not in repo): /mnt/wbterminal2/ac-eor-patch/ patch_client.py + PATCHES.md
  + regenerated exes.
