# Queued 1070 work — batch for the next box session (2026-08-21)

The 1070 is the fleet's only real GPU; batch all eyes-on work into ONE session
(1070-eyetests-batched rule). Order below is by priority + shared setup so a
single boot covers them. Rig facts: tasks `acdtgater10` (1200 s gate,
off-screen+muted), `acdtclick1` (char-select click), `acdtschat` (focus+
SendInput chat, idle-guarded 20 s — the WORKING injector; `acdtchat`
PostMessage does NOT work). Aliveness/ops traps in
reports/r10-acefix-and-eyetest-2026-08-21.md.

## 0. IN FLIGHT — ragdoll-deaths pilot eye-test (orchestrator runs this first)
When the build agent finishes: scp its pilot portal to D:\ac-dat-test (sha-
verify), swap in, gate + ~6 admin kills of Drudge (wcid 7) to sample the
Death-emote probability ladder. PASS = distinct falls across kills, fall
completes within retail timing, corpse rests in the ragdoll sprawl, no part
explosion. Plan file: /mnt/wbterminal2/dat-patch-ragdoll/EYETEST-PLAN.md.
Portal there is r10-lineage → ACE-safe; ACE is already serving the pilot pair.

## 1. kit-gate.ps1 on the (to-be-assembled) r10 kit  [needs kit first]
Not run since r8. HANDOFF-2026-08-20-fill §STILL OPEN 1. Two arms never run at
all: MessageBox loud-fail presentation, and a real play.bat → client launch
(r8-kit-assembly "What is NOT gated"). Blocked on r10 kit assembly (which is
itself blocked on: ac-eor-patch now vaulted ✓; acefix r10 portal ready ✓;
just needs assemble_kit.sh --tag r10 run). Sequence: assemble kit → scp kit
zip to 1070 → run kit-gate.ps1 → drive the two interactive arms.

## 2. VmSize / F-A ledger tour on the r10 pair  [do WITH #0 or #1]
PLAN-2026-08-18 §5 "no lane ships without the VmSize ledger"; r9 shipped with
only a single static 420 s gate, no multi-stop tour, no VmSize. Make the r10
eye-test (or the kit gate) a MULTI-STOP tour (spawn → Holtburg → a dungeon →
Yaraq → soak) sampling peak VmSize per stop via the watcher, since r9+r10 add
~365 MB textures + 1,209 denser meshes. Record the ledger in the r10 kit
report. Cheap to fold into any already-running client session.

## 3. 4.P3 env re-cut in-client gate  [after the re-cut RUN; owner said GO 2026-08-21]
Pre-envgeo portal STAGED + verified (/mnt/wbterminal2/dat-patch-envgeo-prep/,
sha 50b0d325…, Recipe B safe). **Owner approved running the re-cut once the
ragdoll pilot eye-test lands.** ⚠ SEQUENCE (memory rule: serial heavy dat
I/O): run the re-cut ONLY after the ragdoll eye-test frees ACE + the 1070 —
env_geo variant-build/variant_release are multi-GB laptop loads and ACE holds
~2 GB serving the eye-test pair; concurrent = OOM risk (earlyoom corrupted a
dat that way before). Re-cut commands are in
/mnt/wbterminal2/dat-patch-envgeo-prep/PREP-ENVGEO-STAGED.md §5 (env_geo
cluster --top toward 7,236 → variant-build → variant_release.sh →
audit_carve_orientation). THEN the mandatory 1070 gate: feet-sink gone at
displaced dungeon floors, no relief seams at cell edges (audit expects
up-facing shell ~0). Dungeon targets: Cave of Alabree, Underground Passage
(acdt-tour9.ps1 already scripts 4 dungeon stops on the box).

## Setup shared by all of the above
- Client dats on the box must match what ACE serves (DDD). For #0 the pilot
  portal; for #1-#3 the assembled-kit or re-cut portal. Always sha-verify
  after scp, swap client_portal.dat, and repoint ACE to the matching pair
  BEFORE launching (ss -ulpn:900 alive + 120 s past "World is now open").
- Kill choreography via acdtschat: write the line to C:\Temp\acdt\chat.txt,
  scp, `schtasks /run /tn acdtschat`; verify each admin command server-side in
  ACE's audit log before proceeding (the box's PostMessage rig is unreliable).
- Idle-guard: acdt-schat.ps1 self-blocks if the guard threshold exceeds the
  send cadence (SendInput resets GetLastInputInfo) — it's set to 20 s; leave
  it. Owner may be at the box — respect the muted+off-screen discipline.

## Not 1070 work (parallel offline, no box) — for reference
- Announce/distribution (no box): r9/r10 announce doesn't exist; 5 owner
  questions in ANNOUNCE-r8-DRAFT.md still open. Needs an owner decision, not
  the 1070.
- replaceDrawing re-verify on the first NEW creature tranche (offline WBT).
