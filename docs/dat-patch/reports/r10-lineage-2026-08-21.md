# r10 lineage opened — 4.H1 closed, 4.H2 landed, 4.P4 pilot landed + scale-out running (2026-08-21)

Continuing straight from the r9 gate-green + eye-test session. The shipped r9
artifacts are untouched; all new work lands in the r10 work lineage:

```
/mnt/wbterminal2/fill-2026-08-20/r9/client_highres.r10work.dat   (= gated world2 + 4.H2)
/mnt/wbterminal2/fill-2026-08-20/r9/client_portal.r10work.dat    (= r9 portal + 4.P4 subdiv)
```

## 4.H1 creature textures — CLOSED (read-only check, nothing left at policy)

The research doc's first step ("are the 811 recolor-live creatures already
covered at 2×?") answers **yes, effectively all of them**:

| census bucket | in shipped highres | missing | exposure-weight covered |
|---|---|---|---|
| recolor_live (811) | **749** | 62 | 630,300 / 702,494 (89.7 %) |
| already_patched (281) | 281 | 0 | 100 % |
| safe_plain / safe_pal (251) | 251 | 0 | 100 % |

The 62 "missing" are all format-0x65 palettized records at **≤32 px** (55×32px,
6×16px, 1×8px) — below the fill's deliberate ≥64 px floor, where a 2× upscale
buys nothing visible. None were casualties of the r9 UI-blit exclusion
(cross-checked: 0 overlap with ui-blit-suspects-493.txt). Route (b) DXT for
recolor-dead statues stays an optional byte optimization, not coverage work.

## 4.H2 detail textures — LANDED in r10work highres

The three baked+verified records from the 08-20 lane run
(0x060037D2 generic 64→256, 0x06006D57 BarrenRock 256→1024,
0x06006D58 Grassland 256→1024, all DXT5, roundtrip-ok) were extracted from the
lane's scratch dat and DatRecordInsert --compress'd into r10work:
readback 3/3, walk_check entries=9,085 OK, surftex_reach NOT_SAFE=0
(they ride 0x05 SurfaceTextures 0x050012AF/0x05001786/0x05001787 — GPU path).

## 4.P4 creature subdiv — pilot LANDED, full scale-out RUNNING

**Pilot (top-20 by spawn exposure):** exact POC parameters (pn_tessellate,
level 1, orientation off, gfxObjOnly + preservePhysics). 19 tessellated at
4.00× (one correctly skipped: 0x010007EE differs from retail in r9 — the
r9-modified-record guard); batched through the POC WBT project; every part
verified against the base record on the POC invariants (id unchanged, drawn
tris increased, physics polys byte-identical, sort/degrade/surfaces
unchanged); landed with the eye-tested Banderling into the r10 portal:
20/20 readback-verified, walk_check 81,206 entries OK, +51 KB.

**The landing shape that matters:** the WBT export tree is only the
record-encoding intermediary — records are extracted from it and inserted into
the r9-lineage portal with DatRecordInsert. The export tree itself is never
served or shipped (it is retail-complete but not r9-complete; vanilla ACE
crashes on it — see phase4-P4-eyetest-2026-08-21.md).

**Scale-out:** `tools/dat-patch/creature_scaleout.py` (runs from
/mnt/wbterminal2/dat-patch-creature-subdiv/scaleout/) — all remaining
route=candidate parts with ≥31 tris (the POC excludes 1–30-tri eye/shadow
parts): **1,204 parts in 7 sequential batches of 200**, each batch =
tessellate → WBT batch import+export → invariant-verify vs base →
insert --compress → walk_check, hard-stop on any failure, DONE-stamped for
resume. Byte cost is trivial (~15 KB/part ≈ +18 MB portal). Launched detached
2026-08-21; log at scaleout/batchrun.log.

**After it finishes:** sample eye-test on the buildbox wine rig (spawn a
handful of high-exposure creatures from the r10 portal — the rig recipe in
phase4-P4-eyetest-2026-08-21.md), then r10 is ready for kit assembly whenever
enough lanes accumulate.

## Remaining roadmap after this session

- 4.P3 env re-cut — staged portal-lineage session (recipe exists).
- 4.H4 selective 4096² — needs the view-distance input.
- 4.H3 terrain-2x — dead (unchanged).
- Dungeon geometry backlog — 6,236 wall clusters (env_geo --top cap), 24.7 %
  of indoor cells never displaced.
- Optional: uncompressed-2× icon probe (see the icons discussion — dims
  tolerance of the CPU blit is the open question; probe gate designed, unrun).
