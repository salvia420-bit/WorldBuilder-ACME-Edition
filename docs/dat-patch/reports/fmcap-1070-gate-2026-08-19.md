# FMCAP gate — force-mount + advertise-cap on the EOR SAFEPAL exe, 1070 native — 2026-08-19

Executes HANDOFF-2026-08-19-EOD queue item 1 (the one still-open gate item).
Venue: the **1070, native Windows acclient** (buildbox was SPOT-preempting every
8–40 min; the 1070 has the proven acdt harness). Human check per fleet rules:
idle probe run twice 55 s apart, idle climbed 1:1 with wall clock (>45 min) —
nobody at the machine; client launched muted + off-screen via the acdt watcher.

## Arm composition
- exe: `acclient.eor.SAFEPAL-FMCAP.exe` md5 `34b68deaf43a422ad4f80c5578aada63`
  = pristine EOR + the full ship set (palette-leak ×2 + palette-double-free +
  dat-version-preserve + res-4k ×2) **+ highres-force-mount @ 0x0FAFA9 +
  highres-advertise-cap @ 0x0FB051** (the EOR offsets; both applied together),
  built with `patch_client.py apply --only …`, all 8 verified, PE cksum OK.
- portal: **r7.2** (`dat-patch-r7/export/client_portal.dat`, 1,499,357,184 B,
  sha256 `19984c2f…`) — the owner-approved announce portal.
- highres: **our WBT-DXT r8 highres** (`dat-patch-r8/highres-wbtdxt/
  client_highres.dat`, 337,193,984 B, sha256 `a5fa8b9e…`) — NOT the r7.1
  prototype: this run doubles as the first in-client QA of the
  render-surface-import DXT re-encode.
- cell: box copy already byte-identical to ACE's (`2eaf2a84…`).
- server: the live laptop vanilla ACE, untouched (serves
  `/mnt/wbterminal2/dat-patch-r7/ace-r7-dats/`, portal sha `42dfb2b1…`).
- client dir: `D:\ac-dat-test\` (isolated copy; dats resolve from CWD).
  Backups in place: `client_portal.dat.r6c-pre-r72-bak`,
  `client_highres.dat.stub-bak`; exe staged alongside as
  `acclient.eor.SAFEPAL-FMCAP.exe` and copied over `acclient.exe`.

## Results
### Mount + DDD + world entry — ALL PASS (the box-exe blocker does not exist on EOR)
- **Force-mount fired**: `client_highres.dat` held EXCLUSIVELY LOCKED by the
  client (FileShare.None open refused) from login interrogation through world
  entry and the tour — the Windows equivalent of the wine `hr_fd=1` proof.
- **Advertise-cap works against vanilla ACE**: DDD interrogation response
  logged by ACE names EXACTLY three dats
  (`client_portal.dat: 2073 | client_cell_1.dat: 982 | client_Local_English.dat:
  994 | no update required`) — no 4th highres entry, no portal-id collision, no
  `DATsPatchingDisabled` termination. Session held.
- **World entry**: ACE `[LOGIN] Account tailnet1 entered the world with
  character +AttrProbe88259` at 15:53:38. Memory CLIMB assertion (Windows
  metric = `VirtualMemorySize64`): charsel 476,569,600 → in-world
  1,672,110,080 (+1.2 GB); WorkingSet 32 MB → 1.21 GB. Not a charsel-flat
  false pass.
- The box-FMCAP no-render anomaly (VmSize flat 1.58 GB) is therefore a
  BOX-BUILD artifact, as suspected — the EOR SAFEPAL+FMCAP build enters and
  renders normally.

### World tour (tour9: Holtburg, Cave of Alabree, Yaraq, Underground Passage,
### Shoushi, Braid Mansion, Muggy Guruk finale; @attackable off, /day, OBS video)
- **ALL 7 STOPS CLEAN** (15:54:42–16:01:33, ~7 min in-world): no
  CLIENT-EXITED, no user-activity abort, client alive after the tour
  (vm 1.10 GB), **highres still exclusively locked at tour end**, and
  **Windows Application-Error log: FAULTS=0** for acclient in the whole hour.
- Frame review (68 frames @ 1/6 s from the OBS mkv): towns (Holtburg vista,
  Yaraq outskirts, Shoushi palisade + buildings + NPCs), dungeons (Alabree
  rock, Underground Passage, Muggy Guruk organic walls + mushroom props) all
  FULLY textured — **no white/untextured DXT surfaces anywhere** (the
  mip-cap-16 failure mode this WBT-DXT re-encode could have re-introduced).
  Player avatar renders clothed (green garb + skin-tone hands in frame; no
  pink per-joint particles). Portal-transit frames black by design.
- This is also the **first in-client PASS for the r8 WBT-DXT highres**
  (render-surface-import re-encode) — it was the mounted highres for the
  whole arm.

## Gate consequence — SHIPPED
- `highres-force-mount` + `highres-advertise-cap` flipped **enabled=True
  TOGETHER** in the registry (backup patch_client.py.pre-fmcap-20260819.bak);
  their why-notes updated with the gate evidence.
- `acclient.eor.patched.exe` REBUILT with the full 8-patch set — md5
  `34b68deaf43a422ad4f80c5578aada63`, PE cksum `0x004A1974`, **byte-identical
  to the exe that just passed this gate**. Prior SAFEPAL-only exe backed up as
  `acclient.eor.patched.pre-fmcap-20260819.bak`. PATCHES.md updated.
- Any kit distributed from now on carries the pair; kits already built with
  the 0554acd7 SAFEPAL exe still work (they just never mount highres).

## Artifacts
- /mnt/wbterminal2/fmcap-1070-2026-08-19/ — fmcap-tour.mkv (248 MB OBS
  recording), frames/ (68 PNG), tour9/watch/click1 logs,
  ace-session-extract.log, drive-arm.sh, src-dats.sha256.
- 1070 left state: `D:\ac-dat-test\` now holds the r7.2 portal +
  WBT-DXT highres as the live dats (sha256 stamp files updated), FMCAP exe
  as `acclient.exe` (also as `acclient.eor.SAFEPAL-FMCAP.exe`); backups
  `client_portal.dat.r6c-pre-r72-bak`, `client_highres.dat.stub-bak`,
  `acclient.exe.stock-bak`. No test processes left running; the tour restored
  the previous foreground window and killed OBS.
