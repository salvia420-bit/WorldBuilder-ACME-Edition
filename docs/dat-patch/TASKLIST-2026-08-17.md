# TASKLIST — 2026-08-17 (post-r7-trip investigation → take 4 + buildbox-wine gate)

Working tracker. Status: `[ ]` open · `[~]` in progress · `[x]` done · `[?]` blocked/decision.
Context: r7 take 3 tripped the ceiling guard on scenery chunk aa (portal 2,116,969,472 B
vs guard 2.04e9; file size is a high-water mark — DatCompress frees interior blocks but
never shrinks the file). Investigation findings 2026-08-17 AM (this session):

- Terrain looks low-detail for 3 stacked reasons: (a) all tour videos ≤ tour9 recorded at
  degraded texture detail (INI value inversion, fixed 08-16, see 1070-acclient-driving.md);
  (b) terrain sources are 512² and excluded from 4× (48 protected RS);
  (c) BUT the ceiling is DATA: Region 0x13000000 terrainInfo.landSurfaces.texMerge.baseTexSize
  = 1024 (u32, read by TexMerge::UnPack acclient.c:306032); buffer alloc (:305935) and all
  merged-surface sizes derive from it. texTiling=2 → 512² sources today.
- Feet-sink in dungeons: r5-envvariants shells displace along authored normal, physics
  verbatim, NO orientation veto anywhere (relief3d.py / env_geo.py / gfx_subdiv.rs);
  eligibility is texture-class only (pilot.WALL_CLASSES={Brick,Stone,Plank,Timber}) at
  AMP_WALL=0.20 m — stone/brick FLOORS carve upward 20 cm over unmoved physics.
- 1070 unavailable → buildbox becomes the in-client gate box: T4 kept (cheapest GPU),
  CPU cut to n1-standard-2, acclient under Wine on Linux (bonus: find Wine-caused issues).

## A. Buildbox → Wine gate box
- [x] A1. Resize n1-standard-4 → n1-standard-2 (T4 kept, SPOT/STOP) + start.
      Zone is us-central1-b now (runbook says -a — STALE, update fleet-runbooks).
- [x] A2. OS update (apt update/upgrade), verify nvidia driver + `nvidia-smi` still good
      after kernel updates (rerun install_gpu_driver.py if broken). `touch ~/.keep-awake`.
- [x] A3. Headless X on the T4 (nvidia-xconfig --allow-empty-initial-configuration or
      similar) — Wine needs a display; verify GL renderer string is Tesla T4, not llvmpipe.
- [x] A4. Install wine (+ optionally dxvk later; start with WineD3D→OpenGL).
- [~] A5. Ship the EoR client kit + patched exe (dat-decompress patch, NOMIP box exe or
      shipped patched exe — /mnt/wbterminal2/ac-eor-patch/) + UserPreferences.showcase.ini
      (VeryHigh everything — redeploy each launch, client saves prefs on exit).
- [x] A6. Network path to ACE on the laptop (UDP 9000/9001): install tailscale on buildbox
      OR reverse-tunnel scheme; verify login end-to-end.
- [~] A7. Screenshot/video harness (xvfb-run is NOT enough for GPU; use real X + import/
      ffmpeg x11grab). Document every Wine-vs-Windows rendering difference found.
- [x] A8. Update memory/fleet-runbooks.md: zone -b, n1-standard-2, wine-gate role.

## B. Take 4 (r7 completion)
- [x] B1. Compact tool: rebuild portal file to reclaim freed blocks (raw record copy into
      fresh dat — extend DatCompress machinery; records copied VERBATIM incl. compressed
      flags; preserve iteration). Validate: strict walk 83,618 + AceDatWalk-equivalent +
      DatCompress --verify on the compacted file.
- [~] B2. Take-4 driver: compress-first → 5 lanes (bake-cached, minutes) → COMPACT →
      scenery aa+ab → compact → fixup → strict walk → package. Keep the 2.04e9 guard.
- [ ] B3. Ship gate: in-client at VeryHigh on the wine box (A-track) — 5-stop tour.

## C. Feet-sink fix (orientation veto) — win-win: fixes sink AND saves triangles/bytes
- [x] C1. Audit script: walk shipped r5 variant envs + 447 buildings tranche, measure
      up-facing (authored n_z > 0.7) fraction of displaced area per record. Quantifies the
      bug + the byte savings of a veto. Report → docs/dat-patch/reports/.
- [ ] C2. Add orientation gate to relief3d/env_geo (n_z threshold → amp 0 or FLOOR_M-clamp).
- [ ] C3. Decide: re-cut shipped variants now vs fold into r8. (Owner call after C1 numbers.)
- [ ] C4. r8 area-relief lane runs orientation-gated from day one.

## D. Terrain 2× lane (the win-win-win: +4× terrain texel density for ~60-70 MB)
- [ ] D1. Bake 29 base terrain textures at 1024² A8R8G8B8 from the rewrap corpus
      (terrain_lane machinery, drop the final 512 downsample to 1024).
- [ ] D2. Upscale the 8 alpha maps (4 corner + 1 side + 3 road SurfaceTextures) to match —
      MergeTexture derives loop bounds from base and indexes alpha proportionally (the
      r6 clobber crash proved mismatch = OOB). Check their pixel formats first.
- [ ] D3. Patch Region baseTexSize 1024→2048 (single u32; raw byte patch or WBT write path;
      NOT in ~/ac_base_dats — work on the take-4 copy).
- [ ] D4. Smoke gate FIRST on the wine box: one outdoor LB at VeryHigh, watch for
      MergeTexture AV (RVA 0x13EA26 pattern). Only then fold into take 4/r7.1 as a lane.
- [ ] D5. Optional cheap win: upscale the terrain DETAIL textures (TerrainTex.DetailTextureId,
      DetailTexTiling=4; now visible since INI sets LandscapeDetailTextures=True).

## E. Hygiene / follow-ups
- [ ] E1. Re-shoot comparison footage only with VeryHigh INI deployed (everything ≤ tour9
      undersells the work). Note INI redeploy-before-each-launch in the gate scripts.
- [ ] E2. Update MEMORY.md fleet pointer (zone -b, wine-gate) — user-directed edit flow.
- [ ] E3. Monster textures (811 recolor wall) — still parked, owner decision (roadmap).

## Session log 2026-08-17 (this session)
- A2-A4/A6: buildbox resized n1-standard-2+T4 (us-central1-b — runbook zone was stale),
  OS updated, headless Xorg on the T4 (`Tesla T4/PCIe/SSE2`, direct rendering), wine 8.0 +
  wine32; retail acclient RENDERS THE LOGIN UI under WineD3D and reaches ACE over the
  tailnet (DDD "DAT files are incomplete" boot = expected retail-vs-r6 mismatch). Recipe →
  memory/fleet-runbooks.md. Remaining: ship r7 dats + decompress-patched exe (A5), NVIDIA
  compat32 GL for 32-bit hardware GL (currently mesa/software for the 32-bit client), video
  harness (A7).
- B1: DatCompact tool (tools/dat-patch/DatCompact) — seeds from retail base, copies all
  records with metadata templates, --verify byte-compares everything. Proven: 83,618/83,618
  byte-identical; on post-compress r6 reclaims 314 MB (1.768→1.454 GB).
- **DRW FINDING (root cause of the take-3 tear): the Chorizite.DatReaderWriter 2.1.2 NuGet
  write path corrupts the dat on record-grow workloads** — reproduced deterministically
  (write to a growing record walked a phantom unaligned offset past EOF; mmap flavor = the
  AV/silent scribble; stream flavor = "Expand can't shrink" throw; take-3 portal left with
  branch[0]=0xCDCDCDCD leaf garbage + free chain past EOF, which the RETAIL CLIENT's
  BTree::Search would also walk). The vendored external/DatReaderWriter snapshot does NOT
  reproduce. WBT + WorldBuilder.Shared + DatReaderWriter.Extensions + DatCompress +
  DatCompact now build against the vendored source (ProjectReference). TODO: minimal repro +
  upstream issue (same channel as PR #70).
- B2: r7_driver4.sh RUNNING — compress→compact→bake-cached lanes, walk_check.py
  (tools/dat-patch/walk_check.py, client-semantics b-tree/free-chain tripwire) after every
  step. Take-3 torn portal preserved as export/client_portal.dat.take3-torn.
- C1: carve-orientation audit (tools/dat-patch/audit_carve_orientation.py): **28.0% of all
  r5-variant shell area is up-facing** (48,773 polys, 22.9%), placements 100% yaw-only;
  worst variants are 97-100% floor. Full table: /mnt/wbterminal2/dat-patch-r7/carve-audit-full.json.
  Confirms the feet-sink mechanism and sizes the win of an orientation veto (C2).

## F. Discord-archive research additions (2026-08-17 PM sweep; agent-verified, novel-to-community check done)
- [ ] F1. **Degrade-chain audit (HIGH)**: SurfaceTextures list LOW+HIGH RenderSurface pairs
      (e.g. 0x0500278B → [0x06003E7D, 0x06003E7E]); our lanes baked only the high entry
      (e.g. baked/ has 3E7E but not the sibling). The client serves the LOW sibling at
      distance / lower texture-quality ("Models degrade, textures lod" — paradox; adaptive
      degrade "will still degrade with it off"). Audit all lane SurfaceTextures for
      unreplaced low siblings; decide bake-both vs accept; pin AutomaticDegrades/
      DegradeDistance/AutomaticDegradeBias INI during A/Bs. May explain some in-scene
      old-vs-new patchwork.
- [ ] F2. **DXVK-as-d3d9.dll experiment** on the wine gate box (and later 1070): established
      community practice with acclient (trevis's standing advice; working dxvk.conf on
      record). Unexplored there for QUALITY knobs — try samplerAnisotropy/LOD bias via
      dxvk.conf; on Linux wine this is native Vulkan on the T4 (likely faster than WineD3D GL).
- [x] F3. **Pea's "4K Res Unlocked" byte patch** (offsets 0x0006128D / 0x00063D94, needles in
      Yonneh's C# patch table, utilitybelt 20241012) — candidate for the wine box showcase
      captures beyond 1080p; fold into the patch registry harness with signature relocation.
      DONE 2026-08-17: bytes recovered verbatim from Discord #utilitybelt msg
      1294489188577841237 (Yonneh, 2024-10-12; Pea via Mag-ACClientPatcher). Both quoted
      offsets land EXACTLY on our pristine EOR exe. Registered in
      /mnt/wbterminal2/ac-eor-patch/patch_client.py as `res-4k-unlock` (file 0x06128D =
      UIElement::MouseResizeElement+0x7D) and `res-4k-unlock-2` (file 0x063D94 =
      UIElement::ResizeTo+0x34), **both enabled=False**; context sigs unique (count=1),
      box-build sites 0x0611AD (sig relocates) / 0x063C64 (does not — box call rel32s
      differ by 0x50). Semantics: the four UI layout attrs 0x3C MaxHeight / 0x3D MaxWidth /
      0x3E MinHeight / 0x3F MinWidth read via UIElement::GetAttribute_Int; site 2 flips all
      four `je`→`jmp` so no clamp fires, site 1 kills the two max clamps on drag-resize.
      ⚠ OWNER REVIEW: site 1 carries a **defect** (not in any community note) — Pea deleted
      two `push` pairs but re-based only the first `al` spill, so `mov [esp+0x3c],al` at
      0x4612BA now writes one byte into UIElement::MouseMove's saved-ESI slot and leaves
      has_min_height uninitialised. One-byte fix = replace[48] 0x3c→0x34; deliberately NOT
      applied (registry holds Pea's bytes verbatim). Full write-up + risk notes in
      /mnt/wbterminal2/ac-eor-patch/PATCHES.md. No shipped artifact regenerated.
- [ ] F4. Attribution guard for gates: retail ALREADY has bugged UVs at Olthoi tunnel ends
      (gmriggs 20260212) — don't misattribute those seams to our lanes.
- [ ] F5. Live-hook alternative delivery: paradox's DatPatcher hooks AsyncCache::LoadData for
      runtime texture swap (adjacent to trevis's SerializeFromCachePack decompress patch) —
      an option for shipping upscales without whole-DAT distribution. Research-only for now.
- Notes: community has NO prior art on AC AI-upscaling (our artifact findings are first),
  NEVER discussed the four detail-texture channels (our Tier-0 lever is novel), and no one
  has attempted seam/cross-surface blending. INDEX16 palette wall independently sized by
  Crimson-Zan (4,182 surfaces / 432 palettes) — matches our monster-texture census (E3).

## G. Post-r7-package execution plan (2026-08-17 PM — "best determined steps"; sequenced)
Decision applied: gate r7 as built (strictly better than r6 even with the highres mask);
deblock-prebake + highres lane fold into r7.1. Rationale: r7 risk is already retired by
per-step verification; the two quality fixes share one T4 session and shouldn't block the
compression+TRUE-4x milestone.
- [ ] G1. Box prep BEFORE gate: NVIDIA compat32 libs (F2 15-min plan: finish 550.54.15 .run
      download, install --no-kernel-module + 32-bit libs, restart Xorg :1, verify 32-bit GL
      says Tesla T4) so the gate runs hardware GL, not llvmpipe.
- [ ] G2. Ship r7 to box: portal from acme-dats-r7-true4x.tgz (cell dat upload already
      in flight separately), sha-verify on box, stage ~/ac_client-r7/ kit with
      acclient.eor.patched.exe (decompress patch REQUIRED — compressed portal) + VeryHigh INI.
- [ ] G3. ACE serve switch: repoint Config.js DatFilesDirectory → r7 export copy, restart
      via ace_stdin.fifo (memory/ace-live.md), confirm DDD "no update required" +
      backup chain (Config.js.pre-r7-bak) for instant revert.
- [ ] G4. THE GATE (B3): wine box, VeryHigh 1920x1080, 5-stop tour (Holtburg, terrain pan,
      Muggy Guruk, one r5-variant dungeon, scenery stop) + @telepoi hops; screenshots per
      stop; watch: MergeTexture stability (terrain protected-RS), decompress correctness,
      any Wine-vs-Windows rendering deltas (document per A7). Compare Muggy stop vs the
      r6 gate-vhigh capture for the tile-seam improvement (squarish INTERIOR crunch expected
      to remain — that's r7.1's deblock).
- [ ] G5. T4 session (after gate, same box): (a) copy realesrgan/Remacri tooling from the
      upscale-corpus machinery; (b) deblock 40-worst sources (graphical report list) →
      Remacri A/B → re-score exc0 → visual verdict → if green, corpus-wide deblock rebake
      goes on the r7.1 plan (~31 GPU-min + lane re-imports, bake-cached);
      (c) DXVK d3d9 smoke (F2) — dxvk.conf anisotropy knobs, compare fps + visuals vs WineD3D.
- [ ] G6. Highres lane prep (local, parallel): locate client_highres.dat (EoR install
      media / box ac_client? NOT in ~/ac_base_dats listing), export the 1,245 masked ids'
      highres sources, extend texture_lane for highres-dat writes; target = r7.1 lane.
- [ ] G7. r7.1 definition after G4+G5 verdicts: highres lane + deblock rebake (+ optional
      terrain 2x D-lane + env detail-texture upscale) — one combined tier, one gate.

## G-status EOD 2026-08-17
- G1 [x] compat32 + Vulkan + DXVK all installed; T4 GL + Vulkan verified. (Trap logged:
  the nvidia .run UNINSTALLS the kernel module even with --no-kernel-module — rebuild
  needs linux-headers + full run; recipe in session log.)
- G2 [x] r7 kit fully staged on box (sha-verified portal+cell, patched exe, VeryHigh INI).
- G3 [x] ACE serves r7 (ace-r7-dats hardlinks; Config.js.pre-r7-bak = revert).
- G4 [~] **BLOCKED by a Wine-environment bug, r7 EXONERATED**: world entry crashes in a
  Wine DLL (deterministic write fault 0x0B81F000 @ EIP 0x656DB910, thread 0024) with
  IDENTICAL signature across retail dats + retail exe / r7 dats + patched exe / WineD3D /
  DXVK / audio-disabled. PROVEN on the way there: compressed r7 portal passes DDD, world
  entry succeeds server-side, T4 renders login/entry frames. First crash flavor was
  AnimData::UnPack wild read; second flavor blit-loop read — downstream corruption from
  the same DLL's overrun. Resume protocol: kill ALL wine procs + wait 2 min (session
  linger guard 90s+ — crashed clients hold ACE logins → "two accounts" boots poison
  retries), single run with WINEDEBUG=+loaddll, name the module owning 0x656Dxxxx, then
  override/disable it (winetricks/dlloverride). Fallback: 1070 gate when box returns.
- G5-G7 [ ] queued (T4 deblock A/B ~14 GPU-min; highres lane; r7.1 definition).

## G4 wine-gate investigation — FINAL EOD 2026-08-17 (three agent rounds + local forensics)
RESOLVED: **Wine/T4 stack is gate-viable; the in-world crash is r7-SPECIFIC.**
- Fix that unblocked everything: `HKCU\Software\Wine\Direct3D VideoMemorySize=2048` — the
  client's texture-purge trigger (IsAvailableVideoMemoryLow, acclient.c:457974) never fires
  on modern VRAM; uncapped = msvcrt memset 4MiB zero-fill into a smaller region (proven at
  instruction level). With the cap: retail client + retail dats ran IN-WORLD 10+ min, ZERO
  faults, clean visuals (screenshot taildropped to owner).
- r7 + patched exe: enters world, renders, dies ~30-45s in from heap corruption (varying
  EIP = stomped fn-pointers holding record-payload bytes); magenta particles + broken
  lifestone visuals BEFORE death. Refuted along the way: address-space exhaustion (VmSize
  2.1GB at crash), d3d9 format/pitch mismatch (0/2466), detail-level dependence,
  -rodat, win32 prefix, esync, audio, DXVK-vs-WineD3D, oversized inflates (max 8.5MB =
  retail's own; census clean).
- LEAD SUSPECT: compressed-record delivery via the client's ASYNC disk I/O under Wine —
  short/differing overlapped-read semantics feeding the dat-decompress patch → failed
  inflates (magenta) + garbage-parsed buffers (heap stomp). Wine-specific AND r7-specific
  at once, matching every observation. Note: unpatched exe cannot boot r7 at all (UI
  textures compressed → decompress patch is load-bearing at startup).
- NEXT PROBES (fresh session): (1) decomp: client arg table for a sync-IO/no-async switch;
  (2) WINEDEBUG=+file trace of a compressed-record read burst; (3) known-good community
  runner (wine-ge-8-26) one arm; (4) Windows cross-check on the 1070 when available —
  if 1070+r7+patched-exe is STABLE, wine-async theory confirmed; if it crashes too, the
  EoR decompress patch itself is bad in-world (it was only ever Windows-gated via the
  different NOMIP box exe on r6-compressed).
- Box helper scripts: ~/gate-{a,b,c,d}.sh (d = EXE= selectable). 130s inter-launch guard
  MANDATORY. pkill bracket form "[a]cclient" (self-match kills ssh). Box idle-stop re-armed.
- Standing state: ACE serves r7 (ace-r7-dats), box kit = r7 + patched exe, VRAM cap set.

## H. 2026-08-17 late session — G4 UNBLOCKED: crash isolated to the EOR-build exe
Investigation (fresh session, buildbox only — 1070 not used, per owner):
- [x] H1. Probe 1 (decomp): NO async disk I/O exists in the client — DiskDev::Open_File
      has no FILE_FLAG_OVERLAPPED; DiskDev::SyncRead = SetFilePointer+ReadFile(sync),
      serialized by CThreadsafeDiskController's critical section. Arg table (host/port/
      outport/account/prefs/language/debug/logfile/rodat/usemem) has no IO switch.
      **The wine-async lead theory is refuted at the API level.**
- [x] H2. Live forensics on the frozen arm-1 crash (via /proc/<pid>/mem): fault EIP
      0x00526A7E = SoundTweakedHook::UnPack (4 dwords), called from CAnimHook::UnPackHook
      with size argument 0xFC59BCE8 — the remaining-size counter UNDERFLOWED in a hook-
      list parse (PhysicsScript/Animation family) and the cursor walked the wine heap to
      an unmapped page. SerializeFromCachePack live on stack (ret 0x417B72 = patch site
      +0x4A); deep frames: play_script / DoObjDescChanges / SmartBox::HandleCreateObject.
- [x] H3. r7 on-disk data EXONERATED: every 0x33/0x03/0x32/0x34/0x02 record in r7 is
      byte-identical to retail (datlib byte-compare, 14,464 records); BTEntry flag/
      version words preserved by DatCompact (only diff = 20,654 0x06 records gaining
      compressed bit 0, as designed). Decompress (zlib) is bounded by destLen;
      Cache_Pack_t.m_iVersion @+4 (PDB) → alloc math correct; SmartBuffer refcounts
      audit clean (addref-on-copy, idempotent release).
- [x] H4. **Arm 1**: acclient.eor.decompress-only.exe (palette NOPs removed) + r7 → still
      crashes during world entry. Palette-leak patch EXONERATED.
- [x] H5. **Arm 2**: acclient.box-decompress-TEST.exe (4,837,376 build, decompress-only)
      + SAME r7 dats + SAME wine env → 240s soak ZERO faults, clean render (Holtburg
      lifestone + working particles, /tmp/vm-arm2-t240.png). **The EOR-build exe
      (4,841,472) + dat-decompress NOP is the culprit; box build is gate-clean.**
      SerializeFromCachePack + both version-context callees are instruction-identical
      across builds — the divergence is deeper (still open, H8).
- [x] H6. G4 tour driver built (~/gate-tour.sh): xdotool chat-driven @telepoi/@teledungeon
      6-stop tour (Holtburg, Yaraq, Cave of Alabree, Underground Passage, Braid Mansion
      Ruin, Muggy Guruk Caverns — dest cells resolved from ace_world portal weenies)
      + per-stop shots + 3-min soak. First run accidentally DOUBLED (two instances
      typing into one client) = unplanned stress tour: alive, faults=0 through all stops.
      Clean re-run for archival shots in flight.
- [~] H7. **Arm 3 queued**: EOR decompress-only + compressed-NONcompacted retail portal
      (built on-box: DatCompress on client_portal.dat.retail — 20,675 records, 41.9%,
      realCorruption=0, ~/portal-retail-compressed.dat). Splits compaction-vs-
      any-compressed for the EOR failure; doubles as EOR crash reproducibility check.
- [ ] H8. EOR-exe root cause (why identical patched code misbehaves only in the EOR
      build) — matters for release (community runs the EOR exe). Candidates: version-
      context table content divergence at runtime, the 43 in-place byte diffs vs
      yonneh's 20241005 exe, data-table deltas. Gate does NOT wait on this (box exe).
- Note (owner question): community repo AC-Vulkan-Reshade-NoCrash-Fix triaged by static
  analysis — all 3 binaries byte-identical to official upstreams (NTCore 4gb_patch,
  DXVK 3.0.2 x32 d3d9.dll, ReShade 6.8.0 setup); throwaway account but clean content;
  nothing useful to us (LAA already set on our exe; DXVK already staged). Quarantined at
  /mnt/wbterminal2/quarantine/ — do not run its binaries; fetch from upstream if needed.
- [x] H9. **G4/B3 SHIP GATE: PASSED** (box-decompress exe + r7, wine/T4, VeryHigh 1920x1080).
      Clean 6-stop tour via chat-driven @telepoi/@teledungeon (Holtburg, Yaraq, Cave of
      Alabree, Underground Passage, Braid Mansion Ruin, Muggy Guruk Caverns) + 3-min soak:
      alive=1 faults=0 at every mark; earlier the same client had ALSO survived an
      accidental doubled tour (two drivers interleaving teleports) fault-free. Muggy vs
      r6 armC reference: matches Windows render character (moss colors, lighting, no
      missing textures); squarish interior crunch remains as expected (r7.1 deblock).
      Shots: box /tmp/tour-*.png, laptop scratchpad copies. Wine-vs-Windows deltas (A7):
      none pathological observed in tour frames.
- Agent results (local slots): F1 degrade-chain audit DONE — premise inverted: no LOW/HIGH
      pair exists inside portal.dat (lows live in client_highres.dat; kits ship a stub);
      pipeline already collapses baked chains 2→1; ONE stray record 0x05000ECE needs the
      collapse + a CI invariant; bake-both only ever matters with a real highres dat
      (1,342 records, r7.1-highres-lane adjacent). Report:
      docs/dat-patch/reports/degrade-chain-audit-2026-08-17.md. C2 veto + DRW repro in flight.
- [x] H7-RESULT. Arm 3 STABLE (EOR + compressed-uncompacted retail) AND Arm 4 STABLE
      (EOR + compressed+COMPACTED retail, DatCompact-verified 79,694/79,694 on box).
      ⇒ compression mechanics and compaction layout BOTH exonerated; the EOR failure
      requires the TRUE-4x content (sustained multi-MB inflates). H8 narrowed to
      size-scaled paths; forwarded to the in-flight H8 agent. Box r7 portal restored
      + sha-verified (0d2df11f… matches shipped — no-rodat writes did NOT dirty it).
- [x] H10. DRW REPIN APPLIED (owner-approved): 5 projects vendored→NuGet 2.1.8 (our
      PR #70 upstream release). Builds clean, WBT smoke passes. Box ~/net8.0 kits
      still vendored builds. DRW repro + issue drafts: /mnt/wbterminal2/drw-repro/.
- [x] H8-RE. EOR divergence agent (report: reports/eor-exe-divergence-2026-08-17.md):
      corrected crash chain = MotionData::UnPack → AnimData::UnPack (brief mis-named it
      via box-address drift +0xC00); every function on the crash + large-inflate path is
      INSTRUCTION-IDENTICAL across builds (AnimData::UnPack, Decompress, LoadDataEx,
      SmartBuffer::ReconfigureAllocation, GrowBuffer::GetGoodSize/GrowExact incl. size-
      class constants) — the SmartBuffer grow-threshold hypothesis is REFUTED. Underflow
      origin = Archive::GetSizeLeft (unsigned bufSize−used, no floor) → PackObj::UnPack,
      unclamped. Whole-.text diff = 1,249 hunks (separate compiles/inlining), so residual
      divergence hides in .rdata/.data dispatch tables or the CRT new chain (open).
- [x] H11. **Safer EOR patch derived + verified (mine, this session): dat-version-preserve.**
      Instead of NOPping the m_iVersion!=0 guard, re-apply the correct BTEntry version in
      DiskController::LoadDataEx AFTER Decompress's operator= copies its m_iVersion=0 back.
      EOR file 0x271C78 (VA 0x671C78), unique sig 8d4c2424e80f53d9ff (count=1 verified),
      9-byte dead slot = duplicate idempotent ReleaseMasterBuffer:
      `8d 4c 24 24 e8 0f 53 d9 ff` → `0f b7 45 02 89 46 04 90 90`
      (movzx eax,[ebp+2]; mov [esi+4],eax; nop; nop). Liveness PROVEN: 0x671BC9
      mov[esi+4],ecx = buf_out->m_iVersion (esi=buf_out); 0x671C35 test[ebp+0],1 =
      ent_out&1 (ebp=ent_out → [ebp+2]=version word). Makes the shipped guard-NOP
      UNNECESSARY + removes paradox's version-0-schema caveat. Box equivalent site
      0x270CD8. Registry entry drafted (enabled=False) in the report — NOT yet in
      patch_client.py (owner review).
- [~] H12. **DECISIVE TEST IN FLIGHT**: EOR orig + dat-version-preserve ONLY (no guard NOP,
      no palette) + r7, wine/T4. Boot-to-login proves version-preservation enables
      compressed-UI decompress legitimately; in-world crash-free = H8 SOLVED, EOR-exe
      embargo lifts, r7 shippable to EOR-exe community installs with a single 9-byte patch.
      Result → handoff.
- [x] H12-RESULT. **H8 SOLVED — EOR-exe embargo LIFTED.** EOR build + dat-version-preserve
      patch ONLY (no guard NOP, no palette) + r7, wine/T4 VeryHigh: booted r7's COMPRESSED
      UI to char-select (decompress guard passes legitimately — version-preservation
      confirmed) AND entered world + rendered 4x textures crash-free through the full
      240s soak (vm-vfix2-t240.png = cave interior, sharp upscaled rock, vitals full,
      faults=0). First attempt (vfix) booted clean but hit an ACE "Bad handshake" login
      collision (transient, server-log-confirmed) before world entry; clean re-run
      (vfix2) settled it. ⇒ r7 is shippable to community EOR-exe installs with a single
      9-byte patch that REPLACES the guard-NOP and removes paradox's version-0-schema
      caveat. patch_client.py registry entry ready (bytes verified, enabled=False) —
      owner call to add + flip on. Scratch exe: /mnt/wbterminal2/ac-eor-patch not
      touched; test exe in laptop scratchpad (acclient.eor.versionfix-only.exe, md5
      9b3cd17dbbd988f736358b2a8d701ac6).

## OWNER ACCEPTANCE 2026-08-17 EOD2 (all fixes accepted; committed+pushed)
- [x] DRW repin → NuGet 2.1.8 ACCEPTED (5 projects; builds clean, WBT smoke passes).
- [x] C2 orientation veto ACCEPTED into the tree (relief3d.py/env_geo.py/pilot.py);
      default UP_MODE=veto stands; clamp/off remain per-call escape hatches. C3 re-cut
      timing + building-roof clamp-vs-veto = still forward scope for the next area lane,
      not blocking.
- [x] dat-version-preserve ACCEPTED + APPLIED to patch_client.py (external lane): shipped
      acclient.eor.patched.exe regenerated (palette×2 + version-preserve); dat-decompress
      guard-NOP disabled. EOR-exe now the recommended release artifact for r7.
- [x] F1 degrade-chain ACCEPTED: accept-not-bake-both; fix 0x05000ECE collapse + add the
      CI invariant in the NEXT take (data fix, not this commit).
- [x] G6 highres lane spec ACCEPTED as the r7.1 plan (build against synthesized inputs;
      acquire a real 127.0 MiB highres dat per the verified fingerprint).
- Docs committed: HANDOFF-2026-08-17-EOD2.md + 3 reports + this tasklist. External-lane
  edits (patch_client.py, PATCHES.md) are NOT in the repo — noted in the handoff.

## I. 2026-08-17 night session (post-EOD2) — r7.1 execution sprint
- [x] I1. **client_highres.dat ACQUIRED + VERIFIED GENUINE** (G6 data event, months early):
      /mnt/wbterminal2/highres-acquisition/eor2013/client_highres.dat — 133,169,152 B
      exact, dataset=1 subset=0x69466948, Iteration 497 (2013-09-06 = EoR; highres
      stopped patching in 2013). Source: ACCPP public MEGA ACDataRepository (pulled via
      CS API + pure-python AES; nothing executed; provenance + sha256 503e0828… in
      PROVENANCE.txt). 2,294 RS records (2,283 predicted + 11 never named by any portal
      ST); all 2,283 known ids present; ZERO content-id overlap with portal → the
      portal-vs-highres precedence blocker is MOOT. archive.org ac-updates + Brycter/
      ACCPP EOR zips all = the same 507,898,112-B highres-less bundle (eliminated).
- [x] I2. **Highres lane BUILT + PROVEN on the real dat**: synth_highres.py (reader/
      fixture/calibrator + data/highres-eor2013-headers.json so CI needs no 127MiB file)
      + highres_lane.py (plan/run/verify). Real dat parses 2,294/2,294 clean; lane =
      1,320 upscale + 22 passthrough (spec CORRECTED: target r7 output dims, not blind
      2x — a blind 2x would REGRESS 2 records; +20 became +22); INDEX16 stays palette
      (385), no-regression-vs-r7 invariant 1,342/1,342; walk_check + WBT round-trip OK.
      Decomp precedence answered anyway: highres WINS (CLCache::GetDiskController probes
      slot 3 first, acclient.c:292512; mounted only when server DDD sets product bit 4 —
      vanilla ACE writes 1u → owner call needed to serve it). Remaining: real upscaler
      PNGs for the 1,320 (Remacri via WBT render-surface-import).
- [x] I3. **F1 fix landed as tooling** (fix_degrade_chains.py + --check CI invariant):
      chain-keyed collapse via WBT surface-texture-collapse; proofs — --check on shipped
      r7 finds EXACTLY 0x05000ECE (exit 1); fix on sha-verified scratch copy collapses
      to [0x060045B4], recheck 0, walk_check OK, b-tree delta = size 21→17 only, record
      shrunk IN PLACE (no compact needed; grow-bug class inapplicable). README wired.
      Bonus research (verified in decomp): portal ceiling is EXACTLY 2^31−1 (BTEntry
      offsets signed int + DiskDev SetFilePointer no-high-DWORD = two independent walls;
      bit 31 = free-block flag, allocated-chain sign check ABORTS reads). Escapes ranked:
      highres dat (~2x budget, needs ACE DDD product-bit-4 = keep-ACE-vanilla decision) >
      compression+patch (~450MB, shipped) > raise driver guard to ~2.14e9 (+100MB, but
      walk_check would need an explicit offset<0x80000000 assert on allocated chains).
- [x] I4. **G5a/b deblock-prebake: BUILT, RUN, VERDICT** (report:
      reports/deblock-ab-2026-08-17.md): deblock.py — exc0 scorer reproduces
      block-scores.csv EXACTLY; filter improved over the research prototype with a
      SEED GATE (only smooth boundary steps exceeding their off-grid neighbourhood):
      3E7E keeps 98% vs prototype's 87%. 702 material set deblocked (+34.9%→−0.1%,
      kept 103%/p10 99%) AND full 1,630 corpus (+22.9%→−1.0%); Remacri A-arm baked on
      the T4 for ALL 1,630 (out-remacri-full/, sha-verified) → r7.1 rebake is now
      GPU-independent. exc0 A/B: material 702→196, severe 272→57; 40-tex RAW control
      isolates deblock cost at ~15% gradient energy median, and the visual strip
      (ab-visual.png) shows severe-case quilting ELIMINATED. REC: adopt for r7.1 gated
      on the batched eye-test (formal §5C detail bar FAILs but was mis-calibrated —
      control proves ~14% baseline drift + artifact energy counted as detail).
      matlib.py grew the DATPATCH_DEBLOCK_BASE pre-tex_path hook. G5c DXVK smoke: NOT
      run (deprioritized behind D4; still queued). **DECISION RECORDED (owner-delegated
      this session): ADOPT — r7.1 rebakes from out-remacri-full/ corpus-wide; standard
      r7.1 gate + eye-test still apply; severity-gated mix is the pre-approved
      fallback.** Decision artifact = reports/deblock-ab-2026-08-17.md (+ ab-visual.png,
      ab-scores.jsonl, control-compare.json).
- [x] I5. **F3 4K-res patch**: recovered Pea's bytes from Yonneh's utilitybelt table
      (Discord, verbatim offsets land on our EOR exe), disassembled both sites
      (UIElement Max* clamp skips, NOT render-res), found an UNREPORTED defect in
      Pea's site-1 bytes (esp re-base miss → stack scribble into MouseMove's saved-ESI
      slot, one-byte fix identified), registry entries added enabled=False + PATCHES.md.
      Owner review items listed in the agent report (session transcript).
- [~] I6. **D4 terrain-2x smoke IN FLIGHT** (D1-D3 done by agent: 29 bases @1024
      A8R8G8B8 + 8 masks @1024 LSCAPE_ALPHA bicubic (+nearest fallback arm) +
      patch_region_basetexsize.py with parse-proven field location; 512-default lane
      byte-identical re-bake proven). Arm portal built ON BOX from its r7 copy
      (sha-verified source) with the laptop's DRW-2.1.8 WBT: 37/37 imports, Region
      1024→2048 PASS, walk_check OK, 1,632,323,584 B (guard headroom OK). Ship-blocking
      side-find: the 8 blend STs are 2-entry with entry[0] = highres ids → MUST collapse
      (fix_degrade_chains machinery) before any real highres dat ships alongside
      terrain-2x. First smoke attempt hit the SPOT-preempt Xorg trap (relaunched).
- Box events: SPOT preempt mid-session (outputs already pulled, nothing lost);
  Xorg :1 required relaunch after reboot (runbook trap confirmed).
- [ ] I7. **HIFI SPLIT (trevis, owner-relayed; exact form: DELETE the superseded
      base-retail standard textures from the portal, our remacri lives in highres)**:
      MEASURED — variant B (delete superseded retail copies) frees 617 MiB compressed
      from the portal (r7 1.52→~0.9 GiB after compact) with 1.40 GiB highres headroom
      left; variant C (ALL 20,684 textures → highres, portal carries zero 0x06) frees
      0.76 GiB with 1.24 GiB headroom → portal ceiling stops binding either way. TRADE:
      highres becomes LOAD-BEARING (no portal fallback — un-mounted highres = missing
      textures), so the mount must be guaranteed (ACE product-bit-4 one-liner vs
      client-side force-mount patch that keeps ACE vanilla) + DDD iteration semantics
      verified. Full table + preconditions in HANDOFF-2026-08-17-EOD3.md ("THE HIFI
      SPLIT"). Candidate r8 architecture; r7.1 can prototype with the deblock payload.
- [ ] I8. **Deblock color restore (owner-observed, measured)**: A-arm bakes are darker/
      flatter than old (RGB −8..−30%, contrast down; sat −5..−10%). Cause split: mostly
      RUNNER DRIFT (old bakes had exposure/color anchoring vs retail in the rebake
      path; A-arm is raw Remacri) + a small deblock share. FIX at r7.1 re-encode:
      verify/apply the retail-anchor step; residual → per-texture color-stats transfer
      vs retail (CPU). ORDER (owner): any T4 color-enhancement pass = FINISHING
      TOUCHES, only AFTER the freed space is filled. Details in
      reports/deblock-ab-2026-08-17.md "Color note".
