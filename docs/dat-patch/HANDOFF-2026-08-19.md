# HANDOFF — 2026-08-19 (r7.2 built, gated, toured, OWNER-APPROVED)

Continues HANDOFF-2026-08-18.md. Full session detail (root-causes, audits,
crash forensics) lives in reports/eyetest-ab-review-2026-08-18.md — read it
before touching the crash or the corpus.

## HEADLINE — r7.2 PASSED THE ANNOUNCE GATE (owner eye-review "looks good", 2026-08-19)
- Portal: /mnt/wbterminal2/dat-patch-r7/export/client_portal.dat =
  **1,499,357,184 B (1.397 GiB)**. Every gate green: colour ledger PASS,
  degrade-chain 0, strict walk 83,618, **dims ledger 0/0/0/0 vs r7** (new gate).
- In-client: r7 vs r7.2 3-arm tour, 7 stops incl. NEW creature-closeup, all
  faults=0, soak VmSize ≈2.21 GB all arms (r7.2 ≡ r7). Gallery
  /mnt/wbterminal2/eyetest-r72-2026-08-19/ + taildropped to the redmi.
- Highres prototype unchanged: /mnt/wbterminal2/dat-patch-r7.1/client_highres.dat
  (337 MB, verify OK).

## WHAT r7.2 FIXED (the r7.1 gate failure, root-caused this session)
1. **711 textures shipped 4× smaller in r7.1** — the adopted A-arm deblock
   corpus (1,630 files, census-scoped) missed 711 ids the old rewrap corpus
   (4,041) served; lanes silently fell back to 1× (triage caught only 95).
   FIX: deblocked+Remacri'd the 711 (T4), corpus now 2,341 files
   (out-remacri-full/), take5 full rerun. PERMANENT TRIPWIRE:
   tools/dat-patch/dims_ledger.py wired into r7_take5.sh STAGE 4b
   (any downscale/format-change/missing vs previous release = FATAL;
   DATPATCH_PREV_RELEASE, DATPATCH_DIMS_ALLOW).
2. **The r7.1 eyetest crash is NOT dat content** — second sample hit the r7
   BASELINE (Frame::combine wild-read, 64KB-aligned 3.3-3.6GB addr, world
   entry, EIPs 0x525E70/0x525E7E). Opus-agent decomp analysis + refutations
   (b-tree metadata identical; retail ships 8×8 DXTs) → environment/timing
   flake (VA-pressure or CAnimation pos_frames recycle race), ~2/6 world
   entries this week. Clean tours happen at 2.21 GB VmSize, crash sample was
   at 1.82 GB → not a simple threshold. Tour driver now installs
   `winedbg --auto` (real backtrace on next hit) + 5s VmSize sampler.

## COVERAGE AUDIT (owner Qs answered, full numbers in the report)
- Terrain 0% by design (TexMerge composits into baseTexSize=1024 — source res
  can't reach the screen); path = D5 detail textures (+ the dead terrain-2x).
- Buildings 98.8% / Yaraq 100% portal coverage; LOD variants add 0 textures;
  "plain second stories" = model UV design + geometry LOD, not missed bakes.
- Dungeons 99.4%, all z (id-based enumeration; 734,976 cells walked).
- 440 building + 537 dungeon texture TOP levels are highres-resident (all in
  our 337MB build) → force-mount is the biggest remaining quality lever.
- Total genuine gap: 9 textures (report lists ids) — r8 micro-lane.

## NEXT SESSION (in order)
1. **Batched box session**: F3 gate (>2048 display, border-drag defect test) +
   **force-mount arm-D WORLD tour with OUR custom highres** (mount gate only
   reached char-select, and used the real eor2013 file) + pick up
   ~/eyetest-r72/*-vmsize.log (missed the tar; driver fixed in scratchpad copy).
   Only after BOTH pass: flip highres-force-mount + highres-advertise-cap
   enabled=True TOGETHER; confirm F3 in-world.
2. **r8 HIFI split (PLAN Phase 3, variant B)**: ours→highres, DELETE superseded
   retail portal copies, DatCompact both → portal ~0.9 GiB + highres ~648 MB.
   Preconditions still open: fresh-install loud-fail gate (2.3); route DXT
   through WBT render-surface-import (not the scaffolding encoder); the
   9-texture micro-lane rides along.
3. Phase 4 fill per plan: scenery aa+ab, creature-subdiv spike ("triangles"),
   creature-texture project, D5 terrain detail textures.

## UNCOMMITTED TREE (this session adds)
- tools/dat-patch: dims_ledger.py (new), datlib.py (flags + compressed get()),
  r7_take5.sh (STAGE 4b dims gate).
- docs/dat-patch: reports/eyetest-ab-review-2026-08-18.md, this handoff.
- Scratchpad (session-local, copy if wanted): eyetest-r72.sh (hardened tour
  driver: resume + vmsize sampler + winedbg --auto + fixed tar), coverage
  audit scripts + JSON.
- /mnt/wbterminal2: deblock-ab/{in-deblocked-delta711,out-remacri-delta711}/,
  out-remacri-full/ now 2,341, eyetest-ab-2026-08-18/ (r7.1 gate captures +
  crash forensics), eyetest-r72-2026-08-19/ (the approved gallery).

## BOX STATE — clean, powered off
- ~/ac_client/client_portal.dat = r7 (1,520,297,984 verified), VeryHigh INI
  intact, keep-awake disarmed. Parked: ~/r7.2-portal.dat (1,499,357,184),
  ~/eyetest-r72/ outputs incl. vmsize logs, ~/delta711/, Remacri venv+weights.
