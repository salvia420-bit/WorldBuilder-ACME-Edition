# 1111 — net-fixwave session-3 handoff (2026-07-10)

Coordinator: laptop Fable session, executing the 6 usage-failed parts of
`net-fixwave-20260709` **directly** (user directive: no VM, no subagents).
Durable state: `~/from-vm/net-fixwave-20260709-STATUS.md` (the §7 ledger) +
`~/from-vm/net-fixwave-20260709/parts/*.md` (per-part deliverables).
Validation artifacts: `/mnt/wbterminal2/holtburger-scratch/p2-validate-20260710T053713Z/`.

## What landed (branch `net-review-wave2`, merged to origin/master)

| commit | part | one-line | key validation |
|---|---|---|---|
| 6a418c55 | P2 | provability-gated wasm negative cache (`key_known_absent` trait probe; alias-block refusal; remove/clear + clear-on-reinit; call-level `decodeMisses`/`provenAbsent` ABI through the worker boundary; R-9 tolerant prefetch rounds via `PrefetchError::PartialRound`; `dat_decode_diag()` + `__diag.datDecode()`) | ladder probe 3/3 PASS (negcache-abi = the ABI gate); detector 1114 meshes 0 grey/white; datDecode main+worker+jsMissing all PASS; 133+4 host tests |
| d343edcc | P4 | staticScriptSlice residency guard (F3-mirror + late-completion teardown; interior anchors stamp landblockId), teleport spawn-flush (`?spawnTeleportFlush`), `entDrainBudget` default-ON, slice reader honors 0/false | orphan-emitter-probe PASS both arms (orphans=0, owners=0) |
| 11c08b28 | P5 | time-budgeted sealed-purge drain (250 ms first burst / 6 ms steady; `?sealedEvictBurst=off` = legacy staircase), batched `clear_landblocks_collision(&[u32])` (one COW clone per table per drain), one-pass group bucketing (A11-O5) | 2 new Rust equivalence tests; TN A/B: recovered 1274 ms (default) vs 3956 ms (=off), 0 errors |
| f83e1d60 + d995e4e4 | P6 | per-spawn entity rig warm pre-attach (post-USE_MAP variant — the A09-6 lesson), one-shot login-idle archetype-matrix warm (parked proxies — program-cache refcount), paletted-loop 6 ms slice | probe caught BOTH warms silently no-oping (builders bag lacks renderer; local-player trigger suppressed by eager-WorldState) — fixed via render-host fallback + any-spawn trigger; final: **PASS delta=6, archetypeParked=7, warms live** (SwiftShader = coverage only; 1070 latency A/B queued) |
| 108032f9 | W3 | envcellFusion presence-guard dropped (A08-5 — bare URLs ran fusion-OFF while every probe ran it ON) + `scripts/lint-url-flags.mjs` (229 rows / 211 readers / 14 presence-guards adjudicated consistent / 78 undocumented readers = the owed backfill, `--strict` ratchet) | lint exit 0 |
| a433c4ea | W2 partial | wsbridge read end-to-end (gap closed: 1 WS frame ↔ 1 UDP datagram, zero transformation, kernel backpressure; W2-B1..B4 all LOW/INFO) + `scripts/net-review/lint-wire-codec.mjs` (280 opcodes 100% chorizite-or-ACE-confirmed; 4 candidates verified real in ACE) | codec lint exit 0 |

**Rebuild note:** pkg/ was rebuilt RELEASE twice (last: 4,756,608 B with
P5's drain). Other checkouts must rebuild pkg after pulling (P2+P5 changed
crates/ + src/lib.rs); serve.py's staleness warn catches it.

## Traps hit this session (worth knowing)
- `cargo test -p holtburger-web` (bare) FAILS on host — pre-existing: the
  doctest lib build lacks holtburger_dat (wasm+dev-dep only). Use `--lib`.
  One pre-existing test failure (`triangulate_..._composes_part_and_texture`)
  fails on clean ee34363a too.
- Root disk hit 100% mid-build → deleted `target/debug/incremental`; use
  `CARGO_INCREMENTAL=0` for host cargo on this box.
- ACE ghost-session window reconfirmed: serial headless boots need ~130 s
  spacing (60 s stalls the next boot).
- index.html "curated-opts plumb-through trap" struck again: new wasm
  exports must be threaded through the init3D opts (namespace-import style
  for stale-pkg safety) or `wasmExports.X` is silently undefined.
- Under `?nullRender` programs only grow via compileAsync (render() never
  runs) — a warms-off arm can read LOWER Δprograms than warms-on. Probe
  semantics documented in harness/entity-warm-probe.mjs.

## Open (next session)
1. **1070 batched session** (GPU box is a person's machine — off-screen
   only): P6 latency A/B (A08 §5.1 longtask+programs correlation,
   `?lightPoolSize=8` halving, cold vs warm `--user-data-dir`); P5
   single-hitch sub-second check; S1 armor eye-confirm (carried);
   1070 indoor eye-test for envcellFusion-on-bare-URL.
2. **S2 accumulated-residency probes** (fresh-boot TN runs peak lru≈15; the
   field freeze needs a long-session precondition) — then re-judge P5's
   burst under real backlog.
3. **W2 remainder**: full S2C coverage table (worklist = codec lint's INFO
   counts: 11 top-level + 63 action + 10 event values un-enumerated) + ACE
   transport edges (A02-F8 ack-retain verify, 128 KB recv scratch).
4. **W3 remainder**: 78 undocumented flag rows (then flip the lint to
   `--strict` in ci-smoke).
5. **Phase 3 marketplace A/B on the 1070** → execute W5's PR-1 re-land
   package (thresholds are written, PENDING-PHASE3 markers in
   parts/W5-marketplace-reland.md).
6. **Roadmap Phase 9a warm-park** — W4's design doc is ready; it explicitly
   rebases on P5's landed sealed-branch shape.
7. acadmp1ge522 account disposition (user decision, carried from session 2).
8. Consider folding `dat_decode_diag` + the two lints into ci-smoke.sh
   (A16 art.5 wanted exactly this).
