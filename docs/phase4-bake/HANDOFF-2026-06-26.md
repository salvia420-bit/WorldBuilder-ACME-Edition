# Phase 4 Bake-Migration — HANDOFF (2026-06-26)

**Branch:** `feat/phase3-particle-2026-06-24` (local `/home/wbterminal/WorldBuilder-ACME-Edition`).
**Status:** P4.0b transport + dedup **and** all of **P4.3 wind** (codec → fallback → producer → full bake →
consumer flip → default-on) are **COMMITTED + functionally verified**. **8 P4.x commits this session, NONE
pushed** (origin/master: 1 ahead / **17 behind**). The one thing OWED: the **batched 1070 visual eye-test**
(the box is offline). Supersedes `HANDOFF-2026-06-25.md`.

---

## 0. TL;DR
P4.3 (the "first real bake", the payoff) is DONE end-to-end and **default-ON**: tree/foliage wind is now
served from per-DID baked `.windclip` artifacts instead of live JS synthesis. Proven **byte-identical to synth**
(the baked frames ARE the runtime's own `buildTreeWindClip` output, bit-copied). The whole Visual-Behavior Suite
(phases 0/1/3) is default-ON in code; `windBake` (phase 4) is now default-ON too. **Everything ships with `=off`
escapes and OWES the batched 1070 visual sign-off** — "loads + 0 errors ≠ renders right" (handoff §4 of the
2026-06-25 doc; the prior 1612-catalog "white slivers" threw zero errors). The 1070 is currently offline.

---

## 1. What's COMMITTED (8 commits, oldest→newest)
| Commit | Step | Summary | Gate |
|---|---|---|---|
| `102dcdd0` | P4.0b-dedup `[R]` | fold `fnv1a_fold`/`wire_f32_bits` through `holtburger_common::bake_fingerprint` (scenery-bake re-exports; web `placements_freeze_hash` reuses) | `cargo test -p holtburger-scenery-bake` 9/9, golden `3c1ae2c0c6cf0ac6` unchanged |
| `fc3929bf` | P4.0b transport | inert wasm `mod suite_fetch` (per-`(did,type)` binary sidecar) + 4 exports + index.html plumb + `?v=` bump | wasm-pack build; exports in pkg js+dts; SwiftShader boot-smoke `suite_cache_size()==0` inert |
| `ebffb9e2` | P4.3 codec | `holtburger-suite-bake::windclip` — `WindClip{num_parts,num_frames,fps,K buckets,rig}`, raw-f32-LE `encode_payload`/`decode_payload`, Lossless fingerprint | `cargo test -p holtburger-suite-bake` 12/12 |
| `262fc1c2` | P4.3 fallback | coverage-gated peel→re-freeze (`attachWindTrees` returns `{built,failed}`; statics.js `concat(failed)`) + inert `windclip` JS decoder | `node harness/test_wind_fallback_peel.mjs` 32/0 + 3 no-regression |
| `7fb83af0` | P4.3 producer | `tools/bake-windclips.mjs` (headless-chromium, drives the runtime's own rig+clip) + inert `window.__dumpWindRig` diag hook | byte-identity proof (see §2) |
| `2d34dd8b` | P4.3 bake-scope | enumerate the bake set the way the runtime peels (`parseDescriptorsJsonl`+`hasWindBend`) → **103 windBend DIDs** | 103 baked / 0 skipped |
| `bf6d90dc` | P4.3 flip | `?windBake=on` fetch-not-synthesize (DEFAULT-OFF as landed) — branch on `suite.get(setupId,"windclip")`; miss/part-mismatch → `failed`→frozen | harness 68/68; OFF `[R]` byte-identical |
| `7493e833` | P4.3 default-on | flip `windBakeEnabled()` default → **ON** (`?windBake=off` escape) + correct stale `url-flags.md` visual-suite defaults | harness 68/68; boot-smoke (see §2) |

## 2. ★ VERIFICATION STATE (read this)
**Proven (functional):**
- **Codec/bake byte-identity** — for all 103 DIDs: in-page Uint32 bit-compare (golden `buildTreeWindClip` vs
  packed) = **0 diff words**; cross-impl decode via the committed JS decoder AND Rust `windclip::decode_payload`
  (reencode_eq + `fingerprint == sidecar`); producer re-run sha256 **byte-identical** (determinism).
- **`windBake=on` boot-smoke** (SwiftShader, in-world Holtburg, `?treeWind=on&windBake=on`): reached in-world,
  `suite_cache_size=19`, **0 console 404s** (was **13** before the full 103-DID bake — the bug the functional
  smoke caught: the runtime winds the broad windBend set, not the 6 `TREE_WIND_DIDS`), 0 decode/asset errors —
  all wind trees animate from the bake, none frozen.
- **OFF `[R]`** — `?windBake=off` ⇒ no `SuiteAssetSource`, no fetch, exact current `windBend.buildClip` line.

**NOT proven (OWED): the batched 1070 visual eye-test.** Byte-identity makes *steady-state* render provably ==
synth, but the `[C]` deltas are visually unvalidated: (1) baked sway looks identical to synth across instances
(bucket phase de-sync preserved); (2) the **cold-load transient** (first LB load renders frozen until the async
fetch warms, OQ-3) is not jarring; (3) the fallback insertion-order/orphan-node delta (statics.js wind attach
moved before the frozen build); (4) un-baked-DID rename → frozen-not-vanished. **The 1070 is OFFLINE** (tailscale
"last seen ~1h ago" on 2026-06-26 ~01:xx). USER authorized driving it ON-SCREEN past-midnight *if no active
users* — but it must be online first.

## 3. ★ DEFAULTS — the Visual-Behavior Suite is now DEFAULT-ON (and the doc was stale)
Ground truth (code, confirmed empirically): `visualEnabled()`=true, `visualAllEffects()`=true, **every** per-effect
reader defaults to `visualAllEffects()`=true, and `windBake` now defaults true. So phases 0/1/3 effects + the P4
bake all run by default. `url-flags.md` previously claimed the suite was "all default-off / frozen render
byte-identical" — **stale**; corrected this session (the `visual`/`windBake`/`visualAll` + 7 per-effect rows + the
§-intro summary). The only genuinely default-OFF suite flag is `treeWind`/`treeWindStrength`/`treeWindDir` (the
legacy 6-DID path — **redundant**: windBend wind already fires via `visual`). Escapes intact: `?visual=off`
(whole suite), `?visualAll=off` (per-effects), `?<effect>=off`, `?windBake=off`.
- Still undocumented in url-flags.md (default-ON via `visualAll`, noted but no individual rows yet): `gemSparkle`,
  `brazier`, `foliagePollen`/`foliageFireflies`/`foliageLeaves`, `breathFog`, `tipFlex`, `itemFx`.

## 4. ARTIFACTS
`${HOLTBURGER_DIST:-/mnt/wbterminal2/holtburger-dist}/suite/` — **103** `0x{DID:08X}.windclip.bin` (+ `.sha256`
+ `.windclip-hash` each), `windclip-coverage.json` (103 baked / 0 skipped), `bake-source.sha256`. **5.0 MB.**
Outside the git repo (served via the `external/holtburger/dist` symlink). Wire = raw `WindClip::encode_payload()`
(num_parts at offset 0, NOT a SuiteBlob container — `fetch_suite_artifact` strips nothing). Re-bake:
`cd external/holtburger/apps/holtburger-web && node tools/bake-windclips.mjs` (serve.py must be up).

## 5. NEXT STEPS (ordered)
1. **The batched 1070 visual eye-test** (the only blocker to "done"): when the 1070 is back online + idle, drive
   on-screen — `?treeWind=on&windBake=on` vs `?windBake=off` (synth) at a dense-foliage LB. PASS = sway visually
   identical · cold-load transition not jarring (else implement OQ-3 pre-warm-at-scene-init) · un-baked rename →
   frozen tree (no vanish) · `?visual=off` byte-identical · no slivers · no perf regression. Batch the rest of
   the queued suite eye-tests (per-effect: glint/magicGlow/… `=off` byte-identical + effect correct).
2. **Push decision** — 17 commits unpinned. `git push origin feat/phase3-particle-2026-06-24` (backup + the
   buildbox pulls from GitHub). Not pushed by choice.
3. Optional janitorial: add url-flags.md rows for the undocumented default-on effects (§3); the producer's lint
   nits (`require`/`__dirname`/`did` unused in bake-windclips.mjs) are harmless.
4. Deferred from the older roadmap: P4.0c-2b (C# canonical config-move), P4.2 (emissive simplify + the 3 stale
   baseline tests), P4.1b/c (drop nested config wire / `vfx_route.js`).

## 6. INFRA STATE (2026-06-26)
- **Laptop stack UP**: serve.py :8765 (live source + `dist/suite` artifacts), wsbridge :8080, ACE udp 9000/9001.
  ACE connection is **flaky** (first connect often times out → reaches `bootState:error`; a reload usually lands
  in-world; the connection can also drop to `error` after a while — it's the laptop ACE, not the client).
- **1070 OFFLINE** (`<user>@<gpu-box-ip>` = tailscale `desktop-4anudo2`; "offline, last seen ~1h ago"). Eye-test
  blocked until it's back. USER guidance: past-midnight on-screen is OK *iff* no active users (check `quser` /
  `nvidia-smi` / RobloxPlayerBeta first); never `browser.close()` a live session.
- **pkg/** fresh (`?v=phase4-suite-fetch-2026-06-25`); JS-only changes (the flip) need no rebuild.

## 7. ★ DURABLE GOTCHAS (this session)
- **Producer in-page wasm trap**: a bare `import('/…/pkg/holtburger_web.js')` returns an UNINITIALIZED second
  wasm instance (every call throws). Resolve the initialized singleton from the DOM modulepreload href incl. the
  `?v=` query (it drifts per build) — `bake-windclips.mjs` does this.
- **Byte-identity-by-construction**: the producer runs the UNCHANGED in-page `buildTreeWindClip` (V8 math) and
  byte-copies frames LE — NO Node sin/cos re-derivation. The R4 **material gate** (only union verts whose surface
  material loaded) is load-bearing for the rig; a non-gated walk bakes a different bbox → different sway.
- **Bake scope = consume scope**: the runtime peels `(treeWind && isTreeDid) || (visual && hasWindBend)`; with
  `visual` default-ON the consume set is the **windBend-descriptor set (103)**, NOT the 6 `TREE_WIND_DIDS`. Bake
  must cover the union or un-baked DIDs 404→frozen (regression). The functional boot-smoke caught this.
- **Resource-timing buffer caps at 250** → it evicts the windclip/scenery entries; use the wasm `suite_cache_size()`
  (authoritative) + the console 404 count, NOT `performance.getEntriesByType('resource')`, to judge fetches.
- **Doc drift**: `url-flags.md` lagged the code's default-on flips by weeks. Trust the `*Enabled()` reader seeds.

## 8. RULES (unchanged, don't violate)
- **`[R]`** = off+on byte-identical (ship on a green test). **`[C]`** = off byte-identical only; needs a 1070
  eye-test, never waved through on the off-trace (windBake is `[C]` — shipped default-on by USER decision, eye-test
  still owed). **PER-DID**, not per-LB. Deterministic bake; no live input baked (dir/strength baked-authoritative);
  light-count==0. Capped single-crate builds only (never `--workspace`); cargo PATH prefix for `capped-build`.
