# Phase 4 Bake-Migration — HANDOFF (2026-06-25)

**Branch:** `feat/phase3-particle-2026-06-24` (local repo `/home/wbterminal/WorldBuilder-ACME-Edition`).
**Status:** S0 keystone closed + **functionally** validated (visual sign-off owed by user). Bake-transport
foundations landing. **7 local commits, NONE pushed** (origin behind 0 / ahead 7).

---

## 0. TL;DR / where we are
Phase 4 = move the Visual-Behavior Suite **bake-side** (Rust/WB.Terminal produce per-DID artifacts the
runtime FETCHES; three.js renders) **and simplify**, guarded by `off=byte-identical`. Re-phased 2026-06-25:
NEW P4 = bake-migration; texture → P5; classifier → P6.

The 16-agent buildbox sweep already produced the master plan. We're now **implementing** it via the ordered
commit list. P4.0 foundations are mostly done; the keystone bug (S0) is fixed and the suite now resolves its
1168-effect classifier catalog at runtime (proven). Next concrete step: the wasm `mod suite_fetch` + index.html
wiring, then **P4.3 wind** (the first real bake = the payoff).

**Plan / source docs:**
- Master plan: `~/from-vm/phase4-bake-work/PHASE4-BAKE-MIGRATION-SYNTHESIS.md` (+ SCHEMA / SIMPLIFICATION-LEDGER /
  SAFETY-CASE / DETERMINISM-AND-TESTS / parts A01–A16). Sweep tarball sha-verified there.
- Spec: `docs/PHASE4-BAKE-MIGRATION-WORKFLOW-2026-06-25.md`. Design roadmap: `docs/visual-behavior-suite-design-2026-06-23.md` §8.
- Per-step notes: `docs/phase4-bake/P4.0a-preflight-findings.md`, `P4.0c-notes.md`, `probe-s0.mjs`, this file.

---

## 1. What's COMMITTED (7 local commits, oldest→newest)
| Commit | Step | Summary | Gate |
|---|---|---|---|
| `2234b36e` | P4.0a | Pre-flight: proved S0 (served 1168 catalog → **0/0** through the real parser) | probe-s0.mjs |
| `24670fe1` | P4.0c-1 | **Keystone**: tolerant JS parser (`vfx_catalog.js parseDescriptorsJsonl` reads `{name}`, hoists nested config, strips BOM) → served catalog **1168/1168** live | test_vfx_catalog 19/0 |
| `7eb41b0f` | P4.0c-2a | C# `SaveJsonl` emits **no BOM** + `VisualDescriptorRoundTripTests` (xunit) | dotnet test 2/2 |
| `8d1bffcb` | P4.0b-js | `scene3d/suite_assets.js` — per-DID binary-sidecar transport (inert greenfield, wraps wasm `fetch_suite_artifact`) | test_suite_assets 10/0 |
| `a297e11c` | P4.1a | Dedup `_splitConfig`+`mergeComponentConfig` → `scene3d/vfx/config_merge.js` ([R]) | test_config_merge 28/0 |
| `b746ab55` | P4.0b-rust-1 | `holtburger-common::bake_fingerprint` — unified determinism primitive (`fnv1a_fold`/`wire_f32_bits`/`fingerprint_f32_bits` SixDp+Lossless) | capped `cargo test -p holtburger-common` 5/0 |
| `a3fa943f` | P4.0b-rust-2 | `holtburger-suite-bake` crate — per-DID binary-sidecar **container** (`SuiteBlob`: magic/ver/type_tag/payload/content_hash, deterministic) | capped `cargo test -p holtburger-suite-bake` 5/0 |

All gated; no NEW test failures introduced (see §5 baseline).

## 2. What needs COMMITTING / pending decisions
- **The 7 commits are LOCAL and UNPUSHED.** Decide whether to `git push origin feat/phase3-particle-2026-06-24`
  (useful before any buildbox session — the box pulls from GitHub; also a backup). Not pushed yet by choice.
- **`M .gitignore` is NOT ours** — a pre-existing unrelated change (adds `/vanquish_heights.json`, a 16.7MB blob,
  to gitignore). Leave it; do not fold into a Phase-4 commit.
- Pre-existing untracked session junk in `git status` (crates_probe_*.rs, old HANDOFF-*.md, harness/diag-1070.mjs,
  examples/probe_*.rs, 02000ADC.bin, …) — NOT ours; ignore.

## 3. NEXT STEPS (ordered)
1. **P4.0b wasm `mod suite_fetch` + index.html** (the remaining transport half; `suite_assets.js` already wraps it):
   - Clone wasm `mod scenery_fetch` (`apps/holtburger-web/src/lib.rs:2070`, `init_scenery_base_url:2131`,
     `scenery_cache_size:2145`, uses `holtburger_resource_http::fetch_bytes`) → `mod suite_fetch` re-keyed by
     `(did, type)`: `init_suite_base_url(url)` + `fetch_suite_artifact(did:u32, artifact_type:String) -> Vec<u8>`
     (auto-marshals to Uint8Array) + `suite_cache_size()` + `clear_suite_cache()`. `SUITE_BASE_URL="../../dist/suite/"`,
     artifact URL `{base}0x{DID:08X}.{type}.bin`.
   - **index.html plumb-through (the trap)**: wire the new exports into BOTH `wasmExports` sites (named import +
     init3D opts) AND bump `?v=` cache-bust, else `suite_assets.js`'s `typeof wasmExports.fetch_suite_artifact==="function"`
     guard silently no-ops. (A07 §2.8 cites the exact index.html lines.)
   - Rebuild wasm: `cd apps/holtburger-web && PATH="$HOME/.cargo/bin:$PATH" capped-build wasm-pack build --target web --out-dir pkg --dev` (~50s).
   - **Gate (laptop-only, no 1070 needed)**: SwiftShader boot-smoke confirms inert-when-unreferenced
     (`suite_cache_size()==0`, no fetch when no descriptor carries a `sidecars` ref). Nothing renders from it until P4.3.
2. **P4.0b dedup** (the simplify half, [R], LAST + careful): replace the inline `wire_f32_bits`/`fnv1a` copies in
   `holtburger-scenery-bake/src/lib.rs:235/265` + the wasm `lib.rs:2280` twin with `use holtburger_common::bake_fingerprint::*`.
   **Gate: `placements_fingerprint_is_stable` (scenery-bake/tests/integration.rs:367) MUST stay green** (capped test).
3. **P4.3 wind** — the first real bake (the payoff): `holtburger-suite-bake` windclip codec (frame-major raw f32 LE,
   K=4 phase-buckets, rig rides the `.bin`) + a **Node-wasm JS-golden producer** (`tools/bake-windclips.mjs` runs the
   UNCHANGED `buildBboxRig`+`buildTreeWindClip` over wasm `fetchBuildingPlacement` = byte-identical by construction),
   then `animated_scenery.js` fetch-not-synthesize. See A01/A12 + synthesis §4 P4.3 (folds A12 R1–R7, incl. coverage-gated
   peel: a clip miss must NOT vanish a tree).
4. Deferred / parallel: **P4.0c-2b** (C# canonical config-move + minimality + `schemaVersion:1` + re-emit catalog —
   bundle with the 1070 visual batch so the eye-test validates the canonical catalog). **P4.2** (emissive simplify +
   reconcile the 3 stale baseline tests). **P4.1b/c** (drop nested config wire; `vfx_route.js`).

## 4. ★ KEYSTONE (S0) VALIDATION STATE — read this
The pre-P4.0c bug: the served classifier catalog used `components:[{name,config:nested}]` while the JS parser read
`c.id` + top-level `o.config` → **every one of the 1168 descriptors resolved to 0 components / 0 config** → the suite
rendered NOTHING (frag + emissive + particle all inert). P4.0c (tolerant parser) closes it.

**Proven (validated):** `node docs/phase4-bake/probe-s0.mjs` → before 0/0, after **1168/1168**. On the **real GTX 1070
(ANGLE/D3D11)**, in-world outdoor Holtburg, `?visual=on`: console `[vfx] loaded 1168 descriptors`, **0 errors**;
`?visual=off`: catalog never fetched (inert). So the channel is closed end-to-end + boots clean + off is inert.

**NOT proven (owed):** that the 1168 effects ATTACH + RENDER and the world LOOKS CLEAN (no slivers/artifacts — the
careful-rollout gate). **"loads + 0 errors" ≠ "renders right"** — the prior "1612-catalog → world of white slivers"
incident threw ZERO errors. Both screenshot attempts failed: v1 captured the boot-load black frame (init3D ~26–90s,
shot too early), v2 hit GPU contention with a Roblox player on the 1070 (WebGL-context flake → `no-ext`/9 errors).
**USER will do the visual sign-off later.** Until then: "fix landed + functionally confirmed; visual sign-off pending."

## 5. Test baseline (gates are "no NEW fails", never "all green")
- TIER1 full harness `node harness/run-all.mjs --js` takes >5min → gate via targeted subsets.
- **Pre-existing baseline fails at `6828ccc2` (NOT ours; verified by stash-and-rerun): 3 stale-default-OFF VFX tests** —
  `test_vfx_flameflicker.mjs` (1), `test_vfx_frag_attach.mjs` (1), `test_vfx_frag_install.mjs` (2). They assert the old
  `?visual` default-OFF; the suite shipped default-ON (`f3942a95`). **Deferred to P4.2** (A06 per-effect `enabled:`).
  P4.0c already reconciled the same staleness in `test_vfx_catalog.mjs` (12/2 → 19/0).

## 6. INFRA STATE
- **Laptop stack UP** (for the deferred boot-smoke / 1070 work): serve.py (:8765), wsbridge `holtburger-wsbr` (:8080),
  ACE `dotnet ACE.Server.dll` (UDP 9000/9001). `dist` → `/mnt/wbterminal2/holtburger-dist` (served catalog there).
  Laptop tailnet IP `100.116.47.66`. **Leave up or shut down per preference** (doesn't touch the 1070).
- **Reverse tunnel to 1070: DOWN** (torn down — I'm off their machine).
- **1070 BUSY** — `young@100.127.215.75`, a person is playing **Roblox** (RobloxPlayerBeta.exe). Do NOT hammer the GPU;
  the WebGL flake was contention. Resume the visual eye-test only when it's free.
- **Buildbox: TERMINATED** (us-central1-a). Orphaned phase-3 brazier WIP backed up on-box at `~/orphaned-wip-pre-phase4-2026-06-25/`.
- **wasm `pkg/` FRESH**: `--dev` capped build (Jun 25 21:32, 17.6MB). JS-only changes don't need a rebuild; the
  wasm `mod suite_fetch` (step 1) WILL need one.

## 7. ★ DURABLE GOTCHAS learned this session (re-use the 1070 / capped builds)
- **capped wasm build needs the cargo PATH**: `PATH="$HOME/.cargo/bin:$PATH" capped-build wasm-pack build …`,
  else `ionice: failed to execute wasm-pack: No such file or directory`. Single-crate `cargo test -p X` likewise.
- **NEVER `pkill -f rust-analyzer`** — the pattern self-matches your own command string → kills the running shell
  (exit 144). (And there was no rust-analyzer to reclaim anyway.)
- **Laptop has PRE-EXISTING git stashes** (`wip-cells-and-other-diag-entries`, …). **Do NOT `git stash pop` blindly** —
  I accidentally popped an unrelated one mid-verification (conflict markers in cells.js/diag/spatial; recovered via
  `git checkout HEAD -- <files>`, stash preserved). Check `git stash list` first; prefer NOT using git stash.
- **1070 MODE3 real-GPU headless** (driver runs ON the box): `chromium.launch({headless:true, executablePath:
  "C:\\Users\\young\\AppData\\Local\\ms-playwright\\chromium-1223\\chrome-win64\\chrome.exe", args:["--use-angle=d3d11",
  "--use-gl=angle","--ignore-gpu-blocklist","--enable-gpu-rasterization","--disable-dev-shm-usage"]})` via
  **playwright-core**. Stack: serve.py:8765 + reverse tunnel `ssh -fN -R 18765:127.0.0.1:8765 young@100.127.215.75`
  (app at `http://127.0.0.1:18765/...`) + wsbridge `ws://100.116.47.66:8080/` (binds 0.0.0.0, no tunnel) + ACE 9000.
  URL flags: `renderer=3d&quality=mid&nosw=1&autoLogin=1&account=phase4demo&password=phase4demo&autoSpawn=first&server_host=127.0.0.1&server_port=9000&bridge_url=ws://100.116.47.66:8080/`. Spawns outdoor Holtburg `0xa9b40019`.
- **1070 single-session relog (USER guidance)**: a relog **KICKS** the prior character → **wait ~7s** → log in again.
  Use the **same character** (`autoSpawn=first`) for A/B screenshot consistency. (My driver used a 26s gap + reload-retry;
  tighten to the 7s.)
- **1070 screenshot is fighting** (for the visual re-capture): init3D takes ~26–90s → a too-early shot is HUD-on-black.
  In THIS build `__diag` has **no `render`/`particles` key** (keys: spawns/wire/physics/motion/pvs/assets/…),
  `liveScene3d.cameraSwitcher.activeCamera` never goes truthy, and a canvas `drawImage` lum reads 0 (WebGL). So the
  reliable recipe = **taskkill chrome between arms + relaunch-on-`no-ext`/flake** + a long fixed settle (~60–90s) then
  shoot. Driver saved: laptop `…/scratchpad/s0-keystone-1070.mjs` (v2) + on-box `C:\Temp\s0-keystone-1070.mjs`.

## 8. The rules (don't violate when implementing)
- **Dual mandate**: migrate bake-side AND simplify, guarded by **`off=byte-identical`** (every `?<effect>=off`/`?visual=off`
  byte-identical to pre-P4), the legacy firewall (no wire/physics/replicated write; **no live input baked** — weather/
  creature-part-frames/camera stay runtime), the program-key firewall (O(SETs), config/per-instance never in the key),
  light-count==0, deterministic reproducible bake.
- **★ PER-DID, not per-LB** — a tree/spear setup appears in 1000s of LBs; key suite artifacts by DID (like the vfx
  catalog), reuse scenery's bake→fetch MECHANICS but not its per-LB key.
- **[R]** = pure relocation (off+on byte-identical, ship on a green test). **[C]** = ON-path change (off byte-identical
  only; needs a 1070 eye-test, never waved through on the off-trace). P4.0c was [C] → hence the owed visual sign-off.
- WB.Terminal/C# = Opus-4.8 only. OOM: never `cargo build/test --workspace`; capped single-crate only; single-project
  `DOTNET_ROLL_FORWARD=LatestMajor dotnet build/test WorldBuilder.Tests` is memory-safe.
