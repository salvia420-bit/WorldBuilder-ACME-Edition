# P4.0c — S0 keystone fix (part 1: the tolerant JS parser)

**Closes the severed C#↔JS descriptor channel** so the served classifier catalog renders.
Files: `scene3d/vfx_catalog.js` (`parseDescriptorsJsonl`), `test_vfx_catalog.mjs`.

## What changed
`parseDescriptorsJsonl` is now a **strict superset** of the prior parser:
- component id read from `string | {id} | {name}` (the live classifier emits `{name}` — the bug).
- per-component **nested `config`** hoisted to `config[id]`; a top-level `config{}` (the canonical
  future-emit shape) wins on collision. `_splitConfig` (`frag_attach.js:44`) buckets the object-valued
  keys into `byId[componentId]`, so config now reaches every component.
- a leading **UTF-8 BOM** stripped (the C# emit writes one; `res.text()` strips it, a raw node read does not).
- `sidecars{}` + `schemaVersion` recorded (for the P4 bake-migration).

## Proof (the inverse of P4.0a)
`node docs/phase4-bake/probe-s0.mjs` on the **live served catalog**:
```
before P4.0c:  { parsed: 1168, withComps: 0,    withConfig: 0 }
after  P4.0c:  { parsed: 1168, withComps: 1168, withConfig: 1168 }
```
All 1168 descriptors now resolve components + config. `test_vfx_catalog.mjs`: **19/0** (was 12/2);
added {name}, nested-config-hoist, BOM, and gem-sparkle-mech assertions; reconciled the 2 stale
"`?visual` defaults OFF" assertions to the default-ON reality (the real lock is now
`?visual=off ⇒ selects nothing = byte-identical`).

## Gate
- `test_vfx_catalog.mjs` 19/0 ✓ · S0 probe 1168/1168 ✓ · `test_vfx_legacy_safety` 18/0 · `test_vfx_firewall` 7/0 · `test_gemsparkle_emit` 35/0 — **no new fails**.
- **Off-trace (structural):** `?visual=off → isWind selects nothing` asserted; the master kill short-circuits
  before the parser, so OFF stays byte-identical regardless of the parser change.

## Baseline note (expanded)
The pre-Phase-4 baseline at `6828ccc2` carries **3 stale-default-OFF VFX test fails** (verified by stashing
this change and re-running): `test_vfx_flameflicker.mjs` (1: "?flameFlicker alone = off"),
`test_vfx_frag_attach.mjs` (1: "seam OFF…"), `test_vfx_frag_install.mjs` (2: "?visual OFF ⇒ null").
All three assert the old default-OFF in their own flag/seam logic — the same `f3942a95` mismatch P4.0c fixed
in the catalog test. **Not regressions; deferred to P4.2 (A06 per-effect `enabled:` reconciliation).** Gates
are baselined at this set, never compared against all-green.

## Part 2 progress (C# side)
- **DONE — stop the BOM at source + round-trip harness:** `VisualDescriptor.cs:246` `SaveJsonl` now writes
  `new UTF8Encoding(false)` (was `Encoding.UTF8`, which emits a BOM). New `WorldBuilder.Tests/
  VisualDescriptorRoundTripTests.cs` (xunit, 2 facts: emits no BOM; SaveJsonl→LoadJsonl preserves did/archetype/
  mech/component-name/nested-config). `dotnet test --filter VisualDescriptorRoundTrip` → 2/2 pass (net8 under
  net10, LatestMajor). The BOM was already defused on every *current* path (JS parser strips it, C# StreamReader
  strips it, browser strips it) — this fixes it at the source + future-proofs the P4 bake oracle.
- **DEFERRED to the catalog re-emit (bundle with the 1070 batch) — canonical config-move + schemaVersion:**
  emit top-level `config{}` keyed by component-id + drop the nested `VisualComponentRef.Config` from the wire +
  minimality (emit `config[id]` only when ≠ archetype default) + `schemaVersion:1` + `sidecars`. NOT required for
  rendering (the tolerant parser handles the served nested shape); doing it now would only matter after a re-emit,
  and the re-emit is best done right before the 1070 so the eye-test validates the final canonical catalog. Keep
  the `vfx classify` audit JSON distinct (don't null the audit's nested config). [m2]
- **1070 ON sign-off (DEFERRED, batched):** P4.0c is a [C] ON-path activation — the first time the ~1168-effect
  catalog actually renders. Queue the 1070 eye-test (`?visual=on` now-live vs `?visual=off` frozen) on the next batch.
