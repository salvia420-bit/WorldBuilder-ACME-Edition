# 1112 — net-fixwave session-4 handoff (2026-07-10)

Coordinator: laptop Fable session, executing docs/1111.md's open list
**directly** (standing directive: no VM, no subagents). Durable state:
`~/from-vm/net-fixwave-20260709-STATUS.md` (§SESSION 4) + this file.
Probe artifacts: `/mnt/wbterminal2/holtburger-scratch/s2-accum-20260710T125326Z/`.

## What landed (branch `net-review-wave2`; 8989b96c…4521061e pushed +
## merged to origin/master; 19db7648 + this doc's commit pending push)

| commit | 1111 item | one-line | key validation |
|---|---|---|---|
| 8989b96c | 4 + 8 | 78-row flag-docs backfill (url-flags.md §7, values columns match each reader's REAL spellings) + ci-smoke gains L1 (`lint-url-flags --strict` — the ratchet is LIVE), L2 (codec lint), S5 (`__diag.datDecode()` on the S4 boot: main+worker non-null, 0 parse/decode fails, jsMissing empty), 130 s inter-boot spacing | --strict exit 0 (310 rows / 0 undocumented); full chain ALL PASS |
| 492e9cc1 | (regression) | **P6 regression caught by the new S2 gate**: three's `compileAsync` ready-poll throws an UNCATCHABLE `undefined.isReady` TypeError when a material disposes mid-link (fires inside three's own setTimeout). `guardedCompileAsync` (compile + guarded 10 ms poll) at all 3 warm sites | errors=FAIL(2) → PASS; white-tex 1125/1125 |
| 25a63e2f | 3 | W2 remainder: all 84 codec-lint INFO values adjudicated vs ACE (`scripts/net-review/s2c-coverage-2026-07-10.md`). Live gaps enumerated: 0xF7C1 AccountBanned, 0x01C8 → `GameEvent::AllegianceUpdateDone`. Chorizite value errors documented (0x0220 vs ACE 0x021A; 0xF7EB vs 0xF7EA). A02-F8 ack-retain VERIFIED = exact ACE parity (kept). A02 Opt-1: 128 KB recv scratch → 4096 B | protocol 385/385 + session 30/30; codec lint 95/94/93 exit 0; RELEASE wasm 4,757,561 B; full smoke ALL PASS on it |
| 4521061e | 2 | S2: TN probe gains `--accumulate "town,…" --dwell N` (POIs verified vs ace_world). 6 towns → LRU cap (203) by town 3. **P5 burst re-judged under real backlog: 203→2 in one 500 ms bucket, maxDt ≤83 ms, longTaskMax 127 ms, 0 no-frame buckets; `=off` staircase 283 ms spikes / 286 ms longTask (2.3× worse)** | run dir above; both arms' 45 console 404s = empty-LB fetch misses, benign |
| 19db7648 | 6 | **Phase 9a warm-park** (`?warmPark=on`, default OFF pending 1070 eye-gate): park/unpark/disposeParked + pool pressure in landblock_lru.js; loader fast-paths = the unpark seam; atlas hide/show (`setVisibleAt`, iid recorded); emitter rebuild from stashed anchors (statics.js export, R-10-guarded); A11-F3 `__cacheOwned` geometry guard; sealed parks in descending-Chebyshev order | functional round-trip PASS 9/9: 203-LB backlog parked with **evicted=0 for the whole run**, marks kept, pool ~41 MB/256 MB, return trip re-attaches, 0 non-benign errors (`scripts/net-review/warmpark-roundtrip-probe.mjs`) |

**Rebuild note:** pkg/ rebuilt RELEASE once (4,757,561 B — W2's protocol
changes). Phase 9a is JS-only (no wasm delta). Other checkouts must rebuild
pkg after pulling 25a63e2f.

## Observations worth keeping
- Park/unpark churn at the LRU cap boundary is heavy during town-hop
  accumulation (parkedTotal 3217 vs parked 256 in one run) — cheap
  re-attach replacing the old evict/re-bake thrash, but it argues for the
  A11-F7 hysteresis latch (W4 §3.4 wanted it with P5 anyway).
- The S2 A/B's `land→recovered` stage metric is confounded by post-drain
  interior streaming; judge sealed-purge arms by longTaskMax + in-drain
  maxDt instead.
- ACE ghost window: 130 s spacing held all session (5 clean boots).
- rg `-rln` trap struck once mid-session ("ln-redaction" — it IS just
  `--replace ln`); plain `rg -ln` used thereafter.

## Open (next session)
1. **1070 batched session** (grew): P6 latency A/B; P5 single-hitch check;
   S1 armor eye-confirm; envcellFusion-on-bare-URL indoor eye-test;
   **warmPark eye-gate** (park→unpark visual parity + roam feel — then flip
   default-ON per W4 §3.1) + `?warmParkBudgetMb` sizing on real VRAM;
   marketplace Phase 3 → W5 PR-1 re-land.
2. A11-F7 hysteresis latch for the sealed keep-set flicker (now doubly
   evidenced: sealed lru 1↔6 oscillation + warm-park churn).
3. W4 Phase 10 (refcounted wasm decode cache) — P2's `dat_decode_diag`
   counters are its gate instrumentation, all landed.
4. Consider `warmPark` bare-default flip after the 1070 gate; then wire the
   warmpark round-trip probe into ci-smoke as an S6 leg.
5. acadmp1ge522 account disposition (user decision, carried from session 2).
6. MEMORY.md is 24,835 B — over its self-imposed 24,400 B load budget
   (SessionStart hook flags it every session; memory edits are user-directed
   only, so this is a user call).
