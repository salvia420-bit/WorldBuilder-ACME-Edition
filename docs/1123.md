# 1123 — session 15: A15 residency S15a+S15b landed (park UseTime floor default-ON + `?fixedGrid` terrain slot grid), netWorker decided NOT-PROMOTED, bakeQueueCap sweep run + read honestly (keep 4), all gates green (2026-07-11)

Ledger chain: docs/1120.md (+ appendix) → docs/1121.md (S13) → docs/1122.md
(S14) → this file. Executed the docs/1122.md §5 owed list — coordinator
(orchestrator/researcher) + two local Opus 4.8 executors (file-ownership
partition, every agent citation read-verified per house rule). JS-only by
design: NO wasm rebuild (zero Rust touched; next wasm batch carries the S16
candidates).

## 1. What landed (1122 §5 item → code)

- **S15a — park→DBOCache 30 s UseTime floor** (`?parkUseTimeMs`, default-ON
  30000; `=0/off/false` escape = pre-S15 behavior): retail's DBOCache
  discipline (acclient.c:83485 `GetIfUsing`, release ≠ free within the ~30 s
  freelist floor) applied at the park layer. A parked slot younger than the
  floor is NOT true-disposed by park-pool byte PRESSURE
  (landblock_lru.js `_tickParkPoolPressure`); re-entry within the window is a
  pointer re-adopt — zero decode, zero bake. Generalizes sealedKeepRing from
  "the sealed ring" to "any recently-parked slot". The byte LRU stays the
  backstop BEHIND the floor: pressure disposes only entries older than the
  floor; all-young overage is RECORDED (getStats
  `useTimeDeferredCount/Bytes`, cumulative event counters) and ages out —
  pressure never violates the floor. Non-pressure dispose paths
  (evict-on-teleport, flushParked, dispose()) unchanged. NEW
  test_park_usetime.mjs (27 checks: young-survives-pressure, aged-disposable,
  escape parity, sealedKeepRing interplay, all-young deferral, re-adopt).
- **S15b — `?fixedGrid` player-centered TERRAIN slot grid** (default-OFF,
  explicit `1/on/true` opt-in — footgun-safe reader): retail
  `LScape::update_block` (acclient.c:307916) fixed `land_blocks[W²]` grid
  ported per PLAN-fixed-slot-grid-residency-2026-07-11.md §2. NEW pure
  injected-deps core scene3d/fixed_grid.js (`FixedSlotGrid`: shift-in-place,
  exact incoming/vacated edge diffs, teleport |d|≥W → whole-grid invalidate
  == today EXACTLY, map-edge clamp) + thin index.js wiring inside the
  `loadTerrainRing` facade — the shared terrain call-site of BOTH ring
  drivers (world_stream.js + the legacy index.html A15-Q4-SYNC block), so
  both route with zero driver edits. Grid decides WHAT is resident; fetch
  routes back through the factored-out `_runTerrainRingBatch` (the identical
  batched call the OFF path makes — no parallel bake path, plan §3). ON-path
  behavior delta: steady state (sub-LB/stationary) does ZERO terrain work
  where OFF re-ran the ring every position packet. S15b scoping (accepted
  deviation): the vacated-edge `releaseEdge` seam is a documented no-op —
  the only public release primitive parks the WHOLE LB (exceeds terrain-only
  scope); the exact vacated set flows through the seam so S15c wires a real
  park with zero core change. `?diag=1` derived-view assertion
  (grid resident vs `terrainBakedLbs`∩block) warns loudly with the hex diff;
  telemetry `window.__fixedGrid.getStats()`. NEW test_fixed_grid.mjs (39
  checks: 8-direction shifts + exact edge sets, interior-untouched spy
  invariant, multi-LB deltas, teleport invalidate, clamp, flag truth table,
  comparator).
- **netWorker promotion — DECIDED: NOT promoted** (1122 §5.3 closed). From
  the banked S13 Tier-1 A/B (1121 §4–5): workDelta +17% (109 vs 93) AND
  settle +24% (13,765 vs 11,056) on the worker arm — genuinely more streamed
  work per stop on every cold load, not a churn artifact; benefit =
  freeze-survival 8/8 vs 7/8. A +24% cold-load settle tax to insure a 1-in-8
  pathological-freeze case is the wrong default trade. `NET_WORKER_DEFAULT`
  stays false (seam comment + url-flags row record the decision + numbers);
  netWorker remains the opt-in resilience flag for bot/agent sessions;
  loginDefer stays dormant by design. Revisit ONLY if A15 residency erases
  the settle delta.
- **2nd decode worker — stays DEMOTED, now closed** (1122 §5.2): the A14
  gate was "still decode-bound post-B1"; S14 measured decode amp = 1.00 on
  both instances (every distinct palette-free surface decoded once). The
  cold pipe is not decode-bound; no build.
- **wasmMemoryBytes battery column** (1122 §5.6): battery-telepoi.mjs
  samples both instances per landed stop AFTER settle via the same
  `__diag.datDecode()` relay ci-smoke S5b reads — per-row
  `wasmMemMainMB/wasmMemWorkerMB` + summary med/max. Fully fail-soft (own
  3 s race resolving to nulls; legacy pkg/absent worker → nulls; never
  aborts/slows a stop). Coordinator review caught + fixed one real bug: a
  LATE evalInPage rejection after the timeout won the race was an
  unhandledRejection — fatal to the whole driver on modern node — now
  pre-swallowed (`ev.catch(() => {})`).

## 2. bakeQueueCap sweep (1122 §5.1) — run AND the honest read: keep 4

Run dir `/mnt/wbterminal2/holtburger-scratch/battery-s15sweep-20260711T132343Z/`
(provenance.txt inside). cap2 / default(4) / cap8, INTERLEAVED randomized arm
order per round (the appendix risk note), v2 protocol (--maxStops 21,
landPollMs 100), wrapper-owned 65 s gaps before EVERY launch (cross-ARM fresh
launches need the ACE 60 s-reap gap too — the driver's resume-only quietGap
can't cover them), run against a FROZEN snapshot of S14 HEAD 83f5a697 on
:8766 (the live tree was under s15 agent edit — measurement isolated by
construction). All arms 62/62 accounted, 0 stalls, 27/27 boots in-world.

- Headline (MISLEADING): settleMed cap2 8,804 / default 10,915 / cap8 8,102.
- **Honest read: the headline gaps are session-fragmentation artifacts, not
  cap effects.** Renderer crashes fragmented the arms differently (7 vs 9 vs
  11 sessions — settleMed tracks session count monotonically: younger
  sessions = less accumulated renderer RSS = faster stops). The
  fragmentation-immune read is per-POI PAIRED deltas on the same 62 stops:
  **cap2 vs default median −183 ms (29/53 wins), cap8 vs default ±0 ms
  (26/53) — noise.** Verdict: **bakeQueueCap stays 4.** Secondary real
  signal: lower caps issue less in-window work (workMed 51/114/79) — the
  queue drains slower and work trails past the settle window; also cap2
  capped-25s 9 vs default 13. If a cap revisit ever matters it needs a
  crash-free protocol first (the A15 residency line is that work).
- Crash cadence was much higher than s14's run (sessions died every ~2–13
  stops; the laptop was concurrently running two agents + gates). The
  wrapper's resume machinery + interleave absorbed all of it — that, plus
  pairing, is why the sweep still produced a clean read.

## 3. Live validation

- **ci-smoke --full (live S15 tree, bare defaults): PASS end-to-end** —
  boot=PASS(5900 ms), errors=0, white=0 (entityMeshes 1130, greyFallback 0),
  keepalive PASS, datDecode PASS, canaries S5b PASS (worker amp=1.00, cache
  322/1395, mem main 382 MB / worker 104 MB), warmpark PASS. This is the
  default-ON bar for S15a (bare default loads+spawns+0 errors). S0
  WARN-stale-predates-rust-commit is the known mtime-vs-commit-time false
  positive: the s14 wasm (08:20Z) was built from the exact sources committed
  as 83f5a697 (08:54Z); zero Rust touched this session.
- **fixedGrid ON-path probe** (laptop nullRender, `?fixedGrid=1&diag=1`,
  scratchpad driver): boot in-world 6,749 ms, **0 console errors**; seed
  observed; @telepoi teleport-invalidate observed; a real one-LB-west
  `@teleloc` hop exercised the SHIFT path live (crossings=2, shifts=1,
  releaseCalls=1); **930 steady-state updates did zero terrain work** (the
  churn the OFF path pays per packet); explicit derived-view asserts clean
  ({unbacked/untracked/offBlock: []}) at every leg. Known diag noise: 13
  cumulative assertWarns, all transient `unbacked` during the async bake
  window right after seed/teleport (the wired per-tick assert fires before
  bakes land; it goes quiet once baked, and the shift added ZERO warns).
  Diag-only, warn-only, default-OFF flag — left loud on purpose; S15c can
  add a post-crossing grace if it ever matters.

## 4. Gates (all green before commit)

- Combined-tree unit battery (coordinator re-run after BOTH agents landed):
  test_park_usetime 27/0 (NEW), test_fixed_grid 39/0 (NEW),
  terrain_ring_batch 23/0, lru_evict 39/0, warmpark_dualstate 25/0,
  sealed_keepring 5/0, null_lb 12/0, server_urgency 17/0, light_eviction
  9/0, a15_q4 47/0, lb_objects_shared 13/0, init3d_idempotency 10/0 —
  **267 checks, 0 failures**.
- Lints: url-flags --strict PASS (3 changed rows: NEW `parkUseTimeMs` +
  `fixedGrid`, netWorker decision update), harness-params (L3) PASS.
  node --check clean on all touched files.
- Flag-off byte-identity (the fixedGrid ship gate) read-verified in diff:
  grid constructed only under the flag; `FIXED_GRID_ENABLED &&
  this._fixedGrid` short-circuits on the false const; the OFF path is the
  pre-S15 `runTerrainRingBatch` call behind one extra function frame.

## 5. Owed / carried → session 16

1. **fixedGrid A/B soak** (plan gate §4.2): `?fixedGrid=1` battery arm vs
   default on the v2 driver — walk/short-hop settle should drop, teleport
   unchanged, renderer deaths 0; plus the §4.3 B1-hit-rate walk read. Then
   the default-ON flip decision (+ S15c: wire releaseEdge to a real
   whole-LB park through the seam, extend grid to buildings/statics/scenery).
2. **1070 owed grows**: B1 surface-cache real-GPU eye-test — ATTEMPTED this
   session (MODE2i recipe: muted off-screen chrome via schtasks WLS15,
   GPU assert passed = ANGLE NVIDIA GTX 1070 D3D11, `eyetest1`/Developer
   account + "Eyetest Fable" character created via
   `createTestCharacter`) — **box dropped off the tailnet mid-run** (3
   tunnel deaths; tailscale: offline). Groundwork banked on the box
   (C:\Temp\launch-wls-s15.bat + WLS15 task, profile cdpwb-s15) and laptop
   (scratchpad eyetest driver; character exists — next run skips creation).
   Also owed there: fixedGrid seam/LOD shift eye-test (plan §4.5),
   static_batch_x + anim-scenery follow-ons (A13b/c).
3. **S16 wasm batch candidates** (carry from 1122 §5.6): palette-composed
   entity-surface cache (key (did, palette-fingerprint)); wire the NEW
   wasmMem battery columns' trajectory into a ci gate once a battery has
   banked baselines (the columns landed this session; the sweep predates
   them — snapshot driver).
4. Renderer-crash class persists (this sweep: sessions died every ~2–13
   stops under concurrent load) — A15 remains the plan; the maxStops
   machinery + crash labeling kept every run readable.

## 6. Repo state + session notes

Branch `master`. This session's commit: S15a + S15b + netWorker decision +
driver column + docs/1123.md. Battery/sweep/eyetest artifacts on
/mnt/wbterminal2 (not committed; sweep wrapper preserved in the run dir).
ACE pid 3524175 + wsbridge served everything; buildbox untouched; the two
executors were LOCAL Opus 4.8 agents per user direction (NO-VM carries).
Sweep infra: snapshot tree s15-sweep-snapshot-83f5a697 + serve.py :8766
(stopped post-session). Session notes: the `-rn`/`-rln` ripgrep trap was hit
once (output discarded, redone); one `pkill -f` self-kill (exit 144,
harmless — pattern matched own cmdline, the MEMORY.md footgun verbatim);
the fixedGrid probe initially boot-stalled by launching inside ACE's 60 s
reap window after ci-smoke's abrupt close (cross-PROCESS, so boot.mjs's
in-process inter-arm gap couldn't know — same s13 §2 mechanism, new
corollary). MEMORY.md is 24,835 B, over its 24,400 B self-imposed budget
(flagged to user; memory files not touched — autonomous session).
