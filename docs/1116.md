# 1116 — session 8: teleport bake-wait capture → PRIORITY landed (server-LB urgent lane + urgent collision fetch), __diag clobber fix (2026-07-10)

**STANDING DIRECTIVE (carried): NO VM, NO SUBAGENTS/AGENTS.**

Ledger chain: docs/1114.md → docs/1115.md (session 7) → this file (session
8). Executed 1115 §4 top item end-to-end: rapid-telepoi capture → verdict
→ implementation → live re-capture validation.

## 1. Capture (pre-fix arm; scratchpad s8-telepoi-bakewait.json)

10-hop rapid @telepoi window (~4 s dwell vs ~15 s settle; Holtburg →
Rithwic → … → Nanto), nullRender bot on live serve.py. Answers to the
1115 §1 question:

1. **The urgent lane DOES engage** — urgentAsks 60–354 in every hop's 3×3,
   never 0. `getCurrentLbId` (rig-derived) is not stale… once the rig
   moves. Steady-state hops admit the current 3×3 in 18–73 ms.
2. **The starvation lives in the rig-flip gap.** Hop 1 (cold pipe):
   position update named the destination ~1.1 s after @telepoi and the
   load hooks fired — but the rig didn't flip for 17.5 s, and until it
   does `isNearPlayerLb` said "normal lane". The destination 3×3 sat
   cap-skipped 299× for **14.9 s pre-admission** behind the boot town's
   speculative bakes (25–34 s in-run each, cold). Once the rig flipped,
   urgent admission was instant. Compounding it: the rig flip itself
   waits on `ensureTerrainAroundLandblock`'s collision heightmaps (the
   integrator freezes pose Z until they land) — and that fetch rode the
   NORMAL wasm semaphore lane.
3. **In-run is where steady-state time goes**: current-LB statics
   1.1–4.0 s, buildings 0.1–2.8 s with 10–16 bakes in flight.
4. **bakeWorkerStats read null all session** — NOT "worker inactive":
   `installDiag()` (init3D) does a wholesale `window.__diag = diag`,
   wiping the `bakeWorkerStats` surface `configureBakeWorker()` installed
   at boot. Session-7's worker half of the instrumentation was blind from
   birth.

## 2. Verdict: PRIORITY, not flush

Flush buys nothing: stale-town asks stop by themselves the moment the
renderSet flips; in-flight wasm bakes aren't cancellable; urgent bakes
already bypass both the guard cap and the fetch semaphore. The
destination starved ONLY because its asks weren't urgent yet. Landed
(JS-only, wasm untouched):

- **Server-LB urgent lane** — `LandblockLRU.noteServerLb(lbId)` stamped
  from BOTH A15-Q4-SYNC position-update copies (index.html legacy block +
  world_stream.js) BEFORE the load hooks fire; `isNearPlayerLb` now
  honors EITHER the rig center or the server center (Chebyshev ≤ radius
  on each). The two agree outside the teleport window, so walking
  behavior is unchanged; eviction/reclaim still read the rig center only.
  The drift guard's token list doesn't include noteServerLb — sequence
  parity untouched (46/46 green).
- **Urgent collision fetch** — `ensureTerrainAroundLandblock` passes
  `urgent=true` to `fetch_landblock_heightmaps` (it's the rig-blocking
  fetch; both position-update copies share this one implementation via
  deps injection).
- **__diag clobber fix** — `installDiag()` grafts pre-existing `__diag`
  keys onto its new bag before assigning (diag.js keys win on collision;
  single-installation-point rule kept).
- New unit suite `test_landblock_lru_server_urgency.mjs` (17 checks:
  rig-only intact, teleport-window OR, radius honored, full-landblockId
  masking, 0/null clears, agree-case, fail-soft). Existing suites
  untouched-green (evict 30 / null-lb 12 / dualstate 25 / guard / A15-Q4
  46).

## 3. Post-fix validation (scratchpad s8-telepoi-bakewait-POSTFIX.json)

Same 10-hop capture on the fixed tree:

- **Pre-admission starvation ELIMINATED**: hop-1 destination c0 waitMs
  14,918 → **0** (all three kinds), skipCap 299 → 0, asks ==
  urgentAsks from the first fire. Session totals: skipCap 3,608 → 101
  (worst residual wait anywhere: 1.0 s on a cheb>1 normal-lane
  building). urgentAsks 1,234 → 3,539; skipInFlight rose 1,125 → 3,418 —
  urgent re-fires hitting per-key dedup, no duplicated work.
- **Worker stats now visible and active=true**: under the rapid window
  the worker FIFO is genuinely deep — maxPending 58,
  fetchSurfacesPixels avg 2.6 s / max 9.1 s at avgDepth 12.6,
  fetchEntitySurfacesPixels avg 1.9 s at avgDepth 18.9. The in-run half
  of teleport latency is now measurable and lives THERE (+ main-thread
  decode CPU), not in the guard.
- **First-hop landing 17.5 s → 15.1 s only** (single sample): the
  remaining rig-flip delay is in-run cold-start work (hop-1 terrain c0
  dur 18.1 s; worker init + 58-deep FIFO), not lane starvation.
  Steady-state landings unchanged (~260–880 ms).
- **ci-smoke --full**: S0 WARN (benign, unchanged) / L1 / L2 / S4
  keepalive / S5 datDecode / **S6 warmpark PASS with the strict
  noDisposeStorm bound — evicted=0 at all three snapshots again** under
  the new lane. S1 white-tex detector boot-stalled twice in the chain's
  slipstream (back-to-back-session kickDance race — a bare
  launchAndEnter probe between the stalls booted in-world in 1.7 s with
  0 errors), then an instrumented re-run PASSED the full stage:
  entityMeshes=1762, whiteNow=0, greyFallback=0, 0 console errors. Every
  gate green post-change across the two runs.
- Bare-default boot, spawns, 0 console errors in both arms (default-on
  bar; no new flags added — the lane is behavior-internal, escape stays
  `?streamFix=off`).

## 3b. Ops notes

- **ACE_Log.txt appender is DEAD since 21:10:57Z today** (mid-session-7
  wind-down; last entries are a kickDance both-sessions-drop). The server
  itself is fine — every boot since (captures, smoke, probes) played
  normally; only the log file stopped. Fixing it means bouncing ACE
  (user's call — keep-ACE-vanilla; restart recipe in memory/ace-live.md).
  Until then "check the ACE log" answers nothing after that timestamp.
- Headless boot flake: 3 boot-stalls today, all immediately after a prior
  session closed (kickDance/grace race, ~50% when back-to-back); quiet-gap
  retries booted in 1.7 s. Battery drivers already --resume through this;
  one-shot probes should just retry once.

## 4. Owed / carried

- **In-run half (next frontier)**: worker-FIFO priority for
  urgent requests (the FIFO is depth-58 under rapid teleports while the
  current LB's surfaces queue behind stale-town decodes) and/or
  main-thread decode yield. This owns the residual ~15 s cold-pipe
  first-hop landing. Instrument first: `__diag.bakeWorkerStats()` now
  actually works — capture per-type depth across a TN-cluster battery
  before choosing.
- netWorker=1 promotion still HELD on the settle-criterion
  disambiguation (workDelta-plateau driver iteration, next campaign;
  1114 §2.5).
- 1070 batch unchanged (A02 ACK probe, warmPark post-flip verify, 4
  eye-risk flags, cooling inspection — 1113 §4).
- MEMORY.md ~400 B over its load budget (memory edits user-directed;
  carried).
- Battery re-read under the new lane (park Σ / settle columns) rides the
  next campaign — the s8 capture arms are internally comparable but are
  not battery rows.

## 5. Repo state

Branch `master`. Session-8 commit: server-LB urgent lane
(landblock_lru.js noteServerLb + isNearPlayerLb OR, both A15-Q4 stamps) +
urgent collision heightmaps + diag graft fix + server-urgency unit suite +
this ledger. wasm UNCHANGED (JS-only session; pkg release 4,759,836 B
from session 6 still current — S0 WARN-stale-predates-rust-commit remains
benign).
