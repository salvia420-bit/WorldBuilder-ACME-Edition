# 1115 — session 7: TN park↔unpark storm FIXED (+ratchet), bake-wait instrumentation, netWorker re-read (2026-07-10)

**STANDING DIRECTIVE (carried): NO VM, NO SUBAGENTS/AGENTS.**

Ledger chain: docs/1113.md → docs/1114.md (session 6) → this file (session
7). Executed 1114 §5 top item (storm fix + noDisposeStorm ratchet), the §3
instrumentation groundwork, and the netWorker=1 follow-up.

## 1. Landed this session

- **TN-transition park↔unpark storm FIX** (1114 §2b/§5 top item) — restores
  the `entries` XOR `parkPool` invariant at both ends (landblock_lru.js):
  1. *In-flight reclaim deferral*: victim selection (normal at-cap AND both
     sealed-purge arms) skips any LB whose guarded bake is in flight
     (`_hasInFlightBake` reads `scene3d._streamGuardState.inFlight`; the
     three loaders call track() INSIDE the guarded run, so membership
     exactly brackets the race window). Deferred LBs reclaim a tick later
     via the documented sealed "straggler" flow. Warm-park only — classic
     evict+re-bake semantics stay byte-identical to the battery baseline.
  2. *track-while-parked merge*: a track() that still lands while parked
     (envcell build resolving past its park-time cancellation, setup-lights
     rescan, a bake fired against a parked LB's unset kind-mark) merges its
     disposables into the pool copy instead of creating an `entries` entry.
     No attach/detach → the rejected unpark-on-track duplication hazard
     can't occur; both pool exits stay consistent (unpark restores the
     merged entry; disposeParked→evict removes attached strays by userData
     tag and disposes the merged lists).
  - Telemetry: `getStats().trackMergedWhileParked` /
    `reclaimDeferredInFlight`; battery driver samples them per stop
    (`trackMerged`/`reclaimDeferred` — passive fields only, settle
    criterion untouched so arms stay comparable).
  - New unit suite `test_landblock_lru_warmpark_dualstate.mjs` (25 checks:
    deferral, sealed straggler flow, merge, both pool exits, forced legacy
    dual-state self-heal; forces `?warmPark=on` via a window stub before
    dynamic import — the static-import suites stay classic by design).
    Existing suites untouched-green (30/12/9). NOTE:
    `test_materials_paletted_lru.mjs` is PRE-broken (its harness
    Function()-evals materials.js, which now has ES imports) — never part
    of the green set, unrelated.
- **S6 noDisposeStorm RATCHETED 1500 → +5** (1114 §2b owed) —
  warmpark-roundtrip-probe.mjs + ci-smoke.sh comment. VALIDATED: full
  ci-smoke --full ALL GREEN with the strict bound; the probe measured
  **evicted=0 at all three snapshots** (pre / tn-sealed / returned; was
  74/299/614 pre-fix) with 2,208 park ops cycling through the transition —
  the 1112 landing behavior is restored. (S0 WARN-stale-predates-rust-commit
  is benign: pkg built 21:16Z from the same tree the 21:22Z session-6
  commit captured; no Rust changes since.)
- **Bake-wait instrumentation** (1114 §3, instrument-first) —
  - stream_bake_guard.js: per-(kind,LB) lifecycle records on the guard
    state bag — asks/urgentAsks, skip attribution (inFlight/cooldown/cap),
    pre-admission waitMs (firstAsk→start), in-run durMs, queue depth at
    admission, cycle restart on settle. `summarizeStreamBakeWait()` →
    **`window.__diag.bakeWait()`** (installed with the state bag; capped
    1024 keys).
  - bake_worker_client.js: per-request latency + pending-depth aggregates
    by message type → **`window.__diag.bakeWorkerStats()`**.
  - Guard suite extended (+9 assertions), green. The question the next
    rapid-telepoi capture must answer: does the CURRENT LB's bake wait
    pre-admission (guard cap — and does the urgent lane even engage?
    `urgentAsks=0` post-teleport would mean the pose-freeze staleness
    reaches `isNearPlayerLb` exactly like it reached the reclaim center)
    or in-run (wasm fetch semaphore / worker FIFO — bakeWorkerStats
    depth)? Then choose flush vs priority.

## 2. Battery re-read (evening pair; full rows in scratchpad s7-*.json)

| arm (bare defaults = wp+gate+hyst+storm-fix) | active | settleMed | satMed (lru≥200) | capped | park Σ (per segment) |
|---|---|---|---|---|---|
| s7-bare               | 829.7 s | 14.8 s | 15.0 s (n=36) | 21/62 | 27.4k over 53 stops |
| s7-networker (netWorker=1) | 668.2 s | 9.3 s | 19.2 s (n=31) | 14/62 | segmented (2 crashes) |

⚠ Comparability: BOTH arms had SwiftShader renderer crashes mid-cycle
(1×/2×) with --resume relaunches, and the evening environment ran ~40%
slower than the morning s6 set (do NOT read s7-bare 830 s against s6's
588 s as a fix regression — the fix's own counters barely engaged outside
TN stops: 126 deferrals, 0 merges). The s7 pair is internally comparable.

1. **Storm-fix live validation at scale**: every nonzero evicted delta in
   both arms occurs late-cycle with parked ≈ 1,700–2,000 LBs — i.e. the
   pool AT its 256 MB byte budget (S6 probe calibration: ~160 KB/parked
   LB → ~1,800 parked ≈ budget). Those are the designed farthest-first
   ≤2/tick pressure disposes, not the dual-state storm. Early-cycle and
   fresh-pool segments: evicted=0 everywhere. `reclaimDeferred` steps up
   exactly at the TN dungeon cluster (43→126) — the deferral engages
   where the storm lived; `trackMerged` 0–1 total (the deferral catches
   the race before the defensive merge is needed).
2. **netWorker=1 saturated-tail REPLICATES under the new defaults**
   (19.2 s vs s6's 19.6 s classic read) while the arm is again faster
   overall with lower churn. Mechanism hypothesis: the worker-drained
   wire keeps deliveries landing during main-thread bake saturation, so
   the scene-stability settle criterion keeps resetting (a metric
   interaction, not necessarily user-visible lag). The 1114 §2.5 driver
   iteration (settle on workDelta plateau, next campaign) disambiguates.
   HOLD promotion until that read exists.
3. Late-cycle pool-at-budget is the next churn frontier: each dungeon
   transition parks +1.7–5.1k ops (the park↔unpark cycling the
   teleport-flush work targets) and pressure-disposes a few hundred
   farthest LBs. Flush/reprioritization (§1 instrumentation) should cut
   the park churn; if pressure disposes still bite afterwards, consider
   budget/estimate tuning.

## 3. Ops notes
- The 1114 §4 ghost-session "Unsolicited Packet Id 0" stream is GONE
  (ACE log clean; the stale 1070 page/tunnel died on its own). No 1070
  action needed for it.
- Arms + smoke all ran on live serve.py :8765 with nosw boots; ACE
  vanilla untouched.

## 4. Owed / carried
- **Teleport fetch/bake flush** (1114 §3/§5) — instrumentation now in
  place; next session: capture `__diag.bakeWait()` +
  `__diag.bakeWorkerStats()` across a rapid-telepoi window, check the
  urgent-lane engagement question (§1), then choose flush vs priority.
  This also owns the TN park-churn (parkΣ spikes, §2.3).
- netWorker=1: promotion HELD on the settle-criterion disambiguation
  (workDelta-plateau driver iteration, next campaign — NOT mid-campaign).
- A02 ACK packet-rate live probe (no tcpdump locally) + warmPark 1070
  post-flip verification + 4 eye-risk flags (terrainBatch, horizonFade,
  portalStencil, terrainSlopeShading) — all batched for the next 1070
  session (cooling inspection still pending, 1113 §4).
- MEMORY.md ~400 B over its load budget (memory edits user-directed;
  carried).

## 5. Repo state
Branch `net-review-wave2`. Session-7 commit: storm fix + dual-state unit
suite + noDisposeStorm ratchet (+5) + bake-wait/bakeWorker diag surfaces +
guard-suite extension + battery telemetry columns + this ledger. wasm
UNCHANGED (JS-only session; pkg release 4,759,836 B from session 6 still
current).
