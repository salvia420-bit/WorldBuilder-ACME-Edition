# 1114 — session 6: reclaim fixes landed + re-A/B, net ventures, starvation diagnosis (2026-07-10)

**STANDING DIRECTIVE (carried): NO VM, NO SUBAGENTS/AGENTS.**

Ledger chain: docs/1112.md → docs/1113.md (session 5) → this file (session
6). Executed 1113 §2 items 1, 2, 4 and §3 ventures A02/A13/A15, plus the
control arms and the empty-screenshot root-cause (user report).

## 1. Landed this session

- **Reclaim-center freshness gate** (1113 §2.1) — `landblock_lru.js`
  `RECLAIM_GATE_ON`: on a ≥2-LB Chebyshev center jump, at-cap victim
  selection holds until the new center LB's bake is tracked (10 s hard
  cap). `?reclaimGate=off` escape, default ON. `getStats()` gains
  `centerJumps`/`gateHeldTicks` (+`ringFloor`/`reclaimMinAgeMs`).
- **Park hysteresis** (1113 §2.2, A11-F7) — candidates younger than
  `?reclaimMinAgeMs` (default 2000, 0=off) are never victims. Soft cap:
  an all-young tick evicts nothing. `test_landblock_lru_null_lb.mjs`
  updated (it asserted the exact same-instant reclaim the fix removes);
  LRU suites green (12/12, 30/30, 9/9).
- **`?lbRingFloor=N|ringMax`** — floor as a query arm (scene3d/index.js);
  url-flags.md rows for all three new flags (L1 --strict green).
- **Battery driver** (1113 §2.4) — duplicate POIs (verified identical
  destinations in `ace_world.points_of_interest`: Hotel triple, Night Club
  pair) → `noMove`, not failures (exit code honors it); per-stop
  `workDelta` (bake-worker requests via new `window.__bakeWorkerSeq`) and
  `reclaimDelta` columns + medians.
- **A02 Opt-2 ACK coalescing** (Rust, holtburger-session `send.rs`) — one
  deferred cleartext ACK per 500 ms window (`ACK_COALESCE_MS`), payload
  overwritten in place (cumulative semantics verified against ACE's
  `< sequence` prune, NetworkSession.cs:669; call site is strict delivery
  order). Original deadline kept → no starvation. Tests 32/32.
- **A13 leak pair** (Rust) — fragment reassembler capped at 256 pending
  groups, oldest (smallest blob seq) dropped with a warn + unit test;
  `WsTransport` `on_open` closure stored on the struct instead of
  `forget()` (leaked per RECONNECT, not per page).
- **A15 frame.rs copy-sync lint** — folded into `lint-wire-codec.mjs`
  (ci-smoke L2): consts by value + normalized `decode_frame` bodies. It
  immediately caught real drift: the wasm copy's `encode_frame` lacks the
  wsbridge copy's size guard — WAIVED with reason (`FRAME_FN_WAIVERS`),
  fix owed with the next wasm rebuild (bridge-side decode enforces the cap
  on that direction meanwhile).
- **A15 waiver CLOSED same session**: transport-ws `encode_frame` now
  mirrors the wsbridge guard (Result + MAX_PACKET_BYTES check; caller
  `send_to` updated); `FRAME_FN_WAIVERS` is empty — 2 consts + 2 fn
  bodies in sync.
- **wasm REBUILT + validated**: pkg release 4,759,836 B (2026-07-10
  21:16Z) carries A02 + A13×2 + the frame guard; ci-smoke --full ALL
  GREEN (S0/L1/L2, boot, 0 console errors, whiteNow=0, keepalive,
  datDecode). ⚠ Any A/B against pre-session-6 numbers now spans a wasm
  change (ACK cadence!) — treat 2026-07-10 morning numbers as a closed
  baseline set.

## 2. Battery results (full detail: scripts/net-review/battery-findings-2026-07-10-s6.md)

Seven arms, same driver/day. The matrix that matters:

| config | active | settleMed | capped | reclaim Σ / max |
|---|---|---|---|---|
| classic, fixes OFF (control) | 688 s | 9.2 s | 17 | 10,104 / 3,656 |
| classic, fixes ON            | 805 s | 14.5 s | 18 | 17,803 / 3,786 |
| classic+ringFloor, fixes ON  | 799 s | 14.9 s | 19 | 17,567 / 3,691 |
| **warmPark, fixes ON**       | **588 s** | **8.8 s** | **10** | 11,751 / 7,135 |
| warmPark, fixes OFF (control)| 682 s | 10.4 s | 15 | 11,866 / 3,799 |
| warmPark+ringFloor, fixes ON | 657 s | 9.2 s | 15 | **6,310 / 984** |
| netWorker=1 (classic, fixes ON) | 692 s | 10.3 s | 17 | 6,302 / 2,536 |

1. **Fixes are a ~14% win under warmPark, a regression in classic** →
   **DECISION LANDED: `reclaimGate`/`reclaimMinAgeMs` defaults FOLLOW
   `?warmPark`** (on/2000 warm, off/0 classic; explicit override wins).
   Bare-URL behavior stays byte-identical to pre-session-6. Mechanism:
   classic LRU recency already protects the arriving ring when reclaim
   = dispose+re-bake; hysteresis instead cycles mid-ring LBs.
2. **Finding-3 REVERSED as predicted** — with the gate, wp+ringFloor went
   from worst churner (8,515 parks, satMed 16.0 s yesterday) to lowest
   total churn with 7× lower worst-stop max, at ~12% active cost vs plain
   wp. Floor stays default 1; revisit when churn/GC is the complaint.
3. ~121 reclaims/stop at saturation is legitimate ring TURNOVER (cap 203
   vs ring 169), identical everywhere — 1113's "<10/stop" target chased
   the wrong metric; judge Σ and worst-stop max.
4. **netWorker=1 first valid read: no regression, likely faster** (692 vs
   805 s against its like-for-like control) but worst saturated tail
   (satMed 19.6 s) — needs a look before promoting.
5. Driver iteration for next campaign (NOT mid-campaign): settle on
   workDelta plateau, not scene-count stability (workMed ~100 in every
   arm while settle readings spread 60%); gate the next teleport on
   progress to stop the early-cycle starvation cascade (§3).

## 3. Empty-screenshot root cause (user report on shots-wp-off)

The ~10 void screenshots are the FIRST ~10 stops (alphabetical order:
Ahurenga→Cragstone). ONE blockage, not two: the ACE wire was alive (every
void stop landed in ~300 ms; the tell is the missing LOCAL PLAYER rig at
Al-Arqas). Radar blips require completed spawns (`entityMap.set` at Step
E, entities.js:3969) → wire-delivered NPCs with starved geometry are
radar-invisible. Terrain + all rigs funnel through shard-fetch → wasm
decode → bake; 10 rapid teleports (starved stops fast-settle → driver hops
on) pile up ~10 towns of ring work that then lands as one burst (lru 11→57
on the 1070, 9→98 laptop — same signature locally ⇒ not the tunnel).

**Fix candidate (next session): teleport flush/reprioritization for
speculative ring work** — analogue of P4 `spawnTeleportFlush`. Verified
seams: JS guard skips-not-queues at cap 6 with an urgent exemption
(`stream_bake_guard.js` `STREAM_BAKE_DEFAULT_MAX_IN_FLIGHT`, `opts.urgent`);
the real FIFOs are the wasm fetch semaphore
(`holtburger-resource-http/src/manifest_source.rs` `fetch_sem`,
`prefetch_urgent` bypass) and the bake-worker request stream
(`bake_worker_client.js` `_seq`). Instrument WHERE the current-LB bake
waits during early-cycle churn before choosing flush vs priority.
Driver-side: gate the next teleport on progress (workDelta>0 or the
current LB's terrain mark), not just stability — do this only BETWEEN
battery campaigns (mid-campaign driver edits break arm comparability).

## 4. Ops notes
- ACE log shows a continuous "Unsolicited Packet from 127.0.0.1:47156 Id 0"
  stream through the wsbridge — a ghost page from the paused 1070 arm-2
  setup still connected via the standing SSH tunnel, retrying a dead
  session (~1/s). Harmless to accounts (ACE ignores Id-0), but kill the
  1070-side test chrome (match `--user-data-dir`, per runbook) or the
  tunnel on the next 1070 session to silence it. First local boot of the
  evening stalled once (ghost-session window); retry succeeded.
- 1070 items still deferred pending the cooling inspection (1113 §4).
- MEMORY.md remains ~400 B over its load budget (memory edits are
  user-directed; carried).
- The `pgrep -f`/`rg -rn` footguns both nearly fired again this session;
  both caught. `pkill -x rust-analyzer` is the safe form.

## 5. Owed / carried
- A02 ACK packet-rate live probe (unit-tested + green boot chain, but no
  on-wire pps A/B yet — no tcpdump on the laptop; batch with the next
  1070 session alongside the 1112/1113 carried items).
- netWorker=1 saturated-tail investigation (§2.4).
- Teleport fetch/bake flush (§3) — the highest-value client fix found
  this session; instrument first.
- warmPark 1070 arm + eye-gate + default-flip bundle (now including the
  conditional gate/hysteresis) — still pending the cooling inspection.
- 58 shots-wp-off screenshots Taildropped to redmi-note-13-5g (user
  request); the ~10 "void" ones are diagnosed, not a render bug (§3).
- Battery screenshots for round-1/2 arms were not taken (local
  nullRender arms; `--shots` is cdp-only).

## 6. Repo state
Branch `net-review-wave2`. Session-6 commit: LRU gate+hysteresis
(warmPark-conditional defaults) + lbRingFloor + battery driver columns +
A02 + A13×2 + A15 lint (waiver closed) + frame guard + docs + rebuilt
release pkg (4,759,836 B, ci-smoke --full green).
