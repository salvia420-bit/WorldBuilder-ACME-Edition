# 1119 — session 11: TN park↔unpark storm ROOT-CAUSED + FIXED (sealedKeepRing) + battery false-settle guard (2026-07-11)

**STANDING DIRECTIVE (carried): NO VM.** User EXPLICITLY directed this
session be orchestrated with 2 local Opus implementation agents (a
one-session exception, like the s9/s10 researcher exceptions; the
coordinator scoped the sites first, reviewed both diffs, and ran the
validation gate itself). The directive otherwise carries.

Ledger chain: docs/1117.md → docs/1118.md (session 10) → this file.
Executed 1118 §4 items 1 and 2: the TN storm (top item) and the battery
driver follow-ups. JS-only session — wasm UNTOUCHED.

## 1. Landed: sealed-purge keep-ring — the TN storm root cause was a purge↔streamer fight, not residual gate/hysteresis gaps

All the previously-proposed avenues (reclaim gate, park hysteresis,
in-flight deferral, dual-state merge) were already landed and the storm
persisted (1118 §2: TN 3232 / Town Network 3836 / Underground 3355
reclaim ops per 25 s stop vs ~121 for ordinary capped towns). The s11
diagnostic (live PRE probe, 3 sealed hops) disproved the keep-key-flap
hypothesis — `_sealedEvictLbKey` flaps 0–1 over the whole dwell — and
pinned the real loop:

- `tickEviction`'s sealed path parks EVERY resident LB except the keep
  LB, bypassing the 3×3 always-resident floor ("no outdoor LB is
  visible");
- but `world_stream.js::onPositionUpdate` fires
  `loadTerrainForLandblock` for the player's FULL 3×3 on every
  local-player position packet, and the loaders' already-baked
  fast-path UNPARKS any parked LB it hits;
- so purge and streamer fight over the keep LB's own outdoor ring the
  entire sealed dwell: measured park/unpark Town Network **3459/3445**,
  Underground 3027/3018, Storage 5961/5952 — park≈unpark, a pure
  re-attach ping-pong (~9 ring LBs × ~350 position packets / 25 s).
  Classic mode is WORSE (evict clears the baked mark → re-DECODE).

**Fix** (`scene3d/landblock_lru.js`): both sealed victim loops exempt
the keep LB's 3×3 (`lbChebyshev(keep, key) <= SEALED_KEEP_RING_FLOOR`)
— exactly the floor the normal path and the streaming layer already
honor. Everything beyond the 3×3 is still purged (the ~118-of-127
backlog `sealedEvict` exists for); the 9 kept LBs are flat skirt
terrain already hidden by `sealedCull` (zero draw, ~1.4 MB concession).
`?sealedKeepRing` DEFAULT-ON with `=off` legacy escape — unconditional
like the rest of the sealed family (the fight exists in classic AND
warm-park), NOT warmPark-conditional. url-flags.md row added.

**POST (same hops): park/unpark 3459/3445 → 18/0, 3027/3018 → 6/0,
5961/5952 → 0/0 — unpark exactly 0 at all three dungeons (the churning
set was precisely the 3×3), 0 console errors, settle not worse.**
Artifacts: `/mnt/wbterminal2/holtburger-scratch/s11-tn-storm-20260711T021720Z/`
(PRE/POST JSON + probe provenance).

## 2. Landed: battery driver false-settle guard + sessionIdx (1118 §4 item 2)

`scripts/net-review/battery-telepoi.mjs`:

- **Settle guard** (extends 1113 finding 5 / the s10 false-settle race):
  the 3×500 ms stability window can only START counting once streamed
  work has actually begun (workDelta since teleport ≥ `--settleWorkMin`,
  default 5) OR a grace floor (`--settleFloorMs`, default 3000) elapsed
  with work still ≈0 — a cold/backlogged pipe no longer "settles" in
  ~0.5 s before its first delta and then storms the pipe with
  rapid-fire hops. Genuinely idle stops (same-LB/no-move duplicates)
  settle via the floor, NOT the 25 s cap. Fail-soft: pages without
  `__bakeWorkerSeq` behave byte-identically to pre-guard. Rows gain
  `settleGuard` ("work"|"floor"|null) + `lowWork`; summary gains knob
  provenance + guard counts. ⚠ guarded settleMs is NOT comparable to
  pre-guard arms (header + summary comments, 1117 §2 style). The settle
  decision is factored into a pure `settleStep` reducer
  (`<settle-guard>` markers) — harness-driven 12/12 across cold-pipe /
  idle-dup / null-work / normal cases.
- **sessionIdx**: every row stamps the 0-based driver-invocation index
  (`--resume` continues from max prior +1; pre-feature rows backfill to
  0; one invocation == one browser session — deaths abort and resume).
  Summary gains `sessions` + `settleMedBySession` [{sessionIdx, n,
  settleMedMs, reclaimMedMs}] so arm medians can be compared
  session-age-matched automatically (the s10 §2 segmentation confound).

## 3. Validation

- 8/8 unit suites green, re-run by the coordinator after both agents
  landed: evict **39** (was 30; +Test 8 sealed keep-ring purge shape) /
  NEW sealed-keepring **5** (`=off` restores legacy) / null-lb 12 /
  server-urgency 17 / dualstate 25 / stream-bake-guard / A15-Q4 46 /
  queue 34. `node --check` battery driver OK.
- **ci-smoke --full: S0 WARN-stale-predates-rust-commit (benign,
  unchanged — pkg built from byte-identical source just before the s10
  commit; no Rust touched this session) / L1 strict / L2 / S1 boot /
  S2 0 console errors / S3 white-tex whiteNow=0 / S4 keepalive /
  S5 datDecode negCache=0 miss=0 / S6 warmpark ALL GREEN.** Bare-default
  bar met with sealedKeepRing default-ON.

## 4. Owed / carried

- **Full 62-POI battery re-read** under sealedKeepRing + the guarded
  driver — the s10 arm medians are now doubly stale (storm fixed AND
  settle semantics changed); expect the TN-cluster 25 s caps to drop
  out. Next campaign item, alongside the `?bakeQueueCap` 2/4/8 sweep.
- Decode-side capacity (batch splitting / second worker) still owed —
  the ~15 s first-hop cold pipe is now the top residual again.
  Wasm-threads endgame ballpark stands (1118 §5).
- netWorker=1 promotion still HELD. 1070 batch unchanged (1113 §4).
  MEMORY.md still ~400 B over budget (user-directed edits only).

## 5. Repo state

Branch `master`. Session-11 commit: sealedKeepRing (landblock_lru.js
both sealed victim loops + flag consts) + url-flags row + evict suite
39 + new sealed-keepring suite + battery driver guard/sessionIdx + this
ledger. wasm UNCHANGED (pkg release 4,791,563 B from s10 still
current). Battery artifacts on /mnt/wbterminal2 (not committed).
