# 1124 — session 16: fixedGrid SOAKED + FLIPPED DEFAULT-ON, S15c park wiring landed, S16 palette-composed surface cache (wasm), wasmMem ci gate wired, batch-shape canary re-baselined (2026-07-11)

Ledger chain: docs/1121.md (S13) → docs/1122.md (S14) → docs/1123.md (S15) →
this file. Executed the docs/1123.md §5 owed list minus the excluded surfaces
(user directive: NO VM — buildbox untouched; NO 1070 — the eye-test debt
carries, §5). Coordinator (orchestrator/researcher) + Opus 4.8 executors in
waves of two (A soak / B rust, then C S15c / D flip+validate), file-ownership
partition, every agent citation read-verified per house rule.

## 1. What landed (1123 §5 item → code)

- **fixedGrid A/B soak run + DEFAULT-ON FLIP** (1123 §5.1; plan §4 gates):
  2-arm interleaved 62-POI battery (default vs `?fixedGrid=1`) on a frozen
  snapshot of S15 HEAD 8efcb5e5 at :8766, v2 protocol byte-identical to the
  s15 sweep (--maxStops 21, wrapper-owned 65 s gaps), both arms 62/62.
  Honest read §2 → flip executed: `FIXED_GRID_ENABLED`
  (scene3d/index.js:173) is now default-ON with `off`/`0`/`false` escape
  (footgun-safe reader, absent→ON); test_fixed_grid truth table repinned;
  url-flags fixedGrid row → **on** citing the soak.
- **S15c — releaseEdge → real whole-LB park + diag grace** (1123 §5.1
  tail): the S15b no-op seam now feeds a NEW pure `EdgeParkScheduler`
  (scene3d/fixed_grid.js): a vacated edge key parks via `landblockLru.park`
  only after staying continuously vacated for a 2 s dwell (hysteresis kills
  the s11 park↔unpark-storm class; a re-entering edge cancels the pending
  park — zig-zag issues ZERO parks, test-pinned). Layer coverage is pure
  residency: park() already detaches terrain/buildings/statics/cells, and
  outdoor animated scenery rides staticsGroup with userData.landblockId
  (animated_scenery.js:303-304), so buildings/statics/scenery join with
  fetch policy byte-identical (1-LB-on-entry + PVS + boot rings unchanged).
  Sub-escape `?fixedGridPark=off` (default-ON within fixedGrid) isolates
  park from residency for future A/Bs. Plus the 1123 §3 owed diag grace:
  under `?diag=1`, `unbacked` within 3 s of a crossing is classified
  transient (counted, not warned); untracked/offBlock never graced. NEW
  test_fixed_grid_park.mjs (33 checks: exact vacated-set park, re-adopt
  within the UseTime floor, zig-zag no-storm bound, teleport bypasses park,
  grace matrix, layer adoption). Telemetry: `__fixedGrid.getStats().park`
  {parksIssued, reAdoptCancels, …} + `__fixedGridPark`.
- **S16 — palette-composed entity-surface cache** (1122 §5.6 → 1123 §5.3):
  the B1 bypass ("palette-composed decodes bypass it") is gone. The one
  shared 96 MiB ByteBudgetLru is widened to a generic key,
  `SurfaceCacheKey::{PaletteFree(did) | Composed{did, base_palette_id,
  sub_palettes}}` — exact tuple, no hash (a collision would bleed one dye
  variant onto another); sub_palettes kept AS GIVEN (splice order is
  load-bearing). Same completeness+sentinel discipline; both classes
  compete in ONE budget by true recency (the retail DBOCache invariant).
  Flag `?palSurfaceCache` default-ON `off`-escape, master `surfaceCache=off`
  still kills all. Census/amp semantics untouched (S5b amp gate intact);
  additive diag fields surfaceCachePal{Hits,Misses,Inserts,Entries}.
  17 host tests in tests_surface_cache (parity byte-identical, dye variants
  don't cross-contaminate, sentinel, mixed-class eviction, flag matrix).
  pkg/ rebuilt --release: 4,777,519 → 4,785,917 B (prior wasm backed up in
  /mnt/wbterminal2/holtburger-scratch/s16-pkg-backup/). Known pre-existing:
  tests_substitution::triangulate_setup_model_with_substitutions… fails on
  pristine 8efcb5e5 too (stash-verified; not in this diff).
- **wasmMem ci gate** (1123 §5.3): ci-smoke S5b now FAILs on egregious
  wasm-memory runaway at smoke time — main > 600 MB / worker > 300 MB
  (in-code justification cites the smoke baselines 382/104 MB and the
  soak's banked 21-stop med/max 383/679 main, 180–206 worker); fail-soft
  SKIP on legacy pkg. Proof run green (390/153 MB).

## 2. The soak, read honestly (run dir battery-s16soak-20260711T154025Z)

- Paired per-POI deltas (fixedgrid − default) on the 52 POIs landed in both
  arms: **median 0 ms** (25 faster / 18 slower / 9 ties); excluding
  dwell-capped stops (n=31): −266 ms median, mildly favorable, within
  noise. Headline settleMed 11,394 vs 11,533 (+1.2%) sits entirely inside
  session-fragmentation noise (6 sessions/arm, per-session medians swing
  ~5×) — the paired read is the number. Renderer deaths **3 vs 3**
  (arm-equal; the pre-existing class, 1123 §5.4), boot-error 1 vs 0,
  capped 16 vs 15, wasmMem parity (max 679/679).
- Short-hop leg (Holtburg, 6 LB west + back east inside the 30 s floor):
  12/12 hops took the SHIFT path (0 whole-grid invalidates), east re-entry
  ≈ zero decode work, terrainBakedLbs flat until the final ring expansion,
  **3,348 steady-state grid updates did zero terrain work** (the OFF path
  re-runs the ring per position packet), 0 console errors both arms.
- Gate verdicts: §4.2 PASS (teleport parity; deaths arm-equal), §4.3 PASS
  on substance with ONE literal miss — surfaceCache walk hit-rate ~22% in
  BOTH arms (arm delta +0.1pp), a property of B1's compulsory misses over
  fresh terrain (matches the s14 canary ≈23%), so the plan's ">60%" number
  is re-baselined, not the flag; §4.4 PASS (diag asserts clean modulo the
  known transient class — 3 warns, all in the freshest-streaming hop).
  Unit suites green on and off. **Flip recommended and executed.**
- Carried caveat: the soak snapshot predates S15c, so the park path's ship
  gate was §3's live validation, not the battery. Plan §4.5 (1070 seam/LOD
  shift eye-test) still owed; `?fixedGrid=off` covers the interim.

## 3. Live validation (bare default now = grid + park + palette cache + new wasm)

- **ci-smoke --full: PASS end-to-end** — S0 wasm 4,785,917 B release-sized,
  boot 7,096 ms, errors=0, white=0 (entityMeshes 1121, greyFallback 0),
  keepalive/datDecode/warmpark PASS, S5b PASS (worker amp=1.00, mem gate
  green 390/153 MB).
- **Batch-shape canary re-baselined** (first smoke run's ONE red,
  attributed by live A/B before touching the gate): fixedGrid-ON collapses
  the redundant 9-LB ring re-runs (multi-batches 238→5, 2,256→159
  LB-fetches, 14× less terrain work) while solo fetches stay IDENTICAL
  (114 both arms) — soloShare rose only because the batched denominator
  shrank. ci-smoke S5b now flags the true N+1 signature (high soloShare
  AND zero multi-LB batches); the pre-A4 storm sample still FAILs.
- **Palette-cache probe** (Town Network): composed cache live — main
  292h/384m (43% hit-rate), palEntries 261; worker 496h/470m (51%);
  legacy fields sane, worker amp=1.00, 0 errors.
- **S15c park probe** (Holtburg 6W+6E, bare default + diag): westParks 13,
  finalParks 31, east re-adopts (unparkedTotal 13) with ~zero re-entry
  decode, 0 console errors; 93 grace-suppressed vs 17 loud warns, the loud
  ones confined to the initial post-teleport bake window on the pressured
  SwiftShader laptop (the known 1123 §3 transient class exceeding the 3 s
  grace under headless bake slowness) — quiet from hop 3 onward.

## 4. Gates (all green before commit; coordinator re-ran on the merged tree)

- Combined unit battery: park_usetime 27/0, fixed_grid 39/0,
  **fixed_grid_park 33/0 (NEW)**, terrain_ring_batch 23/0, lru_evict 39/0,
  warmpark_dualstate 25/0, sealed_keepring 5/0, null_lb 12/0,
  server_urgency 17/0, light_eviction 9/0, a15_q4 47/0 (drift guard —
  zero streaming-block edits), lb_objects_shared 13/0, init3d_idempotency
  10/0 — **299 checks, 0 failures**. (1123 §4 said "267" for the prior 12
  suites; measured 266 — a 1-check ledger arithmetic drift, corrected here.)
- Rust: tests_surface_cache 17/0 (scoped), full --lib 149/1 with the ONE
  failure pre-existing on pristine HEAD (§1). node --check clean; lints
  url-flags --strict PASS (rows: fixedGrid flip, NEW fixedGridPark, NEW
  palSurfaceCache, surfaceCache note), harness-params PASS, bash -n clean
  on ci-smoke.sh.
- Flag-escape identity: `?fixedGrid=off` restores the pre-S15 path (the
  short-circuit reads the same const; S15b's flag-off byte-identity audit
  carries, direction inverted); `?fixedGridPark=off` = soaked S15b
  behavior exactly.

## 5. Owed / carried → session 17

1. **fixedGrid+park battery under the NEW default** — the park path is
   probe-proven, not battery-soaked (§2 caveat). Next battery bakes
   baselines for the flipped default (also refreshes the wasmMem gate's
   trajectory numbers and gives the §4.3 hit-rate a post-palette-cache
   re-read — the composed cache should lift the walk hit-rate above B1's
   ~22% compulsory-miss floor).
2. **1070 owed grows** (box offline streak continues): B1 surface-cache
   real-GPU eye-test (groundwork banked, 1123 §5.2), plan §4.5 fixedGrid
   seam/LOD shift eye-test — now MORE urgent post-flip, dye-variant visual
   spot-check for the composed cache, static_batch_x + anim-scenery
   follow-ons (A13b/c).
3. **S16 wasm follow-on candidates**: investigate the pre-existing
   tests_substitution failure (§1); palette-composed cache eviction
   telemetry under a dye-heavy battery (TN palEntries 261 is modest — the
   96 MiB shared budget looks comfortable, verify at scale).
4. Renderer-crash class persists (3/arm this soak under concurrent agent
   load) — unchanged plan: A15 residency line continues; crash-labeled
   resume machinery keeps runs readable.

## 6. Repo state + session notes

Branch `master`, this commit: S16 palette cache + S15c park/grace +
fixedGrid default-ON flip + ci-smoke gates + docs/1124.md, pushed to
origin/master. pkg/ (gitignored) holds the release wasm 4,785,917 B —
rebuild with `capped-build wasm-pack … --release` after pulling. Battery/
probe artifacts under /mnt/wbterminal2/holtburger-scratch/
battery-s16soak-20260711T154025Z/ (not committed; provenance.txt inside);
snapshot tree s16-soak-snapshot-8efcb5e5 retained; snapshot serve :8766
stopped post-validation; live serve :8765 + ACE pid 3524175 untouched
throughout. Session notes: executor A's detached-sweep completion event
did not wake it once (54 min idle with data on disk — coordinator nudge
recovered; babysit detached long-runs with an explicit in-turn watcher);
the palette probe's JSON write hit a benign argv bug AFTER data capture
(numbers preserved in palette-probe.log); no `-rln` incidents this
session. MEMORY.md remains over its load budget (flagged to user in-chat;
memory files not touched).
